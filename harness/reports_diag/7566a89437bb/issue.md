# [guard / specialization / compiled-artifact cache] warm-cache result differs from a cold compile when `dtype` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

changing 'dtype' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: the cached artifact was reused under assumptions that no longer hold

Signature `7566a89437bb` · case `op_sparse_sampled_addmm_0` · probe `warm`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _base=base_args) -> Tuple[Any, ...]:
        return _apply_context(tuple(_clone(a) for a in _base), ctx)

# contexts:
#   base: {'variant': 'base'}
#   noncontig: {'variant': 'noncontig'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}

```

## Expected vs actual

- **cold_inductor vs warm_inductor**
  - `mutation` input[0] not comparable after call

## Trigger condition

Changed factor:

```
dtype
```

Controlled factors:

```
variant: unchanged (base)
```

## Cache / specialization evidence

| execution | result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | matches reference |
| compiled A then B (warm) | **differs** |
| recompile triggered by the switch | **no** |

## Suspected stage

```
guard / specialization / compiled-artifact cache
```

Attribution basis: changing 'dtype' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: the cached artifact was reused under assumptions that no longer hold

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

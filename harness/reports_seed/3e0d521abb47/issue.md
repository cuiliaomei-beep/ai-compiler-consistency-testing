# [none] warm-cache result differs from a cold compile when `variant` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

'variant' changed without a recompile and the result matched; guards may be under-specified but nothing is observable yet - useful for steering further mutation

Signature `3e0d521abb47` · case `seed_2026_01109` · probe `warm`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _ns=base_ns, _setup=setup_code,
                    _args=arg_exprs) -> Tuple[Any, ...]:
        # a shallow copy shares the imports/classes but lets `setup` rebind the
        # input names, so every call allocates its own tensors
        ns = dict(_ns)
        exec(_setup, ns)
        return _apply_context(tuple(eval(a, ns) for a in _args), ctx)

# contexts:
#   base: {'variant': 'base'}
#   dtype_float16: {'variant': 'base', 'dtype': 'float16'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}
#   noncontig: {'variant': 'noncontig'}

```

## Observed difference

- (no findings recorded)

## Trigger condition

Changed factor:

```
variant
```

Controlled factors:

```
(none recorded)
```

## Cache / specialization evidence

| execution | result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | matches reference |
| compiled A then B (warm) | **differs** |
| recompile triggered by the switch | **no** |

## Suspected stage

```
none
```

Attribution basis: 'variant' changed without a recompile and the result matched; guards may be under-specified but nothing is observable yet - useful for steering further mutation

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

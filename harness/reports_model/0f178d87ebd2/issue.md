# [TorchInductor (lowering / codegen)] compiled execution differs from eager when `variant=base` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

Signature `0f178d87ebd2` · case `mod_nn_ConvTranspose2d_0__IntroduceAliasWrite+CrossViewWrite_12` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _b=base) -> Tuple[Any, ...]:
        fresh = tuple(
            (a.detach().clone().requires_grad_(a.requires_grad)
             if torch is not None and isinstance(a, torch.Tensor) else a)
            for a in _b
        )
        return _apply_context(fresh, ctx)

# contexts:
#   base: {'variant': 'base'}
#   noncontig: {'variant': 'noncontig'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}

```

## Expected vs actual

- **E0_eager vs E3_inductor**
  - `mutation` input[0] holds a different value after the call (max diff 1.629e+01) - in-place effects diverge

    ```
    shape                     : [2, 4, 3, 4]
    dtype                     : torch.float32
    mismatching elements      : 48
    total elements            : 96
    max absolute error        : 16.290245056152344
    max relative error        : 18.755740634869536
    first mismatch at index   : [0, 0, 0, 0]
    expected at that index    : -3.991455078
    actual at that index      : -0.06738090515
    expected                  : [-3.991455078, -0.3247413635, 5.756046295, 8.947198868, 3.571939468, 1.215835571, 6.034377098, -5.299221039, ... (88 more)]
    actual                    : [-0.06738090515, 4.827992439, -7.40740633, -6.623451233, -3.466389179, 2.413415909, -0.1783189774, 7.136005402, ... (88 more)]
    ```

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
variant: unchanged (base)
```

## Suspected stage

```
TorchInductor (lowering / codegen)
```

Attribution basis: all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

## Accuracy methodology

No float64 reference could be built for this case, so a plain tolerance was used. Treat the numeric claim with corresponding caution.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

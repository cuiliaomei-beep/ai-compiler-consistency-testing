# [TorchInductor (lowering / codegen)] compiled execution differs from eager when `variant=noncontig` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

Signature `737a79296226` · case `op_var_mean_1__IntroduceView_0` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _base=base_args) -> Tuple[Any, ...]:
        return _apply_context(tuple(_clone(a) for a in _base), ctx)

# contexts:
#   base: {'variant': 'base'}
#   noncontig: {'variant': 'noncontig'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}

```

## Expected vs actual

- **E0_eager vs E3_inductor**
  - `value` output[0]: compiled err vs fp64 = 5.456e-07 exceeds 4.0x eager err 6.876e-08

    ```
    shape                     : []
    dtype                     : torch.float32
    mismatching elements      : 1
    total elements            : 1
    max absolute error        : 4.76837158203125e-07
    max relative error        : 6.195940035940169e-08
    first mismatch at index   : []
    eager error vs float64    : 6.875792735172581e-08
    compiled error vs float64 : 5.455950855548508e-07
    accepted budget           : 2.7503170940690325e-07
    expected at that index    : 7.695961475
    actual at that index      : 7.695960999
    expected                  : [7.695961475]
    actual                    : [7.695960999]
    ```
  - `value` output[1]: compiled err vs fp64 = 3.815e-07 exceeds 4.0x eager err 9.537e-08

    ```
    shape                     : []
    dtype                     : torch.float32
    mismatching elements      : 1
    total elements            : 1
    max absolute error        : 4.76837158203125e-07
    max relative error        : 9.56810895703211e-08
    first mismatch at index   : []
    eager error vs float64    : 9.536743128535363e-08
    compiled error vs float64 : 3.8146972691777137e-07
    accepted budget           : 3.8146972514141453e-07
    expected at that index    : -4.9836092
    actual at that index      : -4.983609676
    expected                  : [-4.9836092]
    actual                    : [-4.983609676]
    ```

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
variant: unchanged (noncontig)
```

## Suspected stage

```
TorchInductor (lowering / codegen)
```

Attribution basis: all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

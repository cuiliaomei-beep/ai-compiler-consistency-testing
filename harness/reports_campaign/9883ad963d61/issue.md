# [AOTAutograd / functionalization] compiled execution differs from eager when `variant=base` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

eager and backend='eager' agree but aot_eager differs: implicates AOTAutograd / functionalization (alias and in-place handling) - alias/mutation oracle fired, the highest-value class

Signature `9883ad963d61` · case `op_unsafe_chunk_1__IntroduceAliasWrite_1` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _base=base_args) -> Tuple[Any, ...]:
        return _apply_context(tuple(_clone(a) for a in _base), ctx)

# contexts:
#   base: {'variant': 'base'}
#   noncontig: {'variant': 'noncontig'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}

```

## Expected vs actual

- **E0_eager vs E2_aot_eager**
  - `alias` storage-sharing relation changed: lost [(0, 3), (0, 4), (0, 5), (0, 6), (0, 7)] (indices < 3 are inputs, >= 3 are outputs)
- **E0_eager vs E3_inductor**
  - `alias` storage-sharing relation changed: lost [(0, 3), (0, 4), (0, 5), (0, 6), (0, 7)] (indices < 3 are inputs, >= 3 are outputs)

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
variant: unchanged (base)
```

## Suspected stage

```
AOTAutograd / functionalization
```

Attribution basis: eager and backend='eager' agree but aot_eager differs: implicates AOTAutograd / functionalization (alias and in-place handling) - alias/mutation oracle fired, the highest-value class

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

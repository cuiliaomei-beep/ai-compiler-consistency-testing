# [TorchInductor (lowering / codegen)] compiled execution differs from eager when `variant=base` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

Signature `d73d573b0114` · case `op__softmax_backward_data_0__IntroduceAliasWrite_2` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _base=base_args) -> Tuple[Any, ...]:
        return _apply_context(tuple(_clone(a) for a in _base), ctx)

# contexts:
#   base: {'variant': 'base'}
#   noncontig: {'variant': 'noncontig'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}

```

## Expected vs actual

- **E0_eager vs E3_inductor**
  - `value` output[0]: compiled err vs fp64 = 2.088e-07 exceeds 4.0x eager err 2.961e-08

    ```
    shape                     : [5]
    dtype                     : torch.float32
    mismatching elements      : 1
    total elements            : 5
    max absolute error        : 2.384185791015625e-07
    max relative error        : 9.459146796309949e-08
    first mismatch at index   : [1]
    eager error vs float64    : 2.961055400874102e-08
    compiled error vs float64 : 2.0880802509282148e-07
    accepted budget           : 1.1844221603496408e-07
    expected at that index    : -2.520508289
    actual at that index      : -2.520508528
    expected                  : [0.005748912692, -2.520508289, 0.1652744561, 2.348239422, 0.001245298074]
    actual                    : [0.005748912692, -2.520508528, 0.1652744561, 2.348239422, 0.001245298074]
    ```

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
variant: unchanged (base)
```

## Suspected stage

```
TorchInductor (lowering / codegen)
```

Attribution basis: all pre-Inductor layers agree and only Inductor differs: lowering / code generation against an already-accepted IR

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

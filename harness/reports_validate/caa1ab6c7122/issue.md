# [layer:E_poison] compiled execution differs from eager when `dtype=float16` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `caa1ab6c7122` · case `overlapping_views` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_overlapping(x):
    a = x[:6]
    b = x[4:]
    a.mul_(2.0)
    b.add_(1.0)
    return x, a, b

def _in_overlapping(ctx: Context) -> Tuple[Any, ...]:
    return (_randn(10, dtype=ctx.get("dtype", "float32")),)

# contexts:
#   fp32: {'dtype': 'float32'}
#   fp16: {'dtype': 'float16'}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[1]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
  - `value` output[2]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
  - `alias` storage-sharing relation changed: lost [(0, 2), (0, 3), (1, 2), (1, 3), (2, 3)] (indices < 1 are inputs, >= 1 are outputs)

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
dtype: unchanged (float16)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

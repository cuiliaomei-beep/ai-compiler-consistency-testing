# [layer:E_poison] compiled execution differs from eager when `requires_grad=True` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `d67746a878eb` · case `requires_grad_switch` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_grad(x):
    y = (x * 3.0).relu()
    return y.sum()

def _in_grad(ctx: Context) -> Tuple[Any, ...]:
    return (_randn(32, 32, requires_grad=bool(ctx.get("requires_grad", False))),)

# contexts:
#   nograd: {'requires_grad': False}
#   grad: {'requires_grad': True}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[0]: compiled err vs fp64 = 9.999e-01 exceeds 4.0x eager err 6.124e-05

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
requires_grad: unchanged (True)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

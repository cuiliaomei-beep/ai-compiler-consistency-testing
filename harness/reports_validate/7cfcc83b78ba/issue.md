# [layer:E_poison] compiled execution differs from eager when `dtype=float32` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `7cfcc83b78ba` · case `view_inplace` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_view_inplace(x):
    y = x.view(-1)
    y[0] = y[0] + 1.0
    return x

def _in_view(ctx: Context) -> Tuple[Any, ...]:
    return (_randn(8, 8, dtype=ctx.get("dtype", "float32")),)

# contexts:
#   fp32: {'dtype': 'float32'}
#   fp16: {'dtype': 'float16'}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
  - `alias` storage-sharing relation changed: lost [(0, 1)] (indices < 1 are inputs, >= 1 are outputs)

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
dtype: unchanged (float32)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

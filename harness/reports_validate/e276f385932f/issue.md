# [layer:E_poison] compiled execution differs from eager when `cols=32` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `e276f385932f` · case `noncontig_copy` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_noncontig(x, w):
    z = x[:, ::2]
    z.copy_(w)
    return x

def _in_noncontig(ctx: Context) -> Tuple[Any, ...]:
    n = ctx.get("cols", 16)
    dt = ctx.get("dtype", "float32")
    return (_randn(8, n, dtype=dt), _randn(8, n // 2, dtype=dt))

# contexts:
#   cols16: {'cols': 16}
#   cols32: {'cols': 32}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
  - `alias` storage-sharing relation changed: lost [(0, 2)] (indices < 2 are inputs, >= 2 are outputs)

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
cols: unchanged (32)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

# [layer:E_poison] compiled execution differs from eager when `rows=32` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `4794588951bc` · case `shape_boundary` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_shape_boundary(x):
    if x.shape[0] >= 32:
        return (x * 2.0).sum(dim=0)
    return (x + 1.0).sum(dim=0)

def _in_shape_boundary(ctx: Context) -> Tuple[Any, ...]:
    n = ctx.get("rows", 32)
    return (_randn(n, 16),)

# contexts:
#   rows31: {'rows': 31}
#   rows32: {'rows': 32}
#   rows33: {'rows': 33}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.639e-06

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
rows: unchanged (32)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

# [layer:E_poison] compiled execution differs from eager when `fast=True` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

first divergence appears at layer 'E_poison'

Signature `fac1e2e97da2` · case `python_flag` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

def _f_flag(x, fast: bool):
    if fast:
        return x.sum(dim=0) * 0.5
    return (x * 0.5).sum(dim=0)

def _in_flag(ctx: Context) -> Tuple[Any, ...]:
    return (_randn(32, 32), bool(ctx.get("fast", True)))

# contexts:
#   fast: {'fast': True}
#   slow: {'fast': False}

```

## Observed difference

- **E0_eager vs E_poison**
  - `value` output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 3.688e-07

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
fast: unchanged (True)
```

## Suspected stage

```
layer:E_poison
```

Attribution basis: first divergence appears at layer 'E_poison'

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

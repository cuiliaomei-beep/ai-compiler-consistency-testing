# [AOTAutograd / functionalization] compiled execution differs from eager when `variant=base; dtype=float64` changes

<!-- DRAFT generated by the consistency harness. Review every section before filing. -->

## Summary

eager and backend='eager' agree but aot_eager differs: implicates AOTAutograd / functionalization (alias and in-place handling)

Signature `27bfb9e9fc8e` · case `seed_2026_01161__IntroduceAliasWrite_2` · probe `layered`.

## Environment

- Python: 3.14.7
- PyTorch: 2.14.0+cpu (08187d9e0fba026dc8217405802ab5381dc88d90)
- CUDA: None
- Device: None
- Triton: None
- Platform: Windows-11-10.0.26200-SP0

## Minimal reproducer

```python
import torch

# source unavailable for 'f'
    def make_inputs(ctx: Context, _ns=base_ns, _setup=setup_code,
                    _args=arg_exprs) -> Tuple[Any, ...]:
        # a shallow copy shares the imports/classes but lets `setup` rebind the
        # input names, so every call allocates its own tensors
        ns = dict(_ns)
        exec(_setup, ns)
        return _apply_context(tuple(eval(a, ns) for a in _args), ctx)

# contexts:
#   base: {'variant': 'base'}
#   dtype_float16: {'variant': 'base', 'dtype': 'float16'}
#   dtype_float64: {'variant': 'base', 'dtype': 'float64'}
#   noncontig: {'variant': 'noncontig'}

```

## Expected vs actual

- **E0_eager vs E2_aot_eager**
  - `value` output[0]: non-float values differ exactly

    ```
    shape                     : [3]
    dtype                     : torch.int64
    mismatching elements      : 2
    total elements            : 3
    first mismatch at index   : [1]
    expected at that index    : 3
    actual at that index      : 5
    expected                  : [1, 3, 5]
    actual                    : [1, 5, 9]
    ```
- **E0_eager vs E3_inductor**
  - `value` output[0]: non-float values differ exactly

    ```
    shape                     : [3]
    dtype                     : torch.int64
    mismatching elements      : 2
    total elements            : 3
    first mismatch at index   : [1]
    expected at that index    : 3
    actual at that index      : 5
    expected                  : [1, 3, 5]
    actual                    : [1, 5, 9]
    ```

## Trigger condition

Changed factor:

```
(single context)
```

Controlled factors:

```
dtype: unchanged (float64)
variant: unchanged (base)
```

## Suspected stage

```
AOTAutograd / functionalization
```

Attribution basis: eager and backend='eager' agree but aot_eager differs: implicates AOTAutograd / functionalization (alias and in-place handling)

## Accuracy methodology

A float64 reference was computed; the compiled result was flagged only because its error against that reference exceeded the eager error by more than the configured multiplier.

## Reproducibility

```
Reproduced 1/1 runs with a fixed seed.
```

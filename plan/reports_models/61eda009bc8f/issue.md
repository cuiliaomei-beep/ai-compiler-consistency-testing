# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x):
    with torch.autocast(x.device.type, dtype=torch.bfloat16):
        return model(x)

args = (torch.randn(1, 3, 4, 64).requires_grad_(True),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.007720947266, 0.007720947266, 0.007720947266, 0.007720947266, -0.001121520996, -0.01879882812, -0.03662109375, -0.05419921875, ... (40952 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.007598876953, 0.007598876953, 0.007598876953, 0.007598876953, -0.001235961914, -0.01892089844, -0.03662109375, -0.05419921875, ... (40952 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [gradient] grad[0]: |ref-test| = 1.320e+00 exceeds dtype-aware tolerance 9.475e-03 (no fp64 reference)
      max absolute error        : 1.3203125
      first mismatching element : [0, 0, 0, 1]
      mismatching elements      : 23134
      total elements            : 24576
      expected at that index    : 1.65625
      actual at that index      : 1.6875
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 3 -> 3 lines, inputs 24576 -> 768 elements; 8 reductions kept of 16 tried.
- kept: halve dim 0
- kept: halve dim 2
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 2
- kept: halve dim 2
- kept: halve dim 2

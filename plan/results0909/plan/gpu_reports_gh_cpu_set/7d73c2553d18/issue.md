# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

f = lambda x: torch.repeat_interleave((x + 1).flatten(), 2).flip(0)

args = (torch.randn(2, 2),)

eager = <lambda>(*args)
torch._dynamo.reset()
compiled = torch.compile(<lambda>, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1.72050488, 1.72050488, 1.163970709, 1.163970709, 0.4030438066, 0.4030438066, 0.7212603092, 0.7212603092, ... (120 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.3216492534, 1, 1, 1, 1, 1, 1, 1, ... (120 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.195e+00 exceeds 4.0x eager err 1.192e-07
      max absolute error        : 3.1948819160461426
      first mismatching element : [0]
      mismatching elements      : 127
      total elements            : 128
      eager error vs float64    : 1.1920928955078125e-07
      compiled error vs float64 : 3.1948819160461426
      accepted budget           : 1.5182985180217656e-06
      expected at that index    : 1.72050488
      actual at that index      : 0.3216492534
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 1 -> 1 lines, inputs 64 -> 4 elements; 7 reductions kept of 12 tried.
- kept: halve dim 0
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 1

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/187027 — origin issue of this reproducer

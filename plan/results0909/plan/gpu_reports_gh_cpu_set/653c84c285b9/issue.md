# [pytorch compile] Compiled execution inconsistent with eager (mutation, alias)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling); alias/mutation oracle fired (highest-value class)

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x):
    y = torch.nn.functional.dropout(x, 0.5, False)   # training=False -> y IS x
    y += y                                            # in-place mutation of the input
    return y

args = (torch.randn(1),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [4, 4, 4, 4]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [4, 4, 4, 4]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E2_aot_eager:
  [mutation] input[0] holds a different value after the call (max diff 2.000e+00); in-place effects diverge
      max absolute error        : 2.0
      first mismatching element : [0, 0]
      mismatching elements      : 4
      total elements            : 4
      expected at that index    : 4
      actual at that index      : 2
  [alias] storage-sharing relation changed: lost [(0, 1)] (indices < 1 are inputs, >= 1 outputs)
      alias pairs lost          : [(0, 1)]
      alias pairs gained        : []
E3_inductor:
  [mutation] input[0] holds a different value after the call (max diff 2.000e+00); in-place effects diverge
      max absolute error        : 2.0
      first mismatching element : [0, 0]
      mismatching elements      : 4
      total elements            : 4
      expected at that index    : 4
      actual at that index      : 2
  [alias] storage-sharing relation changed: lost [(0, 1)] (indices < 1 are inputs, >= 1 outputs)
      alias pairs lost          : [(0, 1)]
      alias pairs gained        : []
```

## Suspected Stage

```text
AOTAutograd / functionalization
```

Attribution basis: first divergent layer = E2_aot_eager; eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling); alias/mutation oracle fired (highest-value class)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 4 -> 1 elements; 4 reductions kept of 6 tried.
- kept: drop rank
- kept: halve dim 0
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195422 — origin issue of this reproducer

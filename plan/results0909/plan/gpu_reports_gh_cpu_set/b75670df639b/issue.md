# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(logits, prob_targets, weight):
    return F.cross_entropy(
        logits,
        prob_targets,
        weight=weight,
        reduction="none",
    )

args = (torch.randn(4, 3), torch.randn(4, 3), torch.randn(3))

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [2.193204641, 2.932387114, 4.411627769, 1.771336079]
```

## Actual Behavior

Compiled result:

```text
exception TorchRuntimeError: Dynamo failed to run FX node with fake tensors: call_function <function cross_entropy at 0x79d053b732e0>(*(FakeTensor(..., device='cuda:0', size=(4, 3)), FakeTensor(..., device='cuda:0', size=(4, 3))), **{'weight': FakeTensor(..., device='cuda:0', size=(3,)), 'reduction': 'none'}): got RuntimeError('Cannot call numel() on tensor with symbolic sizes/strides\nException raised from throw_cannot_call_with_symbolic at /pytorch/c10/core/TensorImpl.cpp:289 (most recent call first):\nframe #0: c10::Error::Error(c10::SourceLocation, std::__cxx11::basic_string<char, std::char_traits<char>, std::allocator<char> >) + 0x9d (0x79d145372fdd in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #1: c10::TensorImpl::throw_cannot_call_with_symbolic(char const*) const + 0x8b (0x79d145306e6b in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #2: c10::TensorImpl::numel_custom() const + 0x9c (0x79d14534c4fc in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #3: at::native::cross_entropy_loss_symint(at::Tensor const&, at::Tensor const&, std::optional<at::Tensor> const&, long, c10::SymInt, double) + 0x2482 (0x79d09f6d74a2 in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #4: <unknown function> + 0x33517af (0x79d0a0aa27af in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #5: <unknown function> + 0x773b5f (0x79d0b332eb5f in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_python.so)
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: TorchRuntimeError
E2_aot_eager: TorchRuntimeError
E3_inductor: TorchRuntimeError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <function cross_entropy at 0x79d053b732e0>(*(FakeTensor(..., device='cuda:0', size=(4, 3)), FakeTensor(..., device='cuda:0', size=(4, 3)))
E2_aot_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <function cross_entropy at 0x79d053b732e0>(*(FakeTensor(..., device='cuda:0', size=(4, 3)), FakeTensor(..., device='cuda:0', size=(4, 3)))
E3_inductor:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <function cross_entropy at 0x79d053b732e0>(*(FakeTensor(..., device='cuda:0', size=(4, 3)), FakeTensor(..., device='cuda:0', size=(4, 3)))
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/182399 — origin issue of this reproducer

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def foo(q, k, v, a):
    qk = q @ k.transpose(-2, -1) / 1.0
    qk = qk + a
    attn = torch.softmax(qk, dim=-1)
    output = attn @ v
    return output

args = (torch.randn(1, 1, 64, 64), torch.randn(1, 1, 64, 64), torch.randn(1, 1, 64, 64), torch.randn(64, 64))

eager = foo(*args)
torch._dynamo.reset()
compiled = torch.compile(foo, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.138939261, 0.3853972256, -0.1629444659, -0.1058808044, -0.4223611057, -0.9167493582, -0.3751812577, 0.4285477102, ... (4088 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.4289180338, 0.3480421603, -0.07783012092, 0.2397281677, -0.1697421968, -0.3069644868, -0.1317574829, 0.4276483357, ... (4088 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.204e+00 exceeds 4.0x eager err 3.364e-06
      max absolute error        : 3.2043961733579636
      first mismatching element : [0, 0, 0, 0]
      mismatching elements      : 4096
      total elements            : 4096
      eager error vs float64    : 3.364182206436195e-06
      compiled error vs float64 : 3.204396449414423
      accepted budget           : 1.345672882574478e-05
      expected at that index    : -1.138939261
      actual at that index      : -0.4289180338
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 6 -> 6 lines, inputs 16384 -> 16384 elements; 3 reductions kept of 14 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/175632 — origin issue of this reproducer

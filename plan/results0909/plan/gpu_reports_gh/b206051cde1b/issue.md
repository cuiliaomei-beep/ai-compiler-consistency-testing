# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

lambda x: torch.nn.functional.fractional_max_pool2d(
    x, 3, output_size=(4, 4), return_indices=True),

args = (torch.randn(1, 8, 8),)

eager = <lambda>(*args)
torch._dynamo.reset()
compiled = torch.compile(<lambda>, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: P[T,T]
[0] [1.969313622, 2.266990185, 1.074355721, 0.3632776141, 1.521076441, 0.8933336139, 1.311082363, 1.638702989, ... (40 more)]
[1] [33, 21, 10, 29, 82, 103, 106, 78, ... (40 more)]
```

## Actual Behavior

Compiled result:

```text
structure: P[T,T]
[0] [1.969313622, 2.266990185, 3.217626572, 0.3632776141, 1.521076441, 1.466218233, 3.011888504, 1.638702989, ... (40 more)]
[1] [33, 21, 24, 29, 82, 68, 72, 78, ... (40 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[1]: non-float values differ exactly
      first mismatching element : [0, 0, 0, 2]
      mismatching elements      : 18
      total elements            : 48
      expected at that index    : 10
      actual at that index      : 24
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 2 -> 2 lines, inputs 768 -> 64 elements; 7 reductions kept of 10 tried.
- kept: drop rank
- kept: halve dim 0
- kept: halve dim 1
- kept: halve dim 2
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/183249 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/183249 — `torch.compile` produces wrong results for `fractional_max_pool2d` when `_random_samples` is not provided

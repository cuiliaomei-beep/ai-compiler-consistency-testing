# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, dx):
    return torch.func.jvp(SinWithZeroJvp.apply, (x,), (dx,))[1]

args = (torch.randn(4), torch.randn(4))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.8055554628, -1.025139689, 0.1939273924, -0.2949834466]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [value] output[0]: compiled err vs fp64 = 1.025e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0251396894454956
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0251396894454956
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : -0.8055554628
E2_aot_eager:
  [value] output[0]: compiled err vs fp64 = 1.025e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0251396894454956
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0251396894454956
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : -0.8055554628
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.025e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0251396894454956
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0251396894454956
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : -0.8055554628
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/192985 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/192985 — Dynamo silently drops custom `jvp` of `autograd.Function` under `torch.func.jvp`, producing wrong tangents
- https://github.com/pytorch/pytorch/pull/193277 — Fix Dynamo silently dropping custom jvp of autograd.Function under forward AD

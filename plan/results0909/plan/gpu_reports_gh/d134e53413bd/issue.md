# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': False}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        x = self.conv1(x)
        x = self.gn1(x)
        x = torch.polygamma(0, x + 0.1)
        x = x.unsqueeze(2)
        x = torch.cumulative_trapezoid(x)
        return x

args = (torch.randn(1, 3, 32, 32),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-300.6773987, -326.3009644, -333.1569519, -334.0076904, -333.6399841, -323.1172791, -313.5464783, -314.960022, ... (15864 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-300.6785278, -326.302002, -333.1579895, -334.008728, -333.6410217, -323.1182861, -313.5474854, -314.9610291, ... (15864 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': False} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: |ref-test| = 4.565e+01 exceeds dtype-aware tolerance 3.200e+01 (no fp64 reference)
      max absolute error        : 45.654296875
      first mismatching element : [0, 14, 0, 6, 10]
      mismatching elements      : 21
      total elements            : 15872
      expected at that index    : 32000.99609
      actual at that index      : 31955.3418
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 7 -> 7 lines, inputs 3072 -> 3072 elements; 3 reductions kept of 9 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/185587 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/185587 — [inductor] Numerical inconsistency: polygamma + cumulative_trapezoid under torch.compile(dynamic=True) produces wrong results after Conv2d + GroupNorm
- https://github.com/pytorch/pytorch/issues/185589 — [inductor] Severe numerical inconsistency: diag_embed + erf + normalize(p=-1) under torch.compile(dynamic=True) — max diff up to 565,248

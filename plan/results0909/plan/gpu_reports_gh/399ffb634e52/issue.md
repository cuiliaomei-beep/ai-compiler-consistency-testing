# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x):
    y = torch.quantile(x, 0.0, dim=1, keepdim=True)
    return torch.nan_to_num(y, nan=0.0).sum()

args = (torch.randn(2, 3).to(torch.float64).requires_grad_(True),)

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [gradient] grad[0]: compiled err vs fp64 = 1.000e+00 exceeds 8.0x eager err 0.000e+00
      max absolute error        : 1.0
      first mismatching element : [1, 0]
      mismatching elements      : 2
      total elements            : 6
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0
      accepted budget           : 1e-08
      expected at that index    : 0
      actual at that index      : 1
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 3 -> 3 lines, inputs 6 -> 6 elements; 2 reductions kept of 9 tried.
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/185543 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/185543 — [inductor] Gradient mismatch between eager and inductor for torch.quantile on tied values
- https://github.com/pytorch/pytorch/issues/77764 — General MPS op coverage tracking issue

# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, query, key, value):
        batch_size = query.size(0)

        q = self.query_proj(query).view(batch_size, -1, self.num_heads, self.head_dim)
        k = self.key_proj(key).view(batch_size, -1, self.num_heads, self.head_dim)
        v = self.value_proj(value).view(batch_size, -1, self.num_heads, self.head_dim)

        q = q.permute(0, 2, 1, 3)
        k = k.permute(0, 2, 1, 3)
        v = v.permute(0, 2, 1, 3)

        # Compute attention scores
        scores = torch.matmul(q, k.transpose(-2, -1)) * self.scale_factor

        # Explicit dtype conversion: scores -> float32 for numerical stability
        scores = scores.float()
        attn_weights = self.softmax(scores)

        # Explicit dtype conversion: attn_weights -> float16
        attn_weights = attn_weights.half()

        # This matmul fails in eager mode: attn_weights is float16, v is float32
        output = torch.matmul(attn_weights, v)

        output = output.permute(0, 2, 1, 3).contiguous()
        return output.view(batch_size, -1, self.num_heads * self.head_dim)

args = (torch.randn(2, 64), torch.randn(2, 64), torch.randn(2, 64))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: expected scalar type Half but found Float
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.06521674991, -0.03678695485, -0.01126699243, 0.05965234712, -0.09113837779, 0.03567616269, -0.1975563318, 0.2635120749, ... (4088 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : expected scalar type Half but found Float
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 26 -> 26 lines, inputs 12288 -> 384 elements; 8 reductions kept of 16 tried.
- kept: drop rank
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 0
- kept: halve dim 0

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/171190 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/171190 — torch.compile (Inductor) silently ignores explicit dtype casts in SDPA, diverging from eager semantics

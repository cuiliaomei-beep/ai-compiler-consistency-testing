# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    mean = torch.zeros((4,), dtype=torch.float32)
    std = torch.full((4,), -1.0, dtype=torch.float32)
    return torch.normal(mean, std)

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: normal expects all elements of std >= 0.0
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-1.540996075, 0.2934288979, 2.178789377, -0.5684312582]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : normal expects all elements of std >= 0.0
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 0 -> 0 elements; 0 reductions kept of 2 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/185248 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/185248 — `torch.compile` silently accepts negative standard deviations in `torch.normal` that eager mode rejects

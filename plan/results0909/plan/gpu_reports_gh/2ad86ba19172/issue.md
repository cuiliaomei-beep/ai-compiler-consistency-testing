# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        b, t, _ = x.shape
        q = self.q(x).view(b, t, H, D).permute(0, 2, 1, 3)   # (B, H, T, D)
        k = self.k(x).view(b, t, H, D).permute(0, 2, 3, 1)   # (B, H, D, T) pre-transposed
        v = self.v(x).view(b, t, H, D).permute(0, 2, 1, 3)   # (B, H, T, D)
        w = (torch.matmul(q, k) * (D ** -0.5)).softmax(dim=-1)
        return (w @ v).permute(0, 2, 1, 3).reshape(b, t, E)

args = (torch.randn(1, 8, 64),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.1349242628, -0.1254714578, -0.345441699, -0.3306719363, 0.1881641895, 0.185205251, -0.5236144066, -0.2433385998, ... (504 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.1324211955, -0.0870719254, -0.2665695548, -0.3394888341, 0.1551901251, 0.1267634332, -0.5499697924, -0.2279915214, ... (504 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: |ref-test| = 4.378e-01 exceeds dtype-aware tolerance 8.014e-04 (no fp64 reference)
      max absolute error        : 0.43783456832170486
      first mismatching element : [0, 0, 0]
      mismatching elements      : 506
      total elements            : 512
      expected at that index    : 0.1349242628
      actual at that index      : 0.1324211955
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 7 -> 7 lines, inputs 512 -> 512 elements; 3 reductions kept of 9 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195320 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/195320 — [inductor] SDPA fusion matches a permuted K as if it were key.transpose(-2,-1), silently producing wrong attention outputs
- https://github.com/pytorch/pytorch/pull/195383 — [inductor] Validate pattern-matcher matches by re-tracing with matched inputs

# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def h(x, y):
    pred = torch.tensor(True)

    def true_fn():
        return torch.ones(x.shape[0], x.shape[1])

    def false_fn():
        return torch.zeros(x.shape[0], y.shape[1])

    return torch.cond(pred, true_fn, false_fn)

args = (torch.randn(5, 7), torch.randn(5, 8))

eager = h(*args)
torch._dynamo.reset()
compiled = torch.compile(h, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception InternalTorchDynamoError: ModuleNotFoundError: No module named '_repro_issue_189528_cond_inductor_nameerror_on_inner_dim_bra'
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1, 1, 1, 1, 1, 1, 1, 1, ... (27 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: InternalTorchDynamoError
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [exception] ref raised InternalTorchDynamoError, test returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_189528_cond_inductor_nameerror_on_inner_dim_bra'
E2_aot_eager:
  [exception] ref raised InternalTorchDynamoError, test returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_189528_cond_inductor_nameerror_on_inner_dim_bra'
E3_inductor:
  [exception] ref raised InternalTorchDynamoError, test returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_189528_cond_inductor_nameerror_on_inner_dim_bra'
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/189528 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/189528 — [cond] Inductor NameError on inner-dim branch output mismatch
- https://github.com/pytorch/pytorch/pull/189529 — [Inductor] Fix undefined unbacked symbol in cond branch strides
- https://github.com/pytorch/pytorch/pull/191327 — [AI Codemod][PerfAICT-General] Add equal-batch 3D fast path to _matmul_impl to skip broadcast bookkeeping (#191327)
- https://github.com/pytorch/pytorch/pull/192388 — [assoc_scan] Base version of vmap
- https://github.com/pytorch/pytorch/pull/186373 — Disable duck sizing by default

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, index, src, out):
    return torch.scatter(x, 1, index, src, out=out)

args = (torch.randn(1, 2), torch.randint(0, 4, (1, 2,)), torch.randn(1, 2), torch.randn(1, 2))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1, 1]
```

## Actual Behavior

Compiled result:

```text
exception TorchRuntimeError: Dynamo failed to run FX node with fake tensors: call_function <built-in method scatter of type object at 0x7e86a52e4b40>(*(FakeTensor(..., device='cuda:0', size=(1, s27)), 1, FakeTensor(..., device='cuda:0', size=(1, s27), dtype=torch.int64), FakeTensor(..., device='cuda:0', size=(1, s27))), **{'out': FakeTensor(..., device='cuda:0', size=(1, s27))}): got RuntimeError('Cannot call numel() on tensor with symbolic sizes/strides\nException raised from throw_cannot_call_with_symbolic at /pytorch/c10/core/TensorImpl.cpp:289 (most recent call first):\nframe #0: c10::Error::Error(c10::SourceLocation, std::__cxx11::basic_string<char, std::char_traits<char>, std::allocator<char> >) + 0x9d (0x7e8736572fdd in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #1: c10::TensorImpl::throw_cannot_call_with_symbolic(char const*) const + 0x8b (0x7e8736506e6b in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #2: c10::TensorImpl::numel_custom() const + 0x9c (0x7e873654c4fc in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #3: <unknown function> + 0x214cf95 (0x7e8690a9df95 in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #4: at::meta::structured_scatter_src::meta(at::Tensor const&, long, at::Tensor const&, at::Tensor const&) + 0xd0 (0x7e8690ab3d30 in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #5: <unknown function> + 0x3847606 (0x7e8692198606 in /usr/local/lib/python3.12/dist-pac
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: TorchRuntimeError
E2_aot_eager: TorchRuntimeError
E3_inductor: TorchRuntimeError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method scatter of type object at 0x7e86a52e4b40>(*(FakeTensor(..., device='cuda:0', size=(1, s27)), 1, FakeTensor(..., device='c
E2_aot_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method scatter of type object at 0x7e86a52e4b40>(*(FakeTensor(..., device='cuda:0', size=(1, s27)), 1, FakeTensor(..., device='c
E3_inductor:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method scatter of type object at 0x7e86a52e4b40>(*(FakeTensor(..., device='cuda:0', size=(1, s27)), 1, FakeTensor(..., device='c
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194103 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/194103 — torch.compile(dynamic=True) fails for scatter out variants with symbolic shapes
- https://github.com/pytorch/pytorch/pull/186485 — Fix export dynamic dims with size-one examples
- https://github.com/pytorch/pytorch/pull/186150 — Fix compile storage semantics for as_strided scatter
- https://github.com/pytorch/pytorch/pull/183652 — Fix dynamic slice lowering for generic views
- https://github.com/pytorch/pytorch/pull/191166 — [ROCm][inductor] gfx1250 TDM support for dense mm/addmm

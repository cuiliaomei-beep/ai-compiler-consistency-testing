# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def rollout(state0, xs):
    def body(s, x):
        s_new = torch.tanh(s @ W + x)
        return s_new, s_new.clone()            # documented pattern: emitted slice is a clone of the carry
    _, ys = scan(body, state0, xs)
    return ys

args = (torch.randn(4, 8), torch.randn(16, 4, 8))

eager = rollout(*args)
torch._dynamo.reset()
compiled = torch.compile(rollout, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception InternalTorchDynamoError: ModuleNotFoundError: No module named '_repro_issue_195977_scan_inductor_refuses_a_tanh_sigmoid_rec'
```

## Actual Behavior

Compiled result:

```text
exception TorchRuntimeError: Dynamo failed to run FX node with fake tensors: call_function <built-in function matmul>(*(FakeTensor(..., device='cuda:0', size=(4, 8)), Parameter(FakeTensor(..., size=(8, 8), requires_grad=True))), **{}): got RuntimeError('Unhandled FakeTensor Device Propagation for aten.mm.default, found two different devices cuda:0, cpu')

from user code:
   File "reproducers_gh_cuda/issue_195977_scan_inductor_refuses_a_tanh_sigmoid_rec.py", line 18, in rollout
    _, ys = scan(body, state0, xs)
  File "/usr/local/lib/python3.12/dist-packages/torch/_higher_order_ops/scan.py", line 211, in scan
    carry, out = _maybe_compile_and_run_fn(
  File "/usr/local/lib/python3.12/dist-packages/torch/_higher_order_ops/utils.py", line 102, in _maybe_compile_and_run_fn
    return fn(*args)
  File "/usr/local/lib/python3.12/dist-packages/torch/_higher_order_ops/scan.py", line 209, in run_flattened_scan
    return scan_op(combine_fn, leaves_init, leaves_xs, additional_inputs=())
  File "/usr/local/lib/python3.12/dist-packages/torch/_higher_order_ops/scan.py", line 55, in wrap_combine_fn_flat
    return combine_fn(carry, xs)
  File "reproducers_gh_cuda/issue_195977_scan_inductor_refuses_a_tanh_sigmoid_rec.py", line 16, in body
    s_new = torch.tanh(s @ W + x)

Set TORCHDYNAMO_VERBOSE=1 for the internal stack trace (please do this especially if you're reporting a bug to PyTorch). For even more developer context, set TORCH_LOGS="+dynamo"
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: InternalTorchDynamoError
E1_dynamo_eager: TorchRuntimeError
E2_aot_eager: TorchRuntimeError
E3_inductor: TorchRuntimeError
```

## Difference

```text
E1_dynamo_eager:
  [exception] exception type differs: InternalTorchDynamoError vs TorchRuntimeError
E2_aot_eager:
  [exception] exception type differs: InternalTorchDynamoError vs TorchRuntimeError
E3_inductor:
  [exception] exception type differs: InternalTorchDynamoError vs TorchRuntimeError
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195977 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/195977 — [scan] inductor refuses a tanh/sigmoid recurrence under autograd ("scan might be aliasing the input or the output!"): ScanAutogradOp returns the carry twice as a saved intermediate

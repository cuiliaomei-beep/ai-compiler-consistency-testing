# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)



args = (torch.randn(1), torch.randn(1))

eager = remainder(*args)
torch._dynamo.reset()
compiled = torch.compile(remainder, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [2.641808081e-21]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [166.8701172]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.669e+02 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 166.8701171875
      first mismatching element : [0]
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.0
      compiled error vs float64 : 166.8701171875
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 2.641808081e-21
      actual at that index      : 166.8701172
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 0 -> 0 lines, inputs 2 -> 2 elements; 3 reductions kept of 3 tried.
- source unavailable; program not minimized
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194345 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/194345 — [inductor][cuda] `torch.remainder` / `torch.fmod` wrong results under `torch.compile` on CUDA for large |a/b| ratios
- https://github.com/pytorch/pytorch/pull/186942 — [inductor] Round fmod/remainder scalars for bf16/fp16 CPU

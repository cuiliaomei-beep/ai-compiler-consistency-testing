# P1-3 成对计时：冻结 185 程序，原程序臂 vs 顶层逐语句断点臂（backend=eager，torch 2.14.0+cpu，2026-09-25 17:10）

同一程序、同一输入、同一 oracle；每臂 3 次全新运行（每次 `torch._dynamo.reset()` 后编译 + 2 次调用），墙钟按程序累加。机器同时有一个子代理的 Python 进程在跑（CPU 负载约 23%），三次运行的离散度见下表。

| 臂 | 有效测试（调用） | 编译帧数（`frames.total`，两次调用合计） | 三次总墙钟 (s) | 中位数 (s) | 最小–最大 (s) |
|---|---|---|---|---|---|
| 原程序 | 370 | 614 | 13.59, 13.23, 13.09 | 13.23 | 13.09–13.59 |
| 断点变体 | 370 | 1175 | 32.2, 32.65, 32.97 | 32.65 | 32.2–32.97 |

断点臂的编译帧数是原程序臂的 1.91 倍，墙钟是 2.47 倍（含编译）。eager 参照臂两次调用合计 0.06 s。

首次检出有效根因的时间（第 1 次运行，两臂墙钟按语料顺序累加）：
- `gen_send`：0.6 s
- `collections_types`：5.2 s
- `gen_return_value_stopiteration`：32.2 s
- `gen_throw`：32.3 s

本次运行的成对分类：same 344, new 8, gone 2, same-divergence 12, changed 4（原程序臂 18，断点臂 24）。与 P0-1 审计运行相比的差异：`deepcopy_module_state`#0 same-divergence→same。

结论：在相同预算（同一组程序与调用）下断点臂多花约 2.5 倍编译墙钟，换来 2 个原程序臂找不到的根因；论文若保留“within minutes”的说法，可引用本表（两臂合计中位数 45.9 s）。不主张等时间优势。
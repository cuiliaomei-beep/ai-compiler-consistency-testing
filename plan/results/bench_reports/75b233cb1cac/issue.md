# [pytorch compile] Incorrect result when `layout` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `layout` changes from `(base)` to `noncontig`.
changing 'layout' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold
This test was generated because the static analysis found: stride read in the source (factor `x.stride`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def stride_read(x):
    if x.stride(0) == 1:
        return x.t().contiguous().sum(dim=0)
    return x.sum(dim=1)

args = (torch.randn(8, 8),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'layout': 'noncontig'}
args_b = (torch.randn(8, 8)  # NOTE: non-contiguous in the failing run,)
torch._dynamo.reset()
compiled = torch.compile(stride_read, backend='eager')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(stride_read, backend='eager')(*args_b)
eager = stride_read(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-3.853170156, 1.944055676, -1.13303566, 3.25309515, 0.8078030944, 0.2224572897, 0.958989799, 3.732162952]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-8.19893074, -0.9277412891, 2.799336433, 5.888707161, -1.979459643, 0.6966584921, 0.2260272503, 6.975939751]
```

## Trigger Condition

Changed semantic factor:

```text
layout: (base) -> noncontig
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 4.346e+00 exceeds 4.0x eager err 1.788e-07
      max absolute error        : 4.3457605838775635
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.7881393432617188e-07
      compiled error vs float64 : 4.345760703086853
      accepted budget           : 1.8373347074884805e-06
      expected at that index    : -3.853170156
      actual at that index      : -8.19893074
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 4.346e+00 exceeds 4.0x eager err 1.788e-07
      max absolute error        : 4.3457605838775635
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.7881393432617188e-07
      compiled error vs float64 : 4.345760703086853
      accepted budget           : 1.8373347074884805e-06
      expected at that index    : -3.853170156
      actual at that index      : -8.19893074
```

## Suspected Stage

```text
cache specialization (guard / stale artifact reuse)
```

Attribution basis: first divergent layer = n/a; changing 'layout' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

# [pytorch compile] Incorrect result when `substitute` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `substitute` changes from `(base)` to `{'arg': 1, 'value': 4}`.
changing 'substitute' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold
This test was generated because the static analysis found: parameter compared against a constant / used as index or bound (factor `n`, oracles ['value', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def scalar_compare(x, n):
    if n > 4:
        return x[:n].sum(dim=0)
    return x.sum(dim=0) * n

args = (torch.randn(16, 8), 6)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'substitute': {'arg': 1, 'value': 4}}
args_b = (torch.randn(16, 8), 4)
torch._dynamo.reset()
compiled = torch.compile(scalar_compare, backend='eager')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(scalar_compare, backend='eager')(*args_b)
eager = scalar_compare(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-18.91156578, -12.31404114, 10.91626835, 24.07842064, -6.641626358, 16.10950661, 15.81920433, -2.206307411]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-2.121921778, -3.873087168, -0.2896861434, 1.152163744, 3.919313669, 4.375522614, 0.5776008964, -2.498700619]
```

## Trigger Condition

Changed semantic factor:

```text
substitute: (base) -> {'arg': 1, 'value': 4}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 2.293e+01 exceeds 4.0x eager err 1.639e-06
      max absolute error        : 22.926256895065308
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.6391277313232422e-06
      compiled error vs float64 : 22.926255920203403
      accepted budget           : 1.1481485671538394e-05
      expected at that index    : -18.91156578
      actual at that index      : -2.121921778
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 2.293e+01 exceeds 4.0x eager err 1.639e-06
      max absolute error        : 22.926256895065308
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.6391277313232422e-06
      compiled error vs float64 : 22.926255920203403
      accepted budget           : 1.1481485671538394e-05
      expected at that index    : -18.91156578
      actual at that index      : -2.121921778
```

## Suspected Stage

```text
cache specialization (guard / stale artifact reuse)
```

Attribution basis: first divergent layer = n/a; changing 'substitute' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

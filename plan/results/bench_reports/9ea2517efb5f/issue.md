# [pytorch compile] Incorrect result when `substitute` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `substitute` changes from `{'arg': 1, 'value': 4}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: parameter compared against a constant / used as index or bound (factor `n`, oracles ['value', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def scalar_compare(x, n):
    if n > 4:
        return x[:n].sum(dim=0)
    return x.sum(dim=0) * n

args = (torch.randn(16, 8), 4)

eager = scalar_compare(*args)
torch._dynamo.reset()
compiled = torch.compile(scalar_compare, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-18.91156578, -12.31404114, 10.91626835, 24.07842064, -6.641626358, 16.10950661, 15.81920433, -2.206307411]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-17.91156578, -11.31404114, 11.91626835, 25.07842064, -5.641626358, 17.10950661, 16.81920433, -1.206307411]
```

## Trigger Condition

Changed semantic factor:

```text
substitute: {'arg': 1, 'value': 4} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.639e-06
      max absolute error        : 1.0
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.6391277313232422e-06
      compiled error vs float64 : 1.000000974861905
      accepted budget           : 1.1481485671538394e-05
      expected at that index    : -18.91156578
      actual at that index      : -17.91156578
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

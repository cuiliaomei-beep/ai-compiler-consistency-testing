# [pytorch compile] Incorrect result when `resize` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `resize` changes from `{'arg': 0, 'dim': 0, 'value': 1}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: branch/index predicate: index relation x.shape[0] -> i -> x[i] (factor `x.shape[0]`, oracles ['value', 'metadata', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def index_chain(x):
    n = x.shape[0]
    i = n - 1
    y = x[i]
    return y * 2.0 + x[0]

args = (torch.randn(1, 4),)

eager = index_chain(*args)
torch._dynamo.reset()
compiled = torch.compile(index_chain, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-3.377519608, -3.457080603, -0.7517357469, -1.301636457]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-2.377519608, -2.457080603, 0.2482642531, -0.3016364574]
```

## Trigger Condition

Changed semantic factor:

```text
resize: {'arg': 0, 'dim': 0, 'value': 1} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.192e-07
      max absolute error        : 1.0
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 1.1920928955078125e-07
      compiled error vs float64 : 1.0
      accepted budget           : 1.6484644902448053e-06
      expected at that index    : -3.377519608
      actual at that index      : -2.377519608
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

# [pytorch compile] Incorrect result when `resize` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `resize` changes from `{'arg': 0, 'dim': 0, 'value': 3}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: branch/index predicate: x.shape[0] Lt 4 (line 2) (factor `x.shape[0]`, oracles ['value', 'metadata', 'exception', 'mutation', 'alias']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def exception_path(x):
    if x.shape[0] < 4:
        raise ValueError("need at least 4 rows")
    return x.sum(dim=0)

args = (torch.randn(3, 4),)

eager = exception_path(*args)
torch._dynamo.reset()
compiled = torch.compile(exception_path, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception ValueError: need at least 4 rows
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 0, 0, 0, 0, 0, 0, 0, ... (4 more)]
```

## Trigger Condition

Changed semantic factor:

```text
resize: {'arg': 0, 'dim': 0, 'value': 3} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ValueError
E1_dynamo_eager: ValueError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised ValueError, test returned normally
      exception                 : ValueError
      message                   : need at least 4 rows
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

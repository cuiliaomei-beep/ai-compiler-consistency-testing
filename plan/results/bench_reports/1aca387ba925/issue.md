# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def python_flag_semantic(x, use_relu):
    if use_relu:
        return x.relu().sum(dim=-1)
    return x.tanh().sum(dim=-1)

args = (torch.randn(16, 8), True)

eager = python_flag_semantic(*args)
torch._dynamo.reset()
compiled = torch.compile(python_flag_semantic, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1.540719509, 3.45466876, 3.812004566, 4.855666161, 1.914063096, 3.670017242, 2.64932704, 7.495227814, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [2.540719509, 4.454668999, 4.812004566, 5.855666161, 2.914062977, 4.670017242, 3.64932704, 8.495227814, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 3.576e-07
      max absolute error        : 1.000000238418579
      first mismatching element : [0]
      mismatching elements      : 16
      total elements            : 16
      eager error vs float64    : 3.5762786865234375e-07
      compiled error vs float64 : 1.0000003650784492
      accepted budget           : 3.5740031307796016e-06
      expected at that index    : 1.540719509
      actual at that index      : 2.540719509
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

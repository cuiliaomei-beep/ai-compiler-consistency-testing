# [pytorch compile] Incorrect result when `resize` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `resize` changes from `{'arg': 0, 'dim': 0, 'value': 33}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: branch/index predicate: x.shape[0] GtE 32 (line 2) (factor `x.shape[0]`, oracles ['value', 'metadata', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def shape_boundary(x):
    if x.shape[0] >= 32:
        return (x * 2.0).sum(dim=0)
    return (x + 1.0).sum(dim=0)

args = (torch.randn(33, 16),)

eager = shape_boundary(*args)
torch._dynamo.reset()
compiled = torch.compile(shape_boundary, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-7.280947685, 3.120229959, 3.228347778, 12.48587608, 23.0888195, -0.3564164639, 11.15104198, -6.383094788, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-6.280947685, 4.120229721, 4.228347778, 13.48587608, 24.0888195, 0.6435835361, 12.15104198, -5.383094788, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
resize: {'arg': 0, 'dim': 0, 'value': 33} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.594e-06
      max absolute error        : 1.0
      first mismatching element : [0]
      mismatching elements      : 16
      total elements            : 16
      eager error vs float64    : 1.5944242477416992e-06
      compiled error vs float64 : 1.0000010430812836
      accepted budget           : 1.939197136380244e-05
      expected at that index    : -7.280947685
      actual at that index      : -6.280947685
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def shape_boundary(x):
    if x.shape[0] >= 32:
        return (x * 2.0).sum(dim=0)
    return (x + 1.0).sum(dim=0)

args = (torch.randn(32, 16),)

eager = shape_boundary(*args)
torch._dynamo.reset()
compiled = torch.compile(shape_boundary, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-5.029268265, 5.4249506, 3.729505539, 13.35363388, 21.39139938, -1.740434885, 11.78306675, -2.152656317, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-4.029268265, 6.4249506, 4.729505539, 14.35363388, 22.39139938, -0.740434885, 12.78306675, -1.152656317, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.639e-06
      max absolute error        : 1.0
      first mismatching element : [0]
      mismatching elements      : 16
      total elements            : 16
      eager error vs float64    : 1.6391277313232422e-06
      compiled error vs float64 : 1.0000016391277313
      accepted budget           : 1.8326929421164095e-05
      expected at that index    : -5.029268265
      actual at that index      : -4.029268265
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

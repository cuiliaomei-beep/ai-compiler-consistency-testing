# [pytorch compile] Incorrect result when `layout` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `layout` changes from `(base)` to `noncontig`.
changing 'layout' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold
This test was generated because the static analysis found: layout read, or alias structure is written through (factor `x.is_contiguous()`, oracles ['value', 'metadata', 'alias', 'mutation']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def noncontig_copy(x, w):
    z = x[:, ::2]
    z.copy_(w)
    return x

args = (torch.randn(8, 16), torch.randn(8, 8))

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'layout': 'noncontig'}
args_b = (torch.randn(8, 16)  # NOTE: non-contiguous in the failing run, torch.randn(8, 8)  # NOTE: non-contiguous in the failing run)
torch._dynamo.reset()
compiled = torch.compile(noncontig_copy, backend='eager')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(noncontig_copy, backend='eager')(*args_b)
eager = noncontig_copy(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.8833757639, -1.152360201, -0.4189134836, -0.4338788092, -0.804826498, 0.692009151, 0.5656096339, -2.115219355, ... (120 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.8833757639, 2.111756786e-42, 2.111756786e-42, 0, -0.4189134836, 0, 0, 0, ... (120 more)]
```

## Trigger Condition

Changed semantic factor:

```text
layout: (base) -> noncontig
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 3.411e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 3.4105026721954346
      first mismatching element : [0, 1]
      mismatching elements      : 127
      total elements            : 128
      eager error vs float64    : 0.0
      compiled error vs float64 : 3.4105026721954346
      accepted budget           : 1.626254402253835e-06
      expected at that index    : -1.152360201
      actual at that index      : 2.111756786e-42
  [mutation] input[0] holds a different value after the call (max diff 3.411e+00); in-place effects diverge
      max absolute error        : 3.4105026721954346
      first mismatching element : [0, 1]
      mismatching elements      : 64
      total elements            : 128
      expected at that index    : -1.152360201
      actual at that index      : 2.111756786e-42
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 3.411e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 3.4105026721954346
      first mismatching element : [0, 1]
      mismatching elements      : 127
      total elements            : 128
      eager error vs float64    : 0.0
      compiled error vs float64 : 3.4105026721954346
      accepted budget           : 1.626254402253835e-06
      expected at that index    : -1.152360201
      actual at that index      : 2.111756786e-42
  [mutation] input[0] holds a different value after the call (max diff 3.411e+00); in-place effects diverge
      max absolute error        : 3.4105026721954346
      first mismatching element : [0, 1]
      mismatching elements      : 64
      total elements            : 128
      expected at that index    : -1.152360201
      actual at that index      : 2.111756786e-42
```

## Suspected Stage

```text
cache specialization (guard / stale artifact reuse)
```

Attribution basis: first divergent layer = n/a; changing 'layout' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

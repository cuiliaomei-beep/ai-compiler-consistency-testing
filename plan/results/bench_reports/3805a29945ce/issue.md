# [pytorch compile] Incorrect result when `substitute` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `substitute` changes from `(base)` to `{'arg': 1, 'value': 0}`.
changing 'substitute' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold
This test was generated because the static analysis found: parameter compared against a constant / used as index or bound (factor `n`, oracles ['value', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def loop_bound(x, n):
    for _ in range(n):
        x = x * 1.5 + 1.0
    return x

args = (torch.randn(8, 4), 3)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'substitute': {'arg': 1, 'value': 0}}
args_b = (torch.randn(8, 4), 0)
torch._dynamo.reset()
compiled = torch.compile(loop_bound, backend='eager')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(loop_bound, backend='eager')(*args_b)
eager = loop_bound(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.125839829, -1.152360201, -0.2505785823, -0.4338788092, 0.8487103581, 0.692009151, -0.3160127699, -2.115219355, ... (24 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.9502904415, 0.8607844114, 3.904297352, 3.285659075, 7.614397526, 7.085531235, 3.683456898, -2.388865471, ... (24 more)]
```

## Trigger Condition

Changed semantic factor:

```text
substitute: (base) -> {'arg': 1, 'value': 0}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 9.151e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 9.15088963508606
      first mismatching element : [0, 0]
      mismatching elements      : 32
      total elements            : 32
      eager error vs float64    : 0.0
      compiled error vs float64 : 9.15088963508606
      accepted budget           : 1.0086151860377868e-06
      expected at that index    : -1.125839829
      actual at that index      : 0.9502904415
  [alias] storage-sharing relation changed: lost [(0, 2)] (indices < 2 are inputs, >= 2 outputs)
      alias pairs lost          : [(0, 2)]
      alias pairs gained        : []
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 9.151e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 9.15088963508606
      first mismatching element : [0, 0]
      mismatching elements      : 32
      total elements            : 32
      eager error vs float64    : 0.0
      compiled error vs float64 : 9.15088963508606
      accepted budget           : 1.0086151860377868e-06
      expected at that index    : -1.125839829
      actual at that index      : 0.9502904415
  [alias] storage-sharing relation changed: lost [(0, 2)] (indices < 2 are inputs, >= 2 outputs)
      alias pairs lost          : [(0, 2)]
      alias pairs gained        : []
```

## Suspected Stage

```text
cache specialization (guard / stale artifact reuse)
```

Attribution basis: first divergent layer = n/a; changing 'substitute' did not trigger a recompile, yet the warm result differs from a cold compile of the same context: a cached artifact was reused under assumptions that no longer hold

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

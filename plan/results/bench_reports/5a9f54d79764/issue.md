# [pytorch compile] Incorrect result when `layout` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `layout` changes from `noncontig` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: stride read in the source (factor `x.stride`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def stride_read(x):
    if x.stride(0) == 1:
        return x.t().contiguous().sum(dim=0)
    return x.sum(dim=1)

args = (torch.randn(8, 8)  # NOTE: non-contiguous in the failing run,)

eager = stride_read(*args)
torch._dynamo.reset()
compiled = torch.compile(stride_read, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-3.853170156, 1.944055676, -1.13303566, 3.25309515, 0.8078030944, 0.2224572897, 0.958989799, 3.732162952]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-2.853170156, 2.944055557, -0.1330356598, 4.25309515, 1.807803154, 1.22245729, 1.958989859, 4.732162952]
```

## Trigger Condition

Changed semantic factor:

```text
layout: noncontig -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 1.788e-07
      max absolute error        : 1.0000000596046448
      first mismatching element : [0]
      mismatching elements      : 8
      total elements            : 8
      eager error vs float64    : 1.7881393432617188e-07
      compiled error vs float64 : 1.0000001788139343
      accepted budget           : 1.8373347074884805e-06
      expected at that index    : -3.853170156
      actual at that index      : -2.853170156
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

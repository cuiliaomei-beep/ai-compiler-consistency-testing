# [pytorch compile] Compiled execution inconsistent with eager (mutation)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def chunk_inplace(x):
    a, b = x.chunk(2, dim=0)
    a.mul_(3.0)
    return b + a.sum()

args = (torch.randn(8, 4),)

eager = chunk_inplace(*args)
torch._dynamo.reset()
compiled = torch.compile(chunk_inplace, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-7.079997063, -7.42327404, -5.160692215, -4.933834553, -5.128503799, -7.282438278, -6.068703651, -3.874336958, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-7.079997063, -7.42327404, -5.160692215, -4.933834553, -5.128503799, -7.282438278, -6.068703651, -3.874336958, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [mutation] input[0] holds a different value after the call (max diff 4.230e+00); in-place effects diverge
      max absolute error        : 4.230438947677612
      first mismatching element : [0, 0]
      mismatching elements      : 16
      total elements            : 32
      expected at that index    : -3.377519608
      actual at that index      : -1.125839829
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

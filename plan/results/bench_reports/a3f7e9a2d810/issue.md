# [pytorch compile] Compiled execution inconsistent with eager (mutation, alias)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def conditional_mutation(x):
    if x.shape[0] > 8:
        x.add_(1.0)
    return x

args = (torch.randn(16, 4),)

eager = conditional_mutation(*args)
torch._dynamo.reset()
compiled = torch.compile(conditional_mutation, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.1258398294, -0.1523602009, 0.7494214177, 0.5661212206, 1.848710299, 1.692009211, 0.6839872599, -1.115219355, ... (56 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.1258398294, -0.1523602009, 0.7494214177, 0.5661212206, 1.848710299, 1.692009211, 0.6839872599, -1.115219355, ... (56 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [mutation] input[0] holds a different value after the call (max diff 1.000e+00); in-place effects diverge
      max absolute error        : 1.0000001192092896
      first mismatching element : [0, 0]
      mismatching elements      : 64
      total elements            : 64
      expected at that index    : -0.1258398294
      actual at that index      : -1.125839829
  [alias] storage-sharing relation changed: lost [(0, 1)] (indices < 1 are inputs, >= 1 outputs)
      alias pairs lost          : [(0, 1)]
      alias pairs gained        : []
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

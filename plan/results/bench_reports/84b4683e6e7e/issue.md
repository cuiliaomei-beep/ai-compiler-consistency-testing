# [pytorch compile] Incorrect result when `layout` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `layout` changes from `noncontig` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: layout read, or alias structure is written through (factor `x.is_contiguous()`, oracles ['value', 'metadata', 'alias', 'mutation']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def view_inplace(x):
    y = x.view(-1)
    y[0] = y[0] + 1.0
    return x

args = (torch.randn(8, 8)  # NOTE: non-contiguous in the failing run,)

eager = view_inplace(*args)
torch._dynamo.reset()
compiled = torch.compile(view_inplace, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.1258398294, -1.152360201, -0.2505785823, -0.4338788092, 0.8487103581, 0.692009151, -0.3160127699, -2.115219355, ... (56 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.8741601706, -0.1523602009, 0.7494214177, 0.5661212206, 1.848710299, 1.692009211, 0.6839872599, -1.115219355, ... (56 more)]
```

## Trigger Condition

Changed semantic factor:

```text
layout: noncontig -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0000001192092896
      first mismatching element : [0, 0]
      mismatching elements      : 64
      total elements            : 64
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0000001192092896
      accepted budget           : 1.626254402253835e-06
      expected at that index    : -0.1258398294
      actual at that index      : 0.8741601706
  [alias] storage-sharing relation changed: lost [(0, 1)] (indices < 1 are inputs, >= 1 outputs)
      alias pairs lost          : [(0, 1)]
      alias pairs gained        : []
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

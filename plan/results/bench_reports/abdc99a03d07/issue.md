# [pytorch compile] Incorrect result when `resize` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `resize` changes from `{'arg': 0, 'dim': 0, 'value': 17}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: branch/index predicate: n %16 16 (line 3) (factor `x.shape[0]`, oracles ['value', 'metadata', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def mod_tiling(x):
    n = x.shape[0]
    if n % 16 == 0:
        return x.reshape(n // 16, 16, -1).sum(dim=1)
    pad = 16 - n % 16
    y = torch.cat([x, x[:pad]], dim=0)
    return y.reshape(-1, 16, x.shape[1]).sum(dim=1)

args = (torch.randn(17, 8),)

eager = mod_tiling(*args)
torch._dynamo.reset()
compiled = torch.compile(mod_tiling, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-4.727891445, -3.078510284, 2.729067087, 6.01960516, -1.66040659, 4.027376652, 3.954801083, -0.5515768528, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-3.727891445, -2.078510284, 3.729067087, 7.01960516, -0.6604065895, 5.027376652, 4.954801083, 0.4484231472, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
resize: {'arg': 0, 'dim': 0, 'value': 17} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 4.768e-07
      max absolute error        : 1.000000238418579
      first mismatching element : [0, 0]
      mismatching elements      : 16
      total elements            : 16
      eager error vs float64    : 4.76837158203125e-07
      compiled error vs float64 : 1.0000005811452866
      accepted budget           : 3.332023879920598e-06
      expected at that index    : -4.727891445
      actual at that index      : -3.727891445
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

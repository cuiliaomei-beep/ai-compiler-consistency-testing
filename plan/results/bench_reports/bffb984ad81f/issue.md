# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: eager
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def dtype_read(x):
    if x.dtype == torch.float16:
        return (x.float() * x.float()).sum(dim=-1).half()
    return (x * x).sum(dim=-1)

args = (torch.randn(16, 8).requires_grad_(True),)

eager = dtype_read(*args)
torch._dynamo.reset()
compiled = torch.compile(dtype_read, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [8.619692802, 4.771807194, 11.98369122, 7.023387909, 1.4931252, 10.97386169, 3.018136501, 22.18078232, ... (8 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [8.619692802, 4.771807194, 11.98369122, 7.023387909, 1.4931252, 10.97386169, 3.018136501, 22.18078232, ... (8 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [gradient] grad[0]: compiled err vs fp64 = 6.821e+00 exceeds 8.0x eager err 0.000e+00
      max absolute error        : 6.821005344390869
      first mismatching element : [0, 0]
      mismatching elements      : 128
      total elements            : 128
      eager error vs float64    : 0.0
      compiled error vs float64 : 6.821005344390869
      accepted budget           : 3.25250880450767e-06
      expected at that index    : -2.251679659
      actual at that index      : -4.503359318
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/1 runs with fixed seed 0.
```

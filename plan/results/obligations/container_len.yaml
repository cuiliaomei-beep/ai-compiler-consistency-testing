- site: container_len
  factor: "xs.shape[0]"
  kind: shape
  relation: change_only_one_factor
  constraint: len(xs) Gt 2 (line 2)
  values:
    - 1
    - 2
    - 3
  oracle:
    - value
    - metadata
    - exception
  execution:
    - cold
    - warm_sequence
  origin: "branch/index predicate: len(xs) Gt 2 (line 2)"
- site: container_len
  factor: xs.dtype
  kind: dtype
  relation: change_only_one_factor
  constraint: float dtype ladder
  values:
    - float16
    - float64
    - bfloat16
  oracle:
    - value
    - metadata
  execution:
    - cold
    - warm_sequence
  origin: numerics depend on precision regardless of source
- site: container_len
  factor: xs.requires_grad
  kind: requires_grad
  relation: change_only_one_factor
  constraint: grad on/off
  values:
    - true
    - false
  oracle:
    - value
    - metadata
    - gradient
  execution:
    - cold
    - warm_sequence
  origin: requires_grad changes the traced graph regardless of source
- site: container_len
  factor: compile.dynamic
  kind: compile_flag
  relation: change_only_one_factor
  constraint: dynamic shapes on/off
  values:
    - true
    - false
  oracle:
    - value
    - metadata
  execution:
    - cold
  origin: compile flag changes specialization

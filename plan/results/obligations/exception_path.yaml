- site: exception_path
  factor: "x.shape[0]"
  kind: shape
  relation: change_only_one_factor
  constraint: "x.shape[0] Lt 4 (line 2)"
  values:
    - 3
    - 4
    - 5
  oracle:
    - value
    - metadata
    - exception
    - mutation
    - alias
  execution:
    - cold
    - warm_sequence
  origin: "branch/index predicate: x.shape[0] Lt 4 (line 2)"
- site: exception_path
  factor: x.dtype
  kind: dtype
  relation: change_only_one_factor
  constraint: float dtype ladder
  values:
    - float16
    - float64
    - bfloat16
  oracle:
    - value
    - metadata
    - mutation
    - alias
  execution:
    - cold
    - warm_sequence
  origin: numerics depend on precision regardless of source
- site: exception_path
  factor: x.requires_grad
  kind: requires_grad
  relation: change_only_one_factor
  constraint: grad on/off
  values:
    - true
    - false
  oracle:
    - value
    - metadata
    - gradient
  execution:
    - cold
    - warm_sequence
  origin: requires_grad changes the traced graph regardless of source
- site: exception_path
  factor: compile.dynamic
  kind: compile_flag
  relation: change_only_one_factor
  constraint: dynamic shapes on/off
  values:
    - true
    - false
  oracle:
    - value
    - metadata
  execution:
    - cold
  origin: compile flag changes specialization

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def max_pool2d_backward(*args, kernel_size=(), stride=(), padding=(0,), dilation=(1,), ceil_mode=False, **kwargs):
    out, indices = torch.nn.functional.max_pool2d_with_indices(
        *args, kernel_size=kernel_size, stride=stride, padding=padding, dilation=dilation, ceil_mode=ceil_mode, return_indices=True)
    grad_out = torch.ones_like(out)
    if stride is None:
        stride = kernel_size
    out_b = torch.ops.aten.max_pool2d_with_indices_backward.default(
        grad_out, *args, kernel_size, stride, padding, dilation, ceil_mode, indices)
    return out_b

def opinfo_max_pool2d_with_indices_backward(*args):
    return max_pool2d_backward(*args, kernel_size=(3, 2), stride=2, ceil_mode=False, padding=0, dilation=(1, 2), return_indices=False)

args = (torch.randn(1, 2, 3, 6),)

eager = opinfo_max_pool2d_with_indices_backward(*args)
torch._dynamo.reset()
compiled = torch.compile(opinfo_max_pool2d_with_indices_backward, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 0, 0, 0, 0, 0, 0, ... (28 more)]
```

## Actual Behavior

Compiled result:

```text
exception TorchRuntimeError: RuntimeError when making fake tensor call
  Explanation: Dynamo failed to run FX node with fake tensors: call_function aten.max_pool2d_with_indices_backward.default(*(FakeTensor(..., size=(1, s50, (((s69 - 3)//2)) + 1, (((s87 - 3)//2)) + 1)), FakeTensor(..., size=(1, s50, s69, s87)), (3, 2), 2, 0, (1, 2), False, FakeTensor(..., size=(1, s50, (((s69 - 3)//2)) + 1, (((s87 - 3)//2)) + 1),
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: TorchRuntimeError
E2_aot_eager: TorchRuntimeError
E3_inductor: TorchRuntimeError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : RuntimeError when making fake tensor call
  Explanation: Dynamo failed to run FX node with fake tensors: call_function aten.max_pool2d_with_indices_backward.default(*(FakeTensor(..., size=(1, s50, (((
E2_aot_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : RuntimeError when making fake tensor call
  Explanation: Dynamo failed to run FX node with fake tensors: call_function aten.max_pool2d_with_indices_backward.default(*(FakeTensor(..., size=(1, s50, (((
E3_inductor:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : RuntimeError when making fake tensor call
  Explanation: Dynamo failed to run FX node with fake tensors: call_function aten.max_pool2d_with_indices_backward.default(*(FakeTensor(..., size=(1, s50, (((
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 3.
```

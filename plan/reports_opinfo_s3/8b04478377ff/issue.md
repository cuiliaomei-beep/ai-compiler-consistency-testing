# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `(base)` to `bfloat16`.
changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `args.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty(*args):
    return torch.new_empty(*args, (10,), dtype=torch.float64)

args = (torch.randn(2),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'dtype': 'bfloat16'}
args_b = (torch.randn(5).to(torch.bfloat16),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty, backend='inductor')(*args_b)
eager = opinfo_new_empty(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [2.099400186e-311, 7.344788173e-05, 0.01684813575, 69.00770586, 680.0733647, 5.313038832, 7.74742695e-06, 31.25096517, ... (2 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 4.560982088e+185, 4.487397335e+196, 7.651264815e+222, inf, 2.315390626e+174, 7.183304485e+215, 5.590198014e+295, ... (2 more)]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: (base) -> bfloat16
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | **incorrect** |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : inf
      first mismatching element : [0]
      mismatching elements      : 10
      total elements            : 10
      expected at that index    : 2.099399952e-311
      actual at that index      : 0
eager_vs_cold:
  [value] output[0]: compiled err vs fp64 = 4.525e+103 exceeds 4.0x eager err 6.801e+02
      max absolute error        : 4.525023651418748e+103
      first mismatching element : [2]
      mismatching elements      : 8
      total elements            : 10
      eager error vs float64    : 680.0733646545697
      compiled error vs float64 : 4.525023651418748e+103
      accepted budget           : 2720.2934586182787
      expected at that index    : 0.01684813575
      actual at that index      : 1.455454494e+49
eager_vs_warm:
  [value] output[0]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : inf
      first mismatching element : [0]
      mismatching elements      : 10
      total elements            : 10
      expected at that index    : 2.099400186e-311
      actual at that index      : 0
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 2.048e+207 exceeds dtype-aware tolerance 2.048e+201 (no fp64 reference)
      max absolute error        : 2.0475839045899044e+207
      first mismatching element : [4]
      mismatching elements      : 1
      total elements            : 10
      expected at that index    : -2.047583905e+207
      actual at that index      : -6.55668842e+35
```

## Suspected Stage

```text
compilation state leaking across compiles
```

Attribution basis: first divergent layer = n/a; changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 3.
```

## Minimization

Program 2 -> 2 lines, inputs 5 -> 2 elements; 2 reductions kept of 6 tried.
- kept: simplify values
- kept: halve dim 0

# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `args.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(5).requires_grad_(True),)

eager = opinfo_new_empty_strided(*args)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [9.742944332e+15, 0, 0, nan, nan, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [9.092926802e+15, 0, 0, 0, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [value] output[0]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : 650017530445824.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      expected at that index    : 9.742944332e+15
      actual at that index      : 9.092926802e+15
E2_aot_eager:
  [value] output[0]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : 29033978920960.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      expected at that index    : 9.742944332e+15
      actual at that index      : 9.713910353e+15
E3_inductor:
  [value] output[0]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : 3264175144960.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      expected at that index    : 9.742944332e+15
      actual at that index      : 9.746208508e+15
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 3.
```

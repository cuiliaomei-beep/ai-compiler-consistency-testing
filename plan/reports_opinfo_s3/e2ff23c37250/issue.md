# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `(base)` to `{'arg': 0, 'value': True}`.
after switching 'requires_grad' A -> B -> A, A no longer yields its original result (no recompile on return)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `args.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(5),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'requires_grad': {'arg': 0, 'value': True}}
args_b = (torch.randn(5).requires_grad_(True),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty_strided, backend='inductor')(*args_b)
eager = opinfo_new_empty_strided(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [9.105399387e+15, 0, 0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [9.116016546e+15, 0, 0, 0, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: (base) -> {'arg': 0, 'value': True}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | **incorrect** |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | same |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
eager_vs_cold:
  [value] output[0]: compiled err vs fp64 = 9.094e+15 exceeds 4.0x eager err 1.113e+13
      max absolute error        : 9105399386996736.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      eager error vs float64    : 11132555231232.0
      compiled error vs float64 : 9094266831765504.0
      accepted budget           : 44530220924928.0
      expected at that index    : 9.105399387e+15
      actual at that index      : 1.401298464e-45
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 6.469e+14 exceeds dtype-aware tolerance 9.737e+09 (no fp64 reference)
      max absolute error        : 646856434515968.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      expected at that index    : 9.737103177e+15
      actual at that index      : 9.090246742e+15
```

## Suspected Stage

```text
cache invalidation (A -> B -> A)
```

Attribution basis: first divergent layer = n/a; after switching 'requires_grad' A -> B -> A, A no longer yields its original result (no recompile on return)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 0/2 runs with fixed seed 3.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

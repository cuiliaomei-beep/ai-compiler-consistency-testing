# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(5),)

eager = opinfo_new_empty_strided(*args)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='aot_eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 1.401298464e-45, 2.802596929e-45, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [9.109831793e+15, 0, 0, 0, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E2_aot_eager:
  [value] output[0]: compiled err vs fp64 = 9.110e+15 exceeds 4.0x eager err 8.408e-45
      max absolute error        : 9109831793246208.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      eager error vs float64    : 8.407790785948902e-45
      compiled error vs float64 : 9109831793246208.0
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 9.109831793e+15
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 9.754e+15 exceeds 4.0x eager err 8.408e-45
      max absolute error        : 9753595851309056.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      eager error vs float64    : 8.407790785948902e-45
      compiled error vs float64 : 9753595851309056.0
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 9.753595851e+15
```

## Suspected Stage

```text
AOTAutograd / functionalization
```

Attribution basis: first divergent layer = E2_aot_eager; eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 0/2 runs with fixed seed 3.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

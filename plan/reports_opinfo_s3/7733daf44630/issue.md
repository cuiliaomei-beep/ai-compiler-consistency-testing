# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': False}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(5),)

eager = opinfo_new_empty_strided(*args)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [9.723702879e+15, 0, 0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 1.401298464e-45, 2.802596929e-45, 0, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': False} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [value] output[0]: compiled err vs fp64 = 9.735e+15 exceeds 4.0x eager err 1.117e+13
      max absolute error        : 9723702878928896.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      eager error vs float64    : 11166914969600.0
      compiled error vs float64 : 9734869793898496.0
      accepted budget           : 44667659878400.0
      expected at that index    : 9.723702879e+15
      actual at that index      : 0
E2_aot_eager:
  [value] output[0]: compiled err vs fp64 = 6.482e+14 exceeds 4.0x eager err 1.117e+13
      max absolute error        : 637063909081088.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      eager error vs float64    : 11166914969600.0
      compiled error vs float64 : 648230824050688.0
      accepted budget           : 44667659878400.0
      expected at that index    : 9.723702879e+15
      actual at that index      : 9.08663897e+15
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 0/2 runs with fixed seed 3.
```

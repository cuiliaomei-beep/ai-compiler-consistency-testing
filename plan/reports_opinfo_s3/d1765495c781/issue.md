# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `(base)` to `float16`.
after switching 'dtype' A -> B -> A, A no longer yields its original result (no recompile on return)
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `args.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(2),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'dtype': 'float16'}
args_b = (torch.randn(5).to(torch.float16),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty_strided, backend='inductor')(*args_b)
eager = opinfo_new_empty_strided(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [9.090349822e+15, 0, 0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [5.313723777e-41, 0, 2.580810348e-12, 1.383632696e+29, 1.705424709e+38, 0]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: (base) -> float16
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | same |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 1.705e+38 exceeds dtype-aware tolerance 9.120e+09 (no fp64 reference)
      max absolute error        : 1.7054247093447748e+38
      first mismatching element : [0, 0]
      mismatching elements      : 3
      total elements            : 6
      expected at that index    : 9.119933556e+15
      actual at that index      : 9.711505172e+15
```

## Suspected Stage

```text
cache invalidation (A -> B -> A)
```

Attribution basis: first divergent layer = n/a; after switching 'dtype' A -> B -> A, A no longer yields its original result (no recompile on return)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 3.
```

## Minimization

Program 2 -> 2 lines, inputs 5 -> 2 elements; 3 reductions kept of 6 tried.
- kept: simplify values
- kept: default flags
- kept: halve dim 0

# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float64` to `(base)`.
changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `args.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (2, 3), (7, 8), dtype=torch.float32, device='cpu')

args = (torch.randn(5).to(torch.float64),)

# context A then context B on the same compiled callable (no reset)
# A = {'dtype': 'float64'}
# B = {} (base)
args_b = (torch.randn(5),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty_strided, backend='inductor')(*args_b)
eager = opinfo_new_empty_strided(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [9.745521313e+15, 0, -1.21586727e-24, 4.35870737e+27, 5.396910877e+36, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [9.729715833e+15, 0, -1.21586727e-24, 4.35870737e+27, 5.396910877e+36, 0]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float64 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 5.397e+36 exceeds 4.0x eager err 9.727e+15
      max absolute error        : 5.396910877218867e+36
      first mismatching element : [1, 0]
      mismatching elements      : 2
      total elements            : 6
      eager error vs float64    : 9727138852765696.0
      compiled error vs float64 : 5.396910877218867e+36
      accepted budget           : 3.890855541106278e+16
      expected at that index    : 0
      actual at that index      : 4.35870737e+27
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 6.155e+14 exceeds dtype-aware tolerance 9.739e+09 (no fp64 reference)
      max absolute error        : 615451633647616.0
      first mismatching element : [0, 0]
      mismatching elements      : 1
      total elements            : 6
      expected at that index    : 9.738992963e+15
      actual at that index      : 9.123541329e+15
```

## Suspected Stage

```text
compilation state leaking across compiles
```

Attribution basis: first divergent layer = n/a; changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 3.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

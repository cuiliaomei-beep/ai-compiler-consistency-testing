# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
changing 'requires_grad' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `args.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty(*args):
    return torch.new_empty(*args, (10,), dtype=torch.float64)

args = (torch.randn(5).requires_grad_(True),)

# context A then context B on the same compiled callable (no reset)
# A = {'requires_grad': {'arg': 0, 'value': True}}
# B = {} (base)
args_b = (torch.randn(5),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty, backend='inductor')(*args_b)
eager = opinfo_new_empty(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 4.940656458e-324, 9.881312917e-324, 1.482196938e-323, 1.976262583e-323, 2.470328229e-323, 2.964393875e-323, 3.458459521e-323, ... (2 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [2.09940018e-311, 2.598750534e+46, 1.455454494e+49, 5.259364957e+55, 4.525023651e+103, 3.900821411e+43, 9.206214063e+53, 8.646827556e+73, ... (2 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 4.525e+103 exceeds 4.0x eager err 6.727e+39
      max absolute error        : 4.5250236514187464e+103
      first mismatching element : [1]
      mismatching elements      : 8
      total elements            : 10
      eager error vs float64    : 6.72682960347499e+39
      compiled error vs float64 : 4.5250236514187464e+103
      accepted budget           : 2.690731841389996e+40
      expected at that index    : 7.344788173e-05
      actual at that index      : 2.598750534e+46
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 4.525e+103 exceeds 4.0x eager err 6.727e+39
      max absolute error        : 4.5250236514187464e+103
      first mismatching element : [1]
      mismatching elements      : 8
      total elements            : 10
      eager error vs float64    : 6.72682960347499e+39
      compiled error vs float64 : 4.5250236514187464e+103
      accepted budget           : 2.690731841389996e+40
      expected at that index    : 4.940656458e-324
      actual at that index      : 2.598750534e+46
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 6.727e+51 exceeds dtype-aware tolerance 6.802e-04 (no fp64 reference)
      max absolute error        : 6.726829603468262e+51
      first mismatching element : [1]
      mismatching elements      : 9
      total elements            : 10
      expected at that index    : 7.344788173e-05
      actual at that index      : 1.61206406e+23
```

## Suspected Stage

```text
compilation state leaking across compiles
```

Attribution basis: first divergent layer = n/a; changing 'requires_grad' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 3.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `(base)` to `{'arg': 0, 'value': True}`.
after switching 'requires_grad' A -> B -> A, A no longer yields its original result (no recompile on return)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `args.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty(*args):
    return torch.new_empty(*args, (3, 2, 2))

args = (torch.randn(0, 1, 0),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'requires_grad': {'arg': 0, 'value': True}}
args_b = (torch.randn(0, 5, 0).requires_grad_(True),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty, backend='inductor')(*args_b)
eager = opinfo_new_empty(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [7.932214885e-09, 9.500803588e-43, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, ... (4 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [7.926743706e-09, 9.500803588e-43, 0, 1.970728159, 2, 2.270175219, 2, 2.050056934, ... (4 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: (base) -> {'arg': 0, 'value': True}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | **incorrect** |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | same |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
eager_vs_cold:
  [value] output[0]: compiled err vs fp64 = 2.497e+00 exceeds 4.0x eager err 3.688e-01
      max absolute error        : 2.4968115257288446
      first mismatching element : [0, 1, 1]
      mismatching elements      : 6
      total elements            : 12
      eager error vs float64    : 0.3687964843775262
      compiled error vs float64 : 2.496661901473999
      accepted budget           : 1.4751859375101048
      expected at that index    : 9.999999747e-05
      actual at that index      : 1.970728159
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 2.497e+00 exceeds 4.0x eager err 3.688e-01
      max absolute error        : 2.4968115257288446
      first mismatching element : [0, 1, 1]
      mismatching elements      : 6
      total elements            : 12
      eager error vs float64    : 0.3687964843775262
      compiled error vs float64 : 2.496661901473999
      accepted budget           : 1.4751859375101048
      expected at that index    : 9.999999747e-05
      actual at that index      : 1.970728159
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 2.497e+00 exceeds dtype-aware tolerance 2.301e-06 (no fp64 reference)
      max absolute error        : 2.4969115257263184
      first mismatching element : [0, 1, 1]
      mismatching elements      : 8
      total elements            : 12
      expected at that index    : 0
      actual at that index      : 1.970728159
```

## Suspected Stage

```text
cache invalidation (A -> B -> A)
```

Attribution basis: first divergent layer = n/a; after switching 'requires_grad' A -> B -> A, A no longer yields its original result (no recompile on return)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 0/2 runs with fixed seed 2.
```

## Minimization

Program 2 -> 2 lines, inputs 0 -> 0 elements; 5 reductions kept of 6 tried.
- kept: halve dim 1
- kept: simplify values
- kept: halve dim 1
- kept: simplify values
- kept: simplify values

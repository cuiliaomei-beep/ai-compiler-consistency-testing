# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `bfloat16` to `(base)`.
changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `args.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty(*args):
    return torch.new_empty(*args, (3, 2, 2))

args = (torch.randn(0, 5, 0).to(torch.bfloat16),)

# context A then context B on the same compiled callable (no reset)
# A = {'dtype': 'bfloat16'}
# B = {} (base)
args_b = (torch.randn(0, 5, 0),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty, backend='inductor')(*args_b)
eager = opinfo_new_empty(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [7.958874448e-09, 9.500803588e-43, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, 9.999999747e-05, ... (4 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [3.689348815e+19, 2.514949083, 0, 1.970728159, 2, 2.270175219, 2, 2.050056934, ... (4 more)]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: bfloat16 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | **differs** |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
cold_vs_warm:
  [value] output[0]: compiled err vs fp64 = 3.689e+19 exceeds 4.0x eager err 1.133e+04
      max absolute error        : 3.6893488147419103e+19
      first mismatching element : [0, 0, 0]
      mismatching elements      : 1
      total elements            : 12
      eager error vs float64    : 11327.9921875
      compiled error vs float64 : 3.6893488147419103e+19
      accepted budget           : 45311.96875
      expected at that index    : 7.926047374e-09
      actual at that index      : 3.689348815e+19
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 3.689e+19 exceeds 4.0x eager err 1.133e+04
      max absolute error        : 3.6893488147419103e+19
      first mismatching element : [0, 0, 0]
      mismatching elements      : 1
      total elements            : 12
      eager error vs float64    : 11327.992087500003
      compiled error vs float64 : 3.6893488147419103e+19
      accepted budget           : 45311.96835000001
      expected at that index    : 7.958874448e-09
      actual at that index      : 3.689348815e+19
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 3.169e+29 exceeds dtype-aware tolerance 1.953e-03 (no fp64 reference)
      max absolute error        : 3.1691265005705735e+29
      first mismatching element : [0, 0, 0]
      mismatching elements      : 1
      total elements            : 12
      expected at that index    : 0
      actual at that index      : 3.169126501e+29
```

## Suspected Stage

```text
compilation state leaking across compiles
```

Attribution basis: first divergent layer = n/a; changing 'dtype' did trigger a recompile, but the result still differs from a cold compile: state leaks across compilations

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 2.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float64` to `(base)`.
after switching 'dtype' A -> B -> A, A no longer yields its original result (no recompile on return)
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `args.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_new_empty_strided(*args):
    return torch.new_empty_strided(*args, (10,), (5,), dtype=torch.float64)

args = (torch.randn(5).to(torch.float64),)

# context A then context B on the same compiled callable (no reset)
# A = {'dtype': 'float64'}
# B = {} (base)
args_b = (torch.randn(5),)
torch._dynamo.reset()
compiled = torch.compile(opinfo_new_empty_strided, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(opinfo_new_empty_strided, backend='inductor')(*args_b)
eager = opinfo_new_empty_strided(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1.439128733e-311, 0, 4.940656458e-324, 4.940656458e-324, 9.881312917e-324, 0, 1.482196938e-323, 4.940656458e-324, ... (2 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1.439128741e-311, 4.940656458e-324, 9.881312917e-324, 1.482196938e-323, 1.976262583e-323, 0, 9.881312917e-324, 1.482196938e-323, ... (2 more)]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float64 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | same |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | **no** |

## Difference

```text
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 1.093e+01 exceeds dtype-aware tolerance 1.000e-07 (no fp64 reference)
      max absolute error        : 10.928421020507812
      first mismatching element : [9]
      mismatching elements      : 1
      total elements            : 10
      expected at that index    : 4.940656458e-324
      actual at that index      : 10.92842102
```

## Suspected Stage

```text
cache invalidation (A -> B -> A)
```

Attribution basis: first divergent layer = n/a; after switching 'dtype' A -> B -> A, A no longer yields its original result (no recompile on return)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 2.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

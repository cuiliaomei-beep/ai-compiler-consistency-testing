# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float64` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `x.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, c):
    z = torch.where(c, torch.full_like(x, 0.5), torch.full_like(x, -0.5))
    y = z.to(torch.float16) + x.to(torch.float16)
    return y, y.float().sum()

args = (torch.randn(2).to(torch.float64), torch.randint(0, 4, (2,)).to(torch.bool))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: P[T,T]
[0] [0.5, -0.3999023438, -0.3000488281, -0.1999511719, -0.1000976562, 1, 0.1000976562, 0.2001953125, ... (16 more)]
[1] [20.59960938]
```

## Actual Behavior

Compiled result:

```text
structure: P[T,T]
[0] [0.5, -0.3999023438, -0.3000488281, -0.1999511719, -0.09997558594, 1, 0.09997558594, 0.1999511719, ... (16 more)]
[1] [20.60000038]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float64 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[1]: compiled err vs fp64 = 3.910e-04 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.0003910064697265625
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.0003910064697265625
      accepted budget           : 9.822659194469452e-06
      expected at that index    : 20.59960938
      actual at that index      : 20.60000038
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 48 -> 4 elements; 6 reductions kept of 10 tried.
- kept: drop rank
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: drop rank

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/183761 — torch.compile produces different fp16 results for where + full_like + cast + add pattern
- https://github.com/pytorch/pytorch/issues/173939 — [inductor] Inductor doesn't preserve pin_memory=True for some constructors.
- https://github.com/pytorch/pytorch/issues/96064 — `torch.compile` silently produces wrong answer when compiling functions with (unrolled) loop

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    t = inst_norm(x)
    t = pool(t)
    t = F.normalize(t, dim=0)
    return t

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.2380282581, -0.2828969657, -0.06754474342, -0.1676460057, 0.1668363512, 0.1998581588, -0.05697699636, -0.3849032521, ... (3576 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.1674321741, 0.2230191976, 0.03573259339, 0.1301743239, -0.08086580038, -0.04520190507, 0.03128480539, 0.3242988884, ... (3576 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.871e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.8706058859825134
      first mismatching element : [0, 0, 0, 0]
      mismatching elements      : 3584
      total elements            : 3584
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.8706058859825134
      accepted budget           : 4.76837158203125e-07
      expected at that index    : -0.2380282581
      actual at that index      : 0.1674321741
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 8 -> 5 lines, inputs 0 -> 0 elements; 3 reductions kept of 5 tried.
- removed 2 statement(s): t = F.normalize(t, dim=0); return t
- removed 2 statement(s): t = hardswish(t); return t
- removed 2 statement(s): t = torch.mul(t, torch.where(x > 0, x, x; return t

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/191499 — [Inductor][CPU] torch.compile returns 0 for short positive uint8/int8 amin reductions on Apple Silicon
- https://github.com/pytorch/pytorch/pull/185163 — [aot_autograd] Handle None in saved tensors for LSTM/RNN backward; enable allow_rnn by default
- https://github.com/pytorch/pytorch/pull/185891 — [dynamo] Guard AOTAutograd input mutation overlap changes
- https://github.com/pytorch/pytorch/issues/133734 — PyTorch `torch.compile` inductor backend shows enormous compile-time regressions.
- https://github.com/pytorch/pytorch/pull/195432 — [Dynamo] route PrivateUse1 tensor classes in graph

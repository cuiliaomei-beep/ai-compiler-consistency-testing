# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    t = inst_norm(x)
    t = pool(t)
    t = F.normalize(t, dim=0)
    return t

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.2380282581, -0.2828969657, -0.06754474342, -0.1676460057, 0.1668363512, 0.1998581588, -0.05697699636, -0.3849032521, ... (3576 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.3045192063, 0.3875744641, 0.06759698689, 0.2170179039, -0.1427354515, -0.0867921859, 0.05990697071, 0.5649364591, ... (3576 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.895e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.8952264785766602
      first mismatching element : [0, 0, 0, 0]
      mismatching elements      : 3584
      total elements            : 3584
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.8952264785766602
      accepted budget           : 4.76837158203125e-07
      expected at that index    : -0.2380282581
      actual at that index      : 0.3045192063
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 8 -> 5 lines, inputs 0 -> 0 elements; 3 reductions kept of 5 tried.
- removed 2 statement(s): t = F.normalize(t, dim=0); return t
- removed 2 statement(s): t = hardswish(t); return t
- removed 2 statement(s): t = torch.mul(t, torch.where(x > 0, x, x; return t

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194770 — [PrivateUse1][dynamo] torch._dynamo.reset() should support external reset callbacks for OOT backend caches
- https://github.com/pytorch/pytorch/issues/194234 — [dynamo] `torch.compile` raises `NameError: name 'stream1' is not defined` when a compiled function calls `torch.cuda.Stream.wait_stream` and returns a single-element list constant
- https://github.com/pytorch/pytorch/issues/193760 — [inductor][cuda] `view(torch.bool).copy_(y.view(torch.bool)).view(torch.float32)` silently corrupts data under `torch.compile`
- https://github.com/pytorch/pytorch/issues/194347 — [inductor][cpu] `torch.minimum` with signed zeros returns +0.0 under `torch.compile` on CPU while eager returns -0.0
- https://github.com/pytorch/pytorch/issues/194345 — [inductor][cuda] `torch.remainder` / `torch.fmod` wrong results under `torch.compile` on CUDA for large |a/b| ratios

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(loc, scale):
    d = torch.distributions.Normal(loc, scale)
    return d.sample()

args = (torch.randn(1).to(torch.float16), torch.randn(1).to(torch.float64))

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1.541015625, -0.2934570312, -2.1796875, 0.568359375, -1.084960938, -1.3984375, 0.4033203125, 0.837890625]
```

## Actual Behavior

Compiled result:

```text
exception RuntimeError: expected dtype torch.float64 but got torch.float16
Error in op: torch.ops.aten.normal.Tensor_Tensor
This error most often comes from an incorrect fake (aka meta) kernel for a custom op.
Use torch.library.opcheck to test your custom op.
See https://pytorch.org/docs/stable/library.html#torch.library.opcheck
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: RuntimeError
```

## Difference

```text
E3_inductor:
  [exception] test raised RuntimeError, ref returned normally
      exception                 : RuntimeError
      message                   : expected dtype torch.float64 but got torch.float16
Error in op: torch.ops.aten.normal.Tensor_Tensor
This error most often comes from an incorrect fake (aka meta) kernel for a custom op.
Use torch.libr
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 3 -> 3 lines, inputs 16 -> 2 elements; 6 reductions kept of 6 tried.
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 0

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194547 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/194547 — torch.compile silently promotes dtype for torch.distributions.Normal.sample() where eager preserves it

# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float16` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `x.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def loss(x):
    return SinWithZeroBackward.apply(x).sum()

args = (torch.randn(4).to(torch.float16),)

eager = loss(*args)
torch._dynamo.reset()
compiled = torch.compile(loss, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.53515625, 0.6640625, 0.9868164062, 0.2497558594]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float16 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [value] output[0]: compiled err vs fp64 = 9.868e-01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.98681640625
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.98681640625
      accepted budget           : 0.00390625
      expected at that index    : 0
      actual at that index      : 0.53515625
E2_aot_eager:
  [value] output[0]: compiled err vs fp64 = 9.868e-01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.98681640625
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.98681640625
      accepted budget           : 0.00390625
      expected at that index    : 0
      actual at that index      : 0.53515625
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 9.868e-01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.98681640625
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.98681640625
      accepted budget           : 0.00390625
      expected at that index    : 0
      actual at that index      : 0.53515625
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/193279 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/192985 — Dynamo silently drops custom `jvp` of `autograd.Function` under `torch.func.jvp`, producing wrong tangents
- https://github.com/pytorch/pytorch/pull/193277 — Fix Dynamo silently dropping custom jvp of autograd.Function under forward AD
- https://github.com/pytorch/pytorch/issues/193279 — Dynamo silently drops custom `backward` of `autograd.Function` under `torch.func.grad`, producing wrong gradients

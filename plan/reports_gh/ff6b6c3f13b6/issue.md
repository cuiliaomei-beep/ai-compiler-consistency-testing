# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float64` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `self.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        s = F.softmax(x, dim=-1)
        total = torch.sum(s, dim=-1, keepdim=False)
        return torch.floor(total)

args = (torch.randn(5, 3).to(torch.float64),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [1, 1, 1, 1, 1, 1, 1, 1, ... (35 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1, 1, 1, 0, 1, 1, 0, 1, ... (35 more)]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float64 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0
      first mismatching element : [3]
      mismatching elements      : 19
      total elements            : 43
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0
      accepted budget           : 1e-08
      expected at that index    : 1
      actual at that index      : 0
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 2537 -> 15 elements; 10 reductions kept of 16 tried.
- kept: halve dim 0
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 1
- kept: halve dim 0
- kept: halve dim 1
- kept: halve dim 1

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/189787 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/189787 — Inductor constant-folds `sum(softmax)` to exactly 1.0, causing `floor()` to produce wrong integer results

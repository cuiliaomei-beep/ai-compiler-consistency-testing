# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x, y):
    x = 2 * x  # x = [[0,2],[4,6]]
    c = torch.cat([x, y], dim=1)  # c = [[0,2,0,1], [4,6,2,3]]
    c[:, [1, 0]] = c[:, [0, 1]]  # c should be [[2,0,0,1], [6,4,2,3]]
    return c[:, :2] + x

args = (torch.randint(0, 4, (1, 2,)), torch.randint(0, 4, (1, 2,)))

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [2, 2, 10, 10]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [4, 0, 12, 8]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: non-float values differ exactly
      first mismatching element : [0, 0]
      mismatching elements      : 4
      total elements            : 4
      expected at that index    : 2
      actual at that index      : 4
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 5 -> 5 lines, inputs 8 -> 4 elements; 4 reductions kept of 10 tried.
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194234 — [dynamo] `torch.compile` raises `NameError: name 'stream1' is not defined` when a compiled function calls `torch.cuda.Stream.wait_stream` and returns a single-element list constant
- https://github.com/pytorch/pytorch/issues/195442 — [TorchInductor] `batch_aten_sub` miscompilation drops `alpha`
- https://github.com/pytorch/pytorch/issues/194898 — [TorchInductor] normalization_pass misses torch.concatenate
- https://github.com/pytorch/pytorch/issues/190855 — [inductor] Wrong Hessian: `torch.compile(torch.func.hessian(f))` miscompiles the derivative of `torch.trace` — a constant is added to every Hessian entry
- https://github.com/pytorch/pytorch/issues/181696 — Eval-mode `BatchNorm`/`GroupNorm` produces wrong output under `torch.compile(backend='inductor')`

# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    normed = inst_norm(x)
    pooled = pool1(normed)
    selected = torch.where(pooled > 0, pooled, normed)
    return selected

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 0, 0, 0, 0, 0, 0, ... (32947 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 0, 0, 0, 0, 0, 0, 0, ... (32947 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.927e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 3.9268531799316406
      first mismatching element : [0, 2, 0, 0]
      mismatching elements      : 15379
      total elements            : 32955
      eager error vs float64    : 0.0
      compiled error vs float64 : 3.9268531799316406
      accepted budget           : 1.991124008782208e-06
      expected at that index    : 0.2563272715
      actual at that index      : 0
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 7 -> 5 lines, inputs 0 -> 0 elements; 1 reductions kept of 4 tried.
- removed 2 statement(s): pooled2 = pool2(pooled); return torch.sub(pooled2, selected)

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194770 — [PrivateUse1][dynamo] torch._dynamo.reset() should support external reset callbacks for OOT backend caches
- https://github.com/pytorch/pytorch/issues/193760 — [inductor][cuda] `view(torch.bool).copy_(y.view(torch.bool)).view(torch.float32)` silently corrupts data under `torch.compile`
- https://github.com/pytorch/pytorch/issues/185470 — [Inductor] `F.threshold` bf16 miscompilation: scalar threshold not cast to tensor dtype
- https://github.com/pytorch/pytorch/issues/195784 — [TorchInductor] `_sfdp_pattern_25` and `_sfdp_pattern_26` miss FP16 Q/K/V with an FP32 attention mask
- https://github.com/pytorch/pytorch/issues/192293 — torch.special.i0/i1 (and torch.i0) overflow to inf at large finite float64 inputs (~713.0) although the result is finite

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def test_factory_apis():
    return {
        "ones": torch.ones(2),
        "zeros": torch.zeros(2),
        "tensor": torch.tensor([1.0, 2.0]),
        "arange": torch.arange(2.0),
        "empty": torch.empty(2)
    }

args = (...)  # see execution_trace.json

eager = test_factory_apis(*args)
torch._dynamo.reset()
compiled = torch.compile(test_factory_apis, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: D{'arange':T,'empty':T,'ones':T,'tensor':T,'zeros':T}
[0] [0, 1]
[1] [nan, nan]
[2] [1, 1]
[3] [1, 2]
[4] [0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: D{'arange':T,'empty':T,'ones':T,'tensor':T,'zeros':T}
[0] [0, 1]
[1] [0, 0]
[2] [1, 1]
[3] [1, 2]
[4] [0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[1]: NaN/Inf pattern differs from the float64 truth
      max absolute error        : 0.0
      mismatching elements      : 0
      total elements            : 2
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 1/2 runs with fixed seed 0.
```

## Minimization

_Not minimized: the failure did not reproduce before minimizing; nothing changed_

## Possible existing issues

- https://github.com/pytorch/pytorch/pull/185891 — [dynamo] Guard AOTAutograd input mutation overlap changes
- https://github.com/pytorch/pytorch/issues/176178 — [CuTeDSL] grouped_mm illegal address from TMA descriptor init/update shape mismatch
- https://github.com/pytorch/pytorch/pull/192025 — [Inductor] Keep padded tail divisors non-zero in vectorized integer remainder
- https://github.com/pytorch/pytorch/issues/165280 — [inductor] avoid triton kernel when creating zero tensor
- https://github.com/pytorch/pytorch/issues/161609 — Bucketize lowering crash when using a sliced computed tensor as boundaries

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)



args = (torch.randn(1), torch.randn(1))

eager = add(*args)
torch._dynamo.reset()
compiled = torch.compile(add, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [8, 12]
```

## Actual Behavior

Compiled result:

```text
exception TypeError: Multiple dispatch failed for 'torch._ops.aten.add.Tensor'; all __torch_dispatch__ handlers returned NotImplemented:

  - mode object <torch._functorch._aot_autograd.runtime_wrappers._AnalyzeCustomOpInputOutputMode object at 0x0000016698C773F0>

For more information, try re-running with TORCH_LOGS=not_implemented
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: TypeError
E3_inductor: ok
```

## Difference

```text
E2_aot_eager:
  [exception] test raised TypeError, ref returned normally
      exception                 : TypeError
      message                   : Multiple dispatch failed for 'torch._ops.aten.add.Tensor'; all __torch_dispatch__ handlers returned NotImplemented:

  - mode object <torch._functorch._aot_autograd.runtime_wrappers._AnalyzeCustomOpIn
```

## Suspected Stage

```text
AOTAutograd / functionalization
```

Attribution basis: first divergent layer = E2_aot_eager; eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 0 -> 0 lines, inputs 4 -> 2 elements; 4 reductions kept of 4 tried.
- source unavailable; program not minimized
- kept: halve dim 0
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/191499 — [Inductor][CPU] torch.compile returns 0 for short positive uint8/int8 amin reductions on Apple Silicon
- https://github.com/pytorch/pytorch/pull/185163 — [aot_autograd] Handle None in saved tensors for LSTM/RNN backward; enable allow_rnn by default
- https://github.com/pytorch/pytorch/pull/185891 — [dynamo] Guard AOTAutograd input mutation overlap changes
- https://github.com/pytorch/pytorch/issues/133734 — PyTorch `torch.compile` inductor backend shows enormous compile-time regressions.
- https://github.com/pytorch/pytorch/pull/195432 — [Dynamo] route PrivateUse1 tensor classes in graph

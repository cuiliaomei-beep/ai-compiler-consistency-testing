# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, w, b):
    y = (x @ w).to(torch.bfloat16) + b
    return y, y.mean(-1)

args = (torch.randn(2, 15, 32).requires_grad_(True), torch.randn(32, 17), torch.randn(17))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: P[T,T]
[0] [2.71809411, -11.21207142, 16.73663712, 1.942465901, -7.750169754, 0.3295410872, -4.788474083, -7.005776882, ... (502 more)]
[1] [-2.284110308, 0.697090745, 0.7282977104, -0.7080534697, 0.5299980044, 0.9483113885, -2.660977602, -0.274747014, ... (22 more)]
```

## Actual Behavior

Compiled result:

```text
structure: P[T,T]
[0] [2.708857298, -11.18498516, 16.7206192, 1.942487717, -7.737704277, 0.3312830329, -4.789435863, -7.010945797, ... (502 more)]
[1] [-2.284674168, 0.6968807578, 0.7291796207, -0.7088425159, 0.5345503688, 0.9453678131, -2.661511421, -0.2746063769, ... (22 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.072e-02 exceeds 4.0x eager err 9.537e-07
      max absolute error        : 0.0307159423828125
      first mismatching element : [0, 0, 0]
      mismatching elements      : 507
      total elements            : 510
      eager error vs float64    : 9.5367431640625e-07
      compiled error vs float64 : 0.030715584754943848
      accepted budget           : 7.980650480021723e-06
      expected at that index    : 2.71809411
      actual at that index      : 2.708857298
  [value] output[1]: compiled err vs fp64 = 4.552e-03 exceeds 4.0x eager err 1.771e-07
      max absolute error        : 0.004552364349365234
      first mismatching element : [0, 0]
      mismatching elements      : 30
      total elements            : 30
      eager error vs float64    : 1.7706085664315196e-07
      compiled error vs float64 : 0.00455239765784321
      accepted budget           : 1.497944595030276e-06
      expected at that index    : -2.284110308
      actual at that index      : -2.284674168
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 3 -> 3 lines, inputs 1521 -> 1521 elements; 3 reductions kept of 11 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194770 — [PrivateUse1][dynamo] torch._dynamo.reset() should support external reset callbacks for OOT backend caches
- https://github.com/pytorch/pytorch/issues/194234 — [dynamo] `torch.compile` raises `NameError: name 'stream1' is not defined` when a compiled function calls `torch.cuda.Stream.wait_stream` and returns a single-element list constant
- https://github.com/pytorch/pytorch/issues/193760 — [inductor][cuda] `view(torch.bool).copy_(y.view(torch.bool)).view(torch.float32)` silently corrupts data under `torch.compile`
- https://github.com/pytorch/pytorch/issues/194347 — [inductor][cpu] `torch.minimum` with signed zeros returns +0.0 under `torch.compile` on CPU while eager returns -0.0
- https://github.com/pytorch/pytorch/issues/194345 — [inductor][cuda] `torch.remainder` / `torch.fmod` wrong results under `torch.compile` on CUDA for large |a/b| ratios

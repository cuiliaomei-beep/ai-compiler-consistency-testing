# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `(base)` to `float16`.
after switching 'dtype' A -> B -> A, A no longer yields its original result (recompiled on return)
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `t.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(t):

    class Holder:
        items = []
    y = t * 2
    torch._dynamo.graph_break()
    Holder.items.append(2)
    z = y * 3
    return z.sum() + len(Holder.items)

args = (torch.randn(1).to(torch.float64),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'dtype': 'float16'}
args_b = (torch.randn(3).to(torch.float16),)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(fn, backend='inductor')(*args_b)
eager = fn(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-5.6953125]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-6.6953125]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: (base) -> float16
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | **incorrect** |
| compiled A -> warm B | **incorrect** |
| cold(B) vs warm(B) | same |
| recompile on A -> B | yes |
| compiled A -> B -> A vs A | **differs** |
| recompile on B -> A | yes |

## Difference

```text
eager_vs_cold:
  [value] output[0]: compiled err vs fp64 = 1.002e+00 exceeds 4.0x eager err 1.953e-03
      max absolute error        : 1.0
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.001953125
      compiled error vs float64 : 1.001953125
      accepted budget           : 0.022247314453125
      expected at that index    : -5.6953125
      actual at that index      : -6.6953125
eager_vs_warm:
  [value] output[0]: compiled err vs fp64 = 1.002e+00 exceeds 4.0x eager err 1.953e-03
      max absolute error        : 1.0
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.001953125
      compiled error vs float64 : 1.001953125
      accepted budget           : 0.022247314453125
      expected at that index    : -5.6953125
      actual at that index      : -6.6953125
A_vs_A_after_B:
  [value] output[0]: |ref-test| = 1.000e+00 exceeds dtype-aware tolerance 6.793e-06 (no fp64 reference)
      max absolute error        : 1.0
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      expected at that index    : -6.693237506
      actual at that index      : -5.693237506
```

## Suspected Stage

```text
cache invalidation (A -> B -> A)
```

Attribution basis: first divergent layer = n/a; after switching 'dtype' A -> B -> A, A no longer yields its original result (recompiled on return)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 10 -> 9 lines, inputs 3 -> 1 elements; 5 reductions kept of 14 tried.
- removed 1 statement(s): Holder.items.append(1)
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/192872 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/192872 — dynamo: mutation to a LOAD_BUILD_CLASS-created class's sourceless attribute is lost across a graph break

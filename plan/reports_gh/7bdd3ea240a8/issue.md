# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)



args = (torch.randn(1), torch.randn(1))

eager = add(*args)
torch._dynamo.reset()
compiled = torch.compile(add, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [8, 12]
```

## Actual Behavior

Compiled result:

```text
exception TypeError: Multiple dispatch failed for 'torch._ops.aten.add.Tensor'; all __torch_dispatch__ handlers returned NotImplemented:

  - mode object <torch._functorch._aot_autograd.runtime_wrappers._AnalyzeCustomOpInputOutputMode object at 0x000001669901D860>

For more information, try re-running with TORCH_LOGS=not_implemented
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: TypeError
E3_inductor: ok
```

## Difference

```text
E2_aot_eager:
  [exception] test raised TypeError, ref returned normally
      exception                 : TypeError
      message                   : Multiple dispatch failed for 'torch._ops.aten.add.Tensor'; all __torch_dispatch__ handlers returned NotImplemented:

  - mode object <torch._functorch._aot_autograd.runtime_wrappers._AnalyzeCustomOpIn
```

## Suspected Stage

```text
AOTAutograd / functionalization
```

Attribution basis: first divergent layer = E2_aot_eager; eager and backend='eager' agree but aot_eager differs: AOTAutograd / functionalization (alias and in-place handling)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 0 -> 0 lines, inputs 4 -> 2 elements; 4 reductions kept of 4 tried.
- source unavailable; program not minimized
- kept: halve dim 0
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/194770 — [PrivateUse1][dynamo] torch._dynamo.reset() should support external reset callbacks for OOT backend caches
- https://github.com/pytorch/pytorch/issues/194234 — [dynamo] `torch.compile` raises `NameError: name 'stream1' is not defined` when a compiled function calls `torch.cuda.Stream.wait_stream` and returns a single-element list constant
- https://github.com/pytorch/pytorch/issues/193760 — [inductor][cuda] `view(torch.bool).copy_(y.view(torch.bool)).view(torch.float32)` silently corrupts data under `torch.compile`
- https://github.com/pytorch/pytorch/issues/194347 — [inductor][cpu] `torch.minimum` with signed zeros returns +0.0 under `torch.compile` on CPU while eager returns -0.0
- https://github.com/pytorch/pytorch/issues/194345 — [inductor][cuda] `torch.remainder` / `torch.fmod` wrong results under `torch.compile` on CUDA for large |a/b| ratios

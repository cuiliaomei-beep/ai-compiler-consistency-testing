# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        # Schema: mkldnn::_convolution_transpose_pointwise(X, W, B, padding, output_padding, stride, dilation, groups, attr, scalars, algorithm)
        y = torch.ops.mkldnn._convolution_transpose_pointwise.default(
            x,
            self.weight,
            self.bias,
            [1, 1],      # padding
            [1, 1],      # output_padding
            [2, 2],      # stride
            [1, 1],      # dilation
            1,           # groups
            "none",      # attr
            [],          # scalars
            ""           # algorithm
        )
        # Post-ops to prevent trivial elimination
        y = torch.add(y, 3)
        y = torch.clamp_min(y, 0)
        y = torch.clamp_max(y, 6)
        return torch.div(y, 6)

args = (torch.randn(1, 3, 2, 16),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.3340363503, 0.8523380756, 0.5796812177, 0.4286499023, 0.1820159554, 0.9130280614, 0.2826563716, 0.8879606128, ... (32760 more)]
```

## Actual Behavior

Compiled result:

```text
exception AssertionError: expected size 4==4, stride 8192==3072 at dim=0; expected size 8==3, stride 1==1 at dim=1; expected size 32==32, stride 256==96 at dim=2; expected size 32==32, stride 8==3 at dim=3
Error in op: torch._inductor.fx_passes.functools.partial(<function _register_unary_fusion_lowering.<locals>.fn at 0x0000015114794040>, Match(..., [arg2_1, arg1_1, arg0_1, [1, 1], [1, 1], [2, 2], [1, 1], 1, 'none', [], ''], {}))
This error most often comes from a incorrect fake (aka meta) kernel for a custom op.
Use torch.library.opcheck to test your custom op.
See https://pytorch.org/docs/stable/library.html#torch.library.opcheck
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: AssertionError
```

## Difference

```text
E3_inductor:
  [exception] test raised AssertionError, ref returned normally
      exception                 : AssertionError
      message                   : expected size 4==4, stride 8192==3072 at dim=0; expected size 8==3, stride 1==1 at dim=1; expected size 32==32, stride 256==96 at dim=2; expected size 32==32, stride 8==3 at dim=3
Error in op: torch._
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 20 -> 20 lines, inputs 3072 -> 96 elements; 8 reductions kept of 16 tried.
- kept: halve dim 0
- kept: halve dim 2
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 2
- kept: halve dim 2

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/172711 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/172711 — torch.compile produces wrong output channels for mkldnn._convolution_transpose_pointwise (FakeTensor shape inference bug) 

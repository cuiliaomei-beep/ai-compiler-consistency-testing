# 待提交清单（RQ5，计划 §18.6：人工审核后才提交）

> **2026-09-09 用户决定：第 1–5 条全部暂不提交，仅登记。** 未经用户明确指示不得提交任何 issue。第 6–10 条（2026-09-10）同样只登记。
> **2026-09-10 下午**：按 `0909ISSUEs` 的格式为第 1–10 条各写了一份可直接粘贴的 issue 草稿 → `issues_0910/`（README 有优先级与查重结论；`results/dedup_0910.md` 是系统查重记录）。expand(-1) 0 维一条与 open #70398 同根，改为评论稿。

| # | 发现 | 版本 | 状态 | 提交稿 | 查重 | 备注 |
|---|---|---|---|---|---|---|
| 1 | `torch.var_mean` / `torch.std_mean` 对空张量在 Inductor 下返回 `(nan, 0.)`，eager/aot_eager 为 `(nan, nan)`（mean 分量错误，inductor_codegen）；覆盖 correction=1/0、fp32/fp16、形状 (0,)/(0,1)、dynamic 开关 | torch 2.14.0+cpu | **待提交**（用户 2026-09-08 决定暂不提交） | `reports_opinfo_all/0211b330cc67/ISSUE_FINAL.md` | 非重复；相关 #113167（已关闭，不同情形）、#61901、#191229 | 已手工验证 `minimal_verified.py`；第 4 轮四个上下文 2/2，第 6 轮 std_mean/var_mean 共 5 个程序再次命中 |

提交时：把 `ISSUE_FINAL.md` 粘到 pytorch/pytorch 的 issue tracker，然后

```bash
python run.py campaign --status 0211b330cc67=Submitted --out reports_opinfo_all
```

之后在本表登记 issue 编号，并随开发者回复更新为 Confirmed / Fixed / Duplicate / Invalid。

| 2 | `torch.std / var / var_mean / std_mean` 对 1e30 量级 float32 输入，Inductor 全部返回 NaN；eager 的 `std` 为有限值 9.56e29 且等于 fp64 真值。**补充（2026-09-10，反向差分）**：对 1e-30 量级输入，编译后 `std` 前向返回 0、**梯度全 0**，eager/fp64 前向 1.44e-30、梯度为正确的 O(1) 值（0.318…）；correction=0、逐维同样。根因相同：Inductor 用 fp32 累加方差（平方偏差 1e-60 下溢），CPU eager 用 double | torch 2.14.0+cpu | **待提交**（分解差分发现，2026-09-09） | `reports_decomp/std_var_large_values/minimal_verified.py`、`minimal_verified_tiny_grad.py` | 开 issue #173793（LayerNorm、CUDA、1e37）同根因；#173989 只修了 LayerNorm CPU 路径 → 新实例 | 建议在 #173793 下评论附复现，或另开 issue 引用它；梯度归零是比 NaN 更隐蔽的后果（训练静默停滞），报告时以它为主 |
| 3 | `F.interpolate(torch.empty(2,3,0), size=3)`（空的空间维）：eager 抛 "Input and output sizes should be greater than 0"，aot_eager 抛 IndexError，**Inductor 返回 (2,3,3) 全 NaN**——生成的内核对 0 元素缓冲区越界读；linear / 2-D bilinear 同样；反向情形（W=1、scale 0.6 → 输出 0）compile 静默返回空张量 | torch 2.14.0+cpu | **待提交**（分解差分，2026-09-09） | `reports_decomp/interpolate_zero_size/minimal_verified.py` | 未见同报告；同族 #195550（adaptive_avg_pool2d 校验）、#144362（compile 缺校验）；#184848/#191850 不同 | 越界读在 CUDA 上可能是 IMA，值得单独报 |
| 4 | `torch.linalg.vector_norm(torch.empty(0,5), ord=inf, dim=-1)`（空 batch，被归约维非空）：eager 返回 shape (0,)，compile（eager/aot_eager/inductor 后端都在 Dynamo 追踪期）抛 "cannot compute the inf norm on the dimension -1 because this dimension is empty"；`dim=1` 正常、`dim=-1`/`(-1,)` 失败；ord=-inf / 负数同样，ord=2/0.5/0 正常 | torch 2.14.0+cpu | **待提交**（分解差分，2026-09-09；根因 2026-09-10 修正） | `reports_decomp/vector_norm_empty_batch/minimal_verified.py`、`issues_0910/04-*.md` | 未见同报告；#148809 改过同一检查但保留了未规范化的索引 | 根因：`_refs/linalg/__init__.py` 121 行 `sym_or(x.numel() != 0, d < len(shape) and d >= 0 and shape[d] != 0)` 对负 `d` 直接为假，没做 `d % ndim` |
| 6 | CPU Inductor `dynamic=True`：`(x + m).argmin()`（x (3,4,4)、m (1,4,4) 广播，内维尺寸相等）生成的 C++ 里 arg-reduction 索引写成 `ks1**2*x0 + x1`（Python 幂运算符），C++ 编译失败；static / aot_eager / 逐维 argmin / amin 正常 | torch 2.14.0+cpu | **待提交**（变形关系配置差分，2026-09-10） | `reports_metamorphic/argmin_pow_dynamic/minimal_verified.py` | 未见同报告；#171870（s390x 向量算子缺失）、#193680（融合 arg-reduction 索引值错）、#186373（关 duck sizing 草稿）都不是 | 根因：`torch/_inductor/codegen/cpp.py` 3749 与 256 行用 `str(index)` 而非 `cexpr_index(index)`；两行修复 |
| 11 | `aoti_compile_and_package` 对返回 `torch.return_types.*` 命名元组的模型（`torch.max(x, dim)`、`topk`、`kthvalue`、`cummax/cummin`、`aminmax`、`frexp`、`geqrf`、`lu_unpack`、`nanmedian`）打包失败：`NotImplementedError: No registered serialization name for <class 'torch.return_types.max'>`；export / torch.compile 正常，拆成普通元组返回即可打包 | torch 2.14.0+cpu | **待提交（低–中）**（export/AOTI 差分，2026-09-10） | `reports_aoti/return_types_packaging/minimal_verified.py` | 未见同报告；相关 open PR #113258（pytree 支持 PyStructSequence，2023 至今未合） | 错误信息指向用户无法操作的内部注册调用 |
| 12 | `F.instance_norm` / `F.batch_norm` 的 running stats 是闭包捕获的普通张量（非 buffer）时：eager/compile 正常并原地更新；`torch.export` 成功且导出模块与 eager 一样原地更新捕获的张量（**2026-09-15 复核修正**：早先"静默丢更新"的说法不成立，lifted constant 与原张量是同一存储，`scripts/diag_export_lifted_mutation.py`）；`ep.run_decompositions()` 给出正确报错"Constant lifted_tensor_0 is mutated…register it as buffer"；`aoti_compile_and_package` 却抛 **INTERNAL ASSERT**（RegisterFunctionalization_2.cpp:8058，"please report a bug"） | torch 2.14.0+cpu | **待提交**（export/AOTI 差分，2026-09-10；OpInfo instance_norm 全部 8 个带 running stats 的样本命中） | `reports_aoti/lifted_constant_mutation/minimal_verified.py` | 未见同报告；#150008（closed）是同一断言类的另一触发（narrow 视图上的 copy_） | 两处不一致：export 时不报、AOTI 报内部断言 |
| 10 | **eager 侧硬崩溃**：`F.pdist(torch.empty(0, d, requires_grad=True)).sum().backward()` 让进程以 0xC0000094（整数除零）退出，无 Python 异常；(1, d)、(2, d) 正常。**Linux 也复现**（Kaggle，torch 2.10.0+cpu，SIGSEGV，2026-09-10） | torch 2.14.0+cpu（Win）/ 2.10.0+cpu（Linux） | **待提交（eager 缺陷）**（反向分解差分，2026-09-10） | `reports_decomp/pdist_empty_backward_crash/minimal_verified.py`（子进程逐例运行） | 未见同报告（4 组关键词） | `pdist_backward` 未对行数为 0 设防；compile 路径调用同一 kernel 同样崩溃 |
| 8 | **Windows CPU + `max_autotune`：`torch.bmm` / 批量 `matmul` 必然失败**——`LoweringException: AttributeError: function 'cpp_CppMicroGemmFP32Vec' not found`；`mm`/`addmm`/`linear` 正常 | torch 2.14.0+cpu（Windows/MSVC） | **待提交**（配置差分 max_autotune，2026-09-10） | `reports_metamorphic/bmm_autotune_windows_export/minimal_verified.py` | 未见同报告 | 根因一行：`cpp_bmm_template.py` 第 44 行 `extern "C"` 漏了 `{{export_declaration}}`（`__declspec(dllexport)`），gemm/grouped_gemm 模板都有；自动调优的基准 DLL 里找不到符号 |
| 9 | `max_autotune` + `torch.addmm(标量 bias, x, y)`：lowering 抛 `IndexError: list index out of range`（`kernel/mm.py` 714 行先取 `get_stride()[0]` 再查秩）；1-d/2-d bias 与默认配置正常 | torch 2.14.0+cpu | **待提交（低）**（配置差分，2026-09-10） | `reports_metamorphic/addmm_scalar_bias_autotune/minimal_verified.py` | 同一行有 open PR #185578（issue #185533，SliceView 触发），其 guard 不覆盖 0-d | 建议在 #185533 下评论请求把秩检查前置 |
| 7 | `F.channel_shuffle` / `native_channel_shuffle` 对 channels_last 输入：eager/aot_eager 输出保持 channels_last，Inductor 输出变成 contiguous；同输入的 group_norm 在 Inductor 下保持 | torch 2.14.0+cpu | **待提交（低）**（meta 步长差分，2026-09-10） | `reports_decomp/channel_shuffle_channels_last/minimal_verified.py` | 无同报告；同族已修：#187195（max_unpool2d）、#179830（upsample_nearest3d）、#179837（pad）、open #192681（batch_norm） | 一句话 issue 引用同族 PR |
| 5 | `F.binary_cross_entropy(p_bf16, t_bf16, weight=w_fp32)`：eager/aot_eager 返回 bfloat16，Inductor 返回 float32（fp16 同）；无 weight 或 weight 同 dtype 时一致。另：target 为 fp32 时 eager 报 dtype 错、Inductor 照算 | torch 2.14.0+cpu | **待提交（低）**（分解差分，2026-09-09） | `reports_decomp/bce_weight_dtype/minimal_verified.py` | #171282（closed 2026-03-19）是 with_logits 版同一症状，2.14 上 with_logits 已一致，plain 版未修 → 同类新实例 | 一句话 issue 引用 #171282 即可 |

## 待人工复核后决定（第七轮：已关闭 issue 在 2.14.0 上仍复现）

| issue | 现象（2.14.0+cpu） | 关闭情况 | 建议 |
|---|---|---|---|
| #177821 `[inductor] torch.compile ignores a complex indexing assignment` | issue 自带脚本原样复现：整数输出精确不等（`out1=[[2,2],[10,10]]` vs `out2=[[4,0],[12,8]]`） | 2026-05-06 关闭为 completed，**无评论、无关联 PR/提交** | 最值得跟进：评论请求 reopen，附 2.14.0 复现 |
| #176679 tensor subclass `__torch_function__` 结果错误 | aot_eager/inductor 抛 `TypeError`，eager 正常 | 2026-03-11 关闭，仅有"能复现，待定优先级"评论 | 复核后评论 reopen |
| #174386 `use_deterministic_algorithms` 下 `empty_like` 未填 NaN | 复现 1/2 次 | 修复提交在 2.14.0 内 | 多次运行确认稳定性后再定 |
| #184405 dtype 切换后返回过期 float32 | 复现 1/2 次 | 修复提交在 2.14.0 内 | 同上 |
| #182131 fp16 cast 前后数值差 3.9e-4 | 2/2 复现，误差小 | 修复提交在 2.14.0 内 | 可能是修复只缩小而未消除差异；对照 PR 判断 |

## 待人工复核（GPU，torch 2.10.0+cu128 on Kaggle T4；需在 GPU 环境用 issue 原脚本确认）

| issue | 现象 | 关闭情况 | 建议 |
|---|---|---|---|
| ~~#122381~~ CUDA attention 潜在越界 | 读脚本后判为 **Invalid**：模型含 `nn.Dropout(0.1)` 且处于 training 模式，eager 与 compile 的随机流本就不同，误差 1.11 是 dropout 随机性，不是缺陷。载入器已改为：含 Dropout 的模块目标自动切到 `eval()` 并在 notes 里标注 | 2024-05-14 关闭 | 不提交 |
| #113641 `compiling double inplace clamp on strides produces wrong result` | 2.10 上 Dynamo 抛 `InternalTorchDynamoError`，eager 正常（五个上下文 2/2）；同一脚本在本机 2.14.0 CPU 上三路一致 | 2023-11-17 关闭为 completed | 大概率是 2.10 特有、后续版本已修的 Dynamo 崩溃；可在 Kaggle 上 `!python kaggle/verify/issue_113641_*.py` 看 2.10 的实际报错，若确为已修则不提交 |

## 不提交的（已查重或判为无效）

| 发现 | 结论 |
|---|---|
| `torch.cov(x, aweights=w)` + `dynamic=True` 追踪失败 | Duplicate：PR #190669 正在修（`reports_opinfo/7972f93b54ff/DEDUP.md`） |
| `soft_margin_loss` fp16 eager `inf` vs Inductor 有限值 | Invalid：eager 溢出，编译结果更接近 fp64 真值 |
| `new_empty` / `new_empty_strided` 值差异 | Invalid：未初始化内存，比较无意义 |
| `aten.max_pool2d_with_indices_backward` + `dynamic=True`：来自函数参数的 int 成为 SymInt，`int[2]` 形参拒绝 SymInt 标量 | Duplicate / 相关 #122129（open，同一错误类）；已验证 `reports_opinfo_all/6468272fac80/minimal_verified.py` |
| `index_add` 到空维：Inductor 生成内核越界断言，eager 正常返回 | Duplicate：PR #186532（2026-08-18 合并，2.14 分支切割后）修的正是"非法索引编译通过后在后端代码里失败"；`reports_decomp/index_add_empty_dim/DEDUP.md` |
| `F.multi_margin_loss` float16 CPU：eager 抛 `NotImplementedError`（Half 无 CPU kernel），aot_eager/inductor 经分解正常返回 | Duplicate / #144362 的实例（open："compile 缺少 eager 有的 dtype 检查"）；`reports_opinfo_s3/11ddea7fd13c/DEDUP.md` |
| `F.hinge_embedding_loss(reduction='sum', margin≈8.4)`：编译误差 1.1e-6 vs eager 1.8e-7 | Invalid：求和重结合的 1 ULP 级差异，PyTorch 自己的 `same(fp64_ref)` 判为可接受；oracle 已加该否决 |
| `index_reduce` 到空维 / size-1 维的越界 index：eager IndexError，Inductor 静默返回或内核断言 | Duplicate 族：#186532（index_add，已合）、#185977（open PR，仅 index_add）；index_reduce 是同一分解族，可在 #185977 下提一句 |
| `F.cross_entropy` / `linear_cross_entropy` bf16/fp16 输入 + fp32 weight：eager 抛 "expected scalar type BFloat16 but found Float"，compile 照算 | #144362 族（compile 缺 eager 的 dtype 检查）实例，不单独报 |
| `torch.native_batch_norm` training、每通道仅 1 个值（输入 (1,1)）：eager 返回，compile 抛 ZeroDivisionError（分解里 1/(N−1)） | 低：`F.batch_norm` 的 Python 包装在 eager 就拒绝该情形，只有直接调 legacy `native_batch_norm` 才触发；#147287 建议移除该算子 |
| `F.multilabel_margin_loss` 输入含 NaN：eager（含 fp64）返回有限值，compile 返回 NaN | 分歧在 eager 端：C++ `std::max(0, nan)` 吞掉 NaN，compile 的 clamp 传播 NaN 更合理；#141017 已修过该分解的其它问题。不报 |
| `F.max_pool2d` 窗口完全落在 padding 内（W=1、dilation 2、ceil_mode）：eager 索引 1/3，compile 索引 −2/1 | 两侧索引都无意义（eager 的 3 也越界），输入非法。不报 |
| `F.nll_loss` 对 channels_last（非连续）4-D 输入的反向：eager/aot_eager 抛 `RuntimeError: grad_input must be contiguous`，Inductor 正常算出梯度（2.14 与 nightly 2.15.0.dev20260911 相同，Windows/Linux 都是；nightly 分解差分 2026-09-12 发现） | eager 侧缺陷，Duplicate：#175084（2026-02-16，closed），修复 PR #175246 合并后因内部回归被 revert、2026-07-24 以 stale 关闭 → 缺陷仍在。可在 #175084 下评论"2.14/nightly 仍复现、compile 反而正常"请求 reopen；不单独开 issue |
| `torch.linalg.eigvals` 输入含 NaN：eager 进程直接被 MKL 中止（"Parameter 3 was incorrect on entry to SGEBAL"） | 非 compile 问题；#93124（2023 关闭）在 2.14.0 Windows 上仍复现，#185247（open）同症状。可作为 closed-but-reproduces 附带提一下 |
| `F.interpolate` 2-D `scale_factor=1.15`（输出尺寸等于输入尺寸）nearest / bilinear 编译结果与 eager 不同（偏差 3.6 / 2.7）；1-D、nearest-exact、bicubic 一致 | Duplicate：open #175154（scale 1.3、nearest、high priority），PR #184848 未合；bilinear 是该 issue 未提到的模式，可在 #175154 下补一句。`scripts/confirm_interp_scale.py` |
| `max_pool2d_with_indices_backward` `kernel_size` 以位置/默认/`**kwargs`/闭包/`tensor.item()` 形式传入且 `dynamic=True`：失败；以字面量/全局/属性/列表/字典传入：通过 | #122129 族（已在上表）；绑定形式扫描给出了精确触发条件，不另报 |
| `masked.argmin` `dynamic=True` C++ 编译失败 | 已归入第 6 条（根因是通用的 arg-reduction 索引打印，masked 只是入口） |
| `F.pixel_unshuffle` 空 batch：eager/aot_eager 返回输入形状 (0,1,6,6)，Inductor 返回正确的 (0,9,2,2) | eager 侧缺陷，Duplicate：open #192816 "Return the documented shape from pixel_unshuffle for a zero-element input" |
| `torch.tensor(1.5).expand(-1, 5, 5)`：eager 返回形状为 **(-1, 5, 5)** 的张量（numel 为负），compile 正确报错 | eager 侧缺陷（0 维张量跳过了"-1 不能用于新增前导维"的检查）；查重未见同报告；非 compile 问题，可单独作为 eager issue 报 |
| 约 40 个算子 int64/bool 输入：eager 抛 NotImplementedError（无整数 kernel），compile 经分解正常返回（softmax/gelu/silu/celu/hardswish/interpolate/max_unpool/grid_sample/round/lerp/…） | #144362 族（open，"compile 缺 eager 的 dtype 检查"）；`results/decomp_full_s1/` 有完整清单，可作为该 issue 的补充列表 |
| `hardswish` / `smooth_l1_loss` / `multi_margin_loss` 输入含 NaN 时**梯度**的 NaN 模式与 eager 不同 | 反向分解在 NaN 传播上与 eager kernel 的 where/mask 写法不同；语义争议大，不报 |
| 真实模型 campaign（63 个 torchvision 程序）唯一候选：FCN 式模型 bf16 autocast 下 `requires_grad` 上下文的梯度差 1.32（`reports_models/61eda009bc8f`） | Invalid：用 fp32 无 autocast 的梯度作真值，eager-bf16 与 compiled-bf16 到真值的距离相同（0.0186 vs 0.0189，`scripts/confirm_model_bf16_grad.py`），互差是 bf16 一个 ulp 量级；autocast 下无法构造 fp64 参考，oracle 退化为 dtype 容差才误报。8 个程序超时（googlenet ×4、swin ×3、resnet18 bf16）未测 |

| 13 | **CUDA + `triton.cudagraphs`：`x.new_zeros((2,3), device='cpu')`（x 在 CUDA）返回 CUDA 张量**；eager 与默认配置的编译都返回 CPU 张量，显式 `device='cpu'` 被 cudagraphs 路径忽略 | torch 2.14.0+cu130（Kaggle T4），2.10.0+cu128 同 | **待提交**（配置差分 cudagraphs，2026-09-11） | `kaggle_out/gpuverify/tcc-gpuverify.log` G1/G1b；`issues_0910/13-*.md` | 见下方查重 | 只在开 cudagraphs 时出现，`mode="reduce-overhead"` 即默认开 |
| 14 | **Dynamo：`random.shuffle` / `random.sample` 在编译函数内每次调用返回同一结果**（eager 每次推进全局 RNG 给出不同结果）；`random.random/randint/uniform/choice/choices/gauss` 正常推进。backend=eager 与 inductor 相同，dynamic=False 即可 | torch 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/randomlinux.txt`） | **待提交（高：静默正确性，数据增广/打乱顺序被冻结）**（6.6 Dynamo 语义差分，2026-09-12） | `reports_dynsem/random_module/minimal_verified.py`、`scripts/diag_random_compile.py` | 相关：#196437（open，`random.Random` 实例 + dynamic=True 第 4 次调用重复上一值，不同入口）；PR #188083（已合并 2026-07-06，把模块级 `random.seed` 接进 RandomVariable，同时提到 shuffle/sample 已走该路径——正是该路径在重复调用时不推进状态）；PR #185971（修 `random.Random` 回放，未合并、已关闭）。未见同报告 | shuffle/sample 很可能在追踪时被求值一次并当常量回放 |
| 15 | **Dynamo：编译函数内的 `random.seed(k)` 在第一次（编译）调用时不生效**：`random.seed(0); random.random()` 第一次返回 0.9655（不同状态），之后各次返回正确的 0.7580；`randint/randrange/uniform` 同样；`gauss/choice/sample/shuffle` 正常 | 同上 | **待提交（中）**（6.6，2026-09-12） | 同上 | PR #188083 正是处理模块级 `random.seed`（已合并），但只修了 "graph break"，首调用语义仍错；可与 14 合并成一个 issue 或分开报 | 第一次调用与后续调用结果不同 = 追踪时与回放时 RNG 状态处理不一致 |
| 16 | **Dynamo `dynamic=True`：f-string 对 SymInt/SymFloat 用格式说明符崩溃**：`f"{x.shape[0]:d}"`、`f"{x.shape[0] / 2:.3f}"` 抛 `InternalTorchDynamoError: ValueError: Unknown format code 'd' for object of type 'str'`；eager 与 dynamic=False 正常；不带格式说明符的 f-string 正常 | 2.14.0+cpu（Win）、nightly（Win）、Linux 2.14.0 与 nightly dev20260912（Py3.11，`kaggle_out/leadslinux.txt`） | **待提交（中：Dynamo 内部错误而非 graph break）**（6.6，2026-09-12） | `scripts/diag_dynamic_fstring_namedtuple.py`、`issues_0910/16-*.md` | 无同报告（`results/dedup_0912.txt`） | 符号值被当成 str 再套格式码 |
| 17 | **Dynamo `dynamic=True`：含张量字段的 namedtuple `==` 触发 `RecursionError: Stack overflow`**；eager 对多元素张量抛 RuntimeError（bool 歧义），对单元素张量返回 True（**静默→崩溃**）；普通 tuple/list 的 `==` 与 dynamic=False 正常 | 2.14.0 与 nightly，**仅 Windows + Python 3.14**；Linux + Python 3.11 不复现（`kaggle_out/leadslinux.txt`） | **待提交（中，标明 Python 3.14）**（6.6，2026-09-12） | 同上 | 无同报告（`results/dedup_0912.txt`） | namedtuple.__eq__ 在符号形状下的追踪无限递归 |
| 18 | **Inductor：`torch.ldexp` 输出 dtype 与 eager 双向不一致**：bool/int × fp16/bf16 → eager fp16/bf16、Inductor **float32**；fp16 × fp32/fp64 → eager fp32/fp64、Inductor **fp16**（bf16 同）；aot_eager 与 eager 一致 → Inductor lowering 的 dtype 推断错 | 2.14.0+cpu（Win）、nightly 2.15.0.dev20260911（Win）、2.14.0+cu130（T4，`kaggle_out/dtypesgpu`）、Linux CPU | **待提交（中）**（6.2 dtype 矩阵，2026-09-12） | `scripts/diag_dtype_leads.py` | 查重中 | 规律：浮点 x → 保持 x.dtype；整型 x → float32，忽略 exponent 的 dtype |
| 19 | **Inductor：`F.huber_loss(fp16, bf16/fp32/fp64)` 返回 float32/float64，eager/aot_eager 返回 float16**（bf16 输入同；`delta`、`reduction="none"` 都如此）；`smooth_l1/mse/l1_loss` 三路一致 → 与第 8 条（BCE weight）同机制 | 同上（Linux CPU 复现；CUDA 因 eager 已提升而一致） | **待提交（低–中，可并入第 8 条）**（6.2） | 同上 | 第 8 条的查重结论适用 | |
| 20 | **Inductor：`torch.lerp(bool_tensor, bool_tensor, 0-d 浮点 weight)` 编译失败** `BackendCompilerFailed: NotImplementedError: Subtraction, the - operator, with two bool tensors is not supported`；eager/aot_eager 返回 weight dtype 的张量；int64 输入正常 | 2.14.0+cpu（Win）、nightly（Win）、T4 CUDA、Linux CPU | **待提交（低）**（6.2） | 同上、`issues_0910/20-*.md` | 无同报告 | lerp 分解里 `end - start` 对 bool 未先提升 |
| 21 | **Inductor：`torch.sum(fp16/bf16, dtype=torch.bool)` C++ 编译失败**（`error C2672: sum_masked_reduce 未找到匹配`，fp32 输入与带 dim 的归约正常）；`torch.prod(x, dtype=torch.bool)` 抛 `AssertionError: expected reduction_type in (min, max, any, sum)`；eager/aot_eager 都返回 bool 标量 | 2.14.0+cpu（Win）、nightly（Win）、Linux CPU（sum） | **待提交（低）**（6.2） | 同上、`issues_0910/21-*.md` | 无同报告 | bool 累加类型没有 sum/prod 归约的 C++ 模板 |
| 22 | **Dynamo：完整追踪的函数内 `str(KeyError(...))` 丢掉 CPython 的引号**：eager `str(KeyError("k")) == "'k'"`（KeyError.__str__ 用 repr），compile（fullgraph，或没有 graph break 时）得到 `'k'`；`f"...{e}"` 同样；`dict[missing]` 触发的 KeyError 同样；有 graph break 时走真实异常对象则正确 | 2.14.0+cpu（Win + Linux）、nightly（Win + Linux，`kaggle_out/leads2linux.txt`） | **待提交（低）**（6.6 第二批，2026-09-12） | `scripts/diag_keyerror_str.py`、`issues_0910/22-*.md` | 无同报告（`results/dedup_0912b.txt`） | Dynamo 的异常变量 `__str__` 走的是 BaseException 的实现 |
| 23 | **Inductor：`torch.compile(opt.step)` 对复数参数 + `foreach=True` 的 SGD 编译失败** `InductorError: AssertionError: <built-in function getitem> is not an OpOverload`；eager/aot_eager 正常更新；`foreach=False` 正常；Adam/AdamW/RMSprop/Adagrad 的 foreach 复数路径正常；momentum/nesterov/weight_decay/maximize/dampening 各变体都失败 | 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux） | **待提交（中：编译崩溃）**（6.4 优化器差分，2026-09-12） | `scripts/diag_optim_complex_foreach.py`、`scripts/optimizer_step_diff.py`（全量在跑 `results/optim`） | 无同报告（`results/dedup_0912c.txt`）；PR #178192（"Handle operator.getitem on IR nodes in GraphLowering"，2026-06-22 stale 关闭、未合并）修的正是这条断言的另一个触发点（多输出算子的 MultiOutputLayout），可在 issue 里引用 | SGD 的 foreach 实现不像 Adam 那样先 `view_as_real`，直接对复数张量调 `_foreach_add_`，Inductor 无该 lowering → fallback 的多输出被 `getitem` 索引时触发 `graph.py` 的断言 |
| 24 | **`torch.linalg.pinv` 与 `torch.linalg.matrix_sqrth` 对复数输入的反向在 torch.compile 下算错**（matrix_sqrth：eager 2.2e-6 vs aot_eager/inductor 9.6e-1，同样 2.14 + nightly）：complex64 (4,3) 输入 `(pinv(a).abs()**2).sum()` 的梯度，eager 与 complex128 真值差 4.9e-7，aot_eager 与 inductor 差 **1.4e-1**（(3,2) 输入差 2.4，(4,4) 差 5.6）；float32 三路一致；`pinv(hermitian=True)`、inv/solve/svd/qr/eigvalsh/lstsq/det/cholesky/matrix_exp 的复数梯度三路一致 → 只有 pinv 的复数反向错，且 aot_eager 就错 = 分解层（CompositeImplicit 拆成 svd 后按分解求导）而非 Inductor | 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/pinvlinux.txt`） | **已提交 2026-09-15：https://github.com/pytorch/pytorch/issues/197084**（原优先级 高）（6.3 复数变体 grad 扫描 + 6.7，2026-09-13） | `scripts/diag_complex_linalg_grad.py`、`scripts/diag_pad_pinv.py`、`results/decomp_newvar_grad/DECOMP_DIFF.md`（linalg.pinv dtype_complex64 行） | 无同报告（`results/dedup_0913.txt`）；open #184101 是另一个复数反向问题（Inductor 步长断言崩溃，aot_eager 正常） | 定位到 AOTAutograd：`aot_function` 不经 Dynamo 就错，backend=eager / torch.func.grad / functionalize 都对；追踪的反向图里 `pinv_backward` 的 `.mH` 变成 `aten._conj`，怀疑 conj 视图在 AOT 追踪里丢了共轭位（不是 svd 分解——前向图里 `linalg_pinv` 仍是单节点）；gradcheck 编译版 False、eager True |
| 25 | **compiled_autograd + `F.interpolate` 反向崩溃**：`mode=bilinear/bicubic/linear/trilinear` 的反向在 `torch._dynamo.compiled_autograd` 下抛 `TorchRuntimeError ... failed while attempting to run meta for aten.upsample_bilinear2d_backward.default: isIntList() INTERNAL ASSERT FAILED`（compiled_autograd 后端 eager/aot_eager/inductor 都一样）；`nearest`、`adaptive_avg_pool2d`、`conv2d` 正常；普通 eager 反向正常 | 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/calinux.txt`） | **待提交（中：INTERNAL ASSERT，请报 bug 类）**（6.4b compiled_autograd 差分，2026-09-13） | `scripts/diag_compiled_autograd_interpolate.py`、`results/compiled_autograd/COMPILED_AUTOGRAD.md`、`issues_0910/25-*.md` | 无同报告（`results/dedup_0913b.txt`） | compiled_autograd 生成的 `UpsampleBilinear2DBackward0` 节点把 `output_size`/`scale_factors` 参数打包成 `[True], False, [...], 2.0, 2.0` 的形式，meta 内核收到的不是 IntList |
| 26 | **Inductor 对 float 张量执行位运算并返回按位模式解释的垃圾值**：`torch.bitwise_not(torch.tensor([0.5, -1., 2., 0.]))` eager 与 aot_eager 抛 `NotImplementedError: "bitwise_not_cpu" not implemented for 'Float'`，Inductor 返回 `[-8.0, 4.0, -2.0, nan]`（float64 同）；`bitwise_and/or/xor` 同样静默返回；bf16 与 `bitwise_left_shift` 则抛 InductorError | 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/bitwiselinux.txt`） | **不提交（2026-09-15 用户决定：不开 issue，也不评论）**；已有 open #144247（6.2b 一元 dtype 扫描，2026-09-13） | `scripts/diag_bitwise_float.py`、`results/unary_dtype/UNARY_DTYPE.md` | **部分重复**：open #144247（2025-01，`bitwise_and/or/xor` 接受 float32，明确说 `bitwise_not` **不**受影响；标签 module: error checking，assignee malfet）；修复 PR #186016（含 bitwise_not 的 dtype 校验）2026-08-18 未合并被清理关闭。新信息：2.14/nightly 仍复现，且 `bitwise_not` 现在也受影响并返回垃圾值（`[-8, 4, -2, nan]`）→ 评论请求 reopen/重推修复 | 与"eager 无内核而 compile 经分解正常算"一族的区别：这里算出的值没有任何数学意义 |
| 27 | **前向模式 AD 经 `torch.compile(backend="aot_eager")` 对 `torch.ldexp` 触发进程崩溃（access violation 0xC0000005）**：`torch.compile(lambda x, t: torch.func.jvp(lambda a: torch.ldexp(a, e), (x,), (t,)), backend="aot_eager")(x, t)` 直接让解释器退出；eager 的 jvp、backend=eager、inductor、以及 aot_eager 的前向/反向都正常 | 2.14.0+cpu（Win 0xC0000005 / Linux SIGSEGV）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/jvplinux.txt`） | **待提交（高：硬崩溃）**（6.4d 前向 AD 差分，2026-09-13；就是这条让全量扫描两次无声退出） | `scripts/diag_jvp_ldexp_crash.py`（子进程逐例，带退出码） | 无同报告（`results/dedup_0913d.txt`） | ldexp 又是第 18 条的主角（原生 lowering）；这里崩在 AOTAutograd 处理 jvp 的 dual tensor 上，inductor 反而没事 |
| 28 | **前向模式 AD 经 torch.compile 触发 `INTERNAL ASSERT FAILED: InferenceMode::is_enabled() && primal.is_inference() && tangent.is_inference()`（`_make_dual`）**：`addr`、`quantile`（任意 q）、`nanquantile` 的 jvp 在新进程里 eager/aot_eager/inductor 三个后端全部断言；eager 的 jvp 正常；median/kthvalue/sort 正常。**2026-09-15 复核修正**：早先 Windows 矩阵里的部分 ok 是同进程内状态残留造成的（`scripts/diag_jvp_make_dual_isolated.py` 逐用例独立进程后全部断言） | 2.14.0+cpu 与 nightly（Win，独立进程）；Linux 2.14.0 与 nightly 相同（`kaggle_out/jvp2linux.txt`） | **待提交（中→高：INTERNAL ASSERT，无后端可绕过）**（6.4d 前向 AD 差分） | `scripts/diag_jvp_make_dual_isolated.py`、`scripts/diag_jvp_make_dual.py`、`results/forward_ad/FORWARD_AD.md`、`0913issues/07-*.md` | 无同报告（`results/dedup_0913d.txt`） | Dynamo 追踪 `torch.func.jvp` 时，这些算子分解出的中间量以 inference 张量进入 `_make_dual`；同进程内先前的调用会改变后续结果（状态依赖） |
| 29 | **Inductor 编译 `torch.nan_to_num(complex_tensor)` 失败**：`BackendCompilerFailed: RuntimeError: Complex dtype is not supported for isneginf, got dtype torch.complex64`（nightly 为 TypeError）；eager 与 aot_eager 正常返回 complex64；输入无 NaN 也一样；float32 正常 | 2.14.0+cpu（Win + Linux）、nightly 2.15.0.dev20260911（Win）/ dev20260912（Linux，`kaggle_out/unarylinux.txt`） | **不提交：Duplicate（2026-09-15 用户决定：不评论）** | `scripts/diag_unary_out_complex.py`、`results/unary_dtype/UNARY_DTYPE.md` | **重复**：open #190275（"[inductor] Compiling torch.nan_to_num on complex128 raises RuntimeError: Complex dtype is not supported for isneginf"）与 closed #147109（complex64 同症状，2025）；可在 #190275 下补一句 complex64 与 2.14/nightly 仍复现 | Inductor 的 nan_to_num 分解对复数输入直接调 `isneginf` |
| 30 | `torch.abs(complex64, out=torch.empty(0, dtype=float64/bfloat16))`：eager 返回 out dtype 的张量，compile（aot_eager/inductor）在 FakeTensor 阶段抛 RuntimeError；`out=float32` 与 `angle(complex, out=float64)` 三路一致 | 同上（Linux 同，`kaggle_out/unarylinux.txt`） | **待提交（低）**（6.2b） | 同上 | 无同报告（`results/dedup_0913e.txt`；#125718 是复数支持的总 issue） | meta 内核对 abs 的 out dtype 校验比 eager 严（要求等于 real dtype） |

**2.14 复核结论（`tcc-gpuverify` v3，torch 2.14.0+cu130，Tesla T4 + Linux CPU，2026-09-11）**：第 3、4、6、10 条在 CUDA 上同样复现；第 5、8、9 条 CPU 专属（CUDA 上 eager 与 Inductor 一致）；第 7 条 Linux CPU 段错误、CUDA 正常；第 2 条 Linux 正常（Windows 专属）；第 1 条 CUDA/Triton 正常、Linux CPU g++ 同样编译失败（跨编译器）；第 11、12 条 Linux 同样复现。2.10 线索 G2（adaptive_avg_pool2d SymInt）、G5（quantile dynamic）、G6（nonzero_static rebind）在 2.14 已修，不报。

**nightly 复核结论（`tcc-nightlygpu`，torch 2.15.0.dev20260911+cu130，git fb8ff0f1，Tesla T4 + Linux CPU，2026-09-12；日志 `kaggle_out/nightlygpu.txt`）**：13 条在 main 上**全部仍在**，现象与 2.14 完全一致（第 1 条 g++ 编译错、第 3/4/6/10 条 CPU+CUDA、第 5/8/9 条 CPU 专属、第 7 条 CPU 段错误 exit -11、第 11/12 条 CPU+CUDA、第 13 条 cudagraphs 返回 CUDA 张量）；第 2 条 Windows 专属，在本机 Windows nightly（`..env_nightly`，2.15.0.dev20260911+cpu，MSVC）上直接复现，且 main 源码 `cpp_bmm_template.py` 仍缺 `{{export_declaration}}`；同一 Windows nightly 上第 1、3–12 条也全部复现（`results
ightly_verify_windows.txt`，第 7 条退出码 0xC0000094）。G3 combinations 在 CUDA 上 eager/inductor 步长 (1,2) vs (1,1) 只是 size-1 维的无意义步长，2.14 同样，不算。各草稿已各加一句 "Still present on nightly"。

## 待判定（Kaggle T4 上 torch 2.10 发现，2.14 已复核，见上）

| 线索 | 现象（2.10.0+cu128，Tesla T4） | 状态 |
|---|---|---|
| G1 `x.new_zeros((2,3), device='cpu')`（x 在 CUDA）开 `triton.cudagraphs` | eager 与默认编译返回 CPU 张量，cudagraphs 返回 **CUDA** 张量（显式 `device='cpu'` 被忽略） | 配置差分 `cudagraphs` 发现；2.14 待验 |
| `torch.quantile` / `nanquantile` 输入含**并列值**时的梯度归属（Linux 专属）：`x=[1,1,1,2,2]`, q=0.5 → eager 把梯度给 index 0，compile（aot_eager/inductor 同）给 index 2；全等输入 keepdim 时 eager 给最后一行、compile 给第 3 行；无并列值时一致。Linux 2.14.0 与 nightly 2.15.0.dev20260911 相同，Windows（MSVC）两版都一致 | nightly 分解差分 2026-09-12 发现（harness 的 prime_dim0 变体平铺放大维度，天然带并列值）。判定：eager 与分解都是 sort+gather，只是并列元素的排序次序不同（libstdc++ 非稳定排序 vs 分解的稳定排序），任一并列元素得到梯度都是合法次梯度；PyTorch 对并列的梯度归属不做承诺 → **不报**，只作为"编译改变并列值梯度归属"的记录（`scripts/diag_quantile_ties.py`，`kaggle_out/nightlyties.txt`） |
| `numpy` 标量在编译函数内变成 0 维 `ndarray`：`type(np.int64(3) * 2).__name__` eager 为 `int64`，编译后为 `ndarray`（`np.float32` 同） | 6.6 发现。torch._numpy 的设计限制（numpy 标量用 0 维数组表示），文档有提及 → 倾向不报，列为已知语义差 |
| `torch.masked_fill(bool_tensor, mask, 1.5+0.5j)`：eager 返回 bool 张量，compile（eager/aot_eager/inductor 后端）在 FakeTensor 阶段抛 RuntimeError；int64 张量 eager 也抛（溢出错误） | 6.2 dtype 矩阵发现。eager 对 bool 过于宽松（复数填进 bool），compile 的 meta 核更严格；算 eager 侧缺陷、低价值 |
| 编译函数内**未被使用**的会报错的算子被 DCE 掉：`torch.full_like(int_tensor, 1.5+0.5j)` 结果不用时，eager 抛 "value cannot be converted"，compile 正常返回 | 6.2 dtype 矩阵的 18 条 raise 型分歧全部是这一机制（`sum/cumsum/softmax(dtype=full_like(...).dtype)`）。Dynamo 不承诺保留无副作用死代码的异常 → 不报 |
| `var/std/var_mean` 对**均值远大于方差**的数据（N(1e5, 1)，N=1e5）：compile 相对误差 3e-3，eager 5e-8（两遍 double）；`group_norm` 同族 2.7e-3 vs 2.7e-4；`tiny_spread`（1 ± 1e-4）误差放大 1000× | `scripts/large_reduce_sweep.py`（6.3，837 例 26 条 flag），其余 flag 是 1e18 溢出 / 1e-30 下溢（= 第 5 条已有）和 fp16 `prod` 累加。**并入第 5 条草稿**作为更贴近实际数据的第三个例子（Welford/两遍 vs 单遍 fp32） |
| `torch.compile(opt.step)` 对 Adam/AdamW `capturable=True` 的 CPU 参数：eager 抛 `AssertionError: If capturable=True, params and state_steps must be on supported devices`，compile 正常跑完（36 个配置组合，foreach 开/关都一样） | 6.4 优化器差分发现。compile 侧的 OptimizerVariable 绕过了 eager 的 Python 断言（Dynamo 编译优化器时本来就按 capturable 语义处理），属"compile 跳过 eager 校验"一族 → 不报 |
| `torch.Tensor` 子类的 `__torch_function__` 在编译函数内被额外调用几十次（`__get__`、`_is_view`、`size`、`stride`、`dim`），eager 只看到 `mul/add/sum/item` 四次；返回值一致 | 6.6 第三批发现。Dynamo 追踪子类时的元数据查询也经过 `__torch_function__`，属追踪机制的可见副作用；文档对子类支持有保留 → 低价值，不报 |
| G6 `torch.nonzero_static(x, size=n)` 同一编译产物先 n=5 再 n=6 | CUDA 上第二次调用抛 `AssertionError: expected size 5==5, stride 1==2 ...`（Linux CPU 正常） | 绑定形式 rebind 发现；2.14 待验 |
| G2 `adaptive_avg_pool2d` 的 output_size 以参数/闭包/`.item()` 传入且 dynamic=True | `cannot determine truth value of Relational: (((2*s16+6)//s16))**2 > 25`，字面量/容器形式正常；CUDA 与 Linux CPU 都如此 | open #185510 族的新触发；本机 2.14 CPU 未复现 → 可能已修 |

**跨平台复核（v2，torch 2.10，Linux CPU + T4）**：第 3（interpolate 越界 NaN）、4（vector_norm）、6（var_mean 空张量）三条在 CUDA 上同样复现；第 5、8、9 在 Linux CPU 复现、CUDA 上 eager 本身与 Inductor 一致（CPU 专属）；第 7 pdist 在 Linux CPU 段错误、CUDA 正常；第 2 bmm 在 Linux 正常（Windows 专属确认）；第 1 argmin 在 CUDA（Triton）正常，Linux CPU 报 g++ 编译错误（细节等 v3）。


## 2026-09-20 第四/五轮新增（提交稿在 `../0920issues/`，README 有提交顺序；全部**未提交**，由用户手工提交）

| # | 发现 | 版本 | 状态 | 提交稿 | 查重 | 备注 |
|---|---|---|---|---|---|---|
| 31 | **Inductor 下 autograd 的"反向需要的张量被原地修改"检查丢失 → 静默错梯度**：`out = compiled(x, w)`（`x @ w`、`bmm`、`einsum`、3 维 `F.linear`、`matmul`、`addmm`、`x[0] * p`、attention 式）之后 `x.add_(1)` 再 `backward()`：eager / backend=eager / aot_eager / aot_eager_decomp_partition 都抛 "modified by an inplace operation"，**Inductor 不抛并返回用改后的 x 算的梯度**（误差 1–4）。`x.t() @ p`、`x * p`、2 维 linear、conv2d、MLP 正常报错 | 2.14.0+cpu 与 nightly（Win）；Linux CPU 2.14.0 + nightly dev20260919；T4 CUDA 2.14.0+cu130（`kaggle_out/savedviewlinux.log`、`savedviewgpu.log`），12 个变体的表四个环境完全相同 | **待提交（高：静默错梯度）**（8.1 第二批 `error_parity_batch2.py` 的 `saved_input_modified_before_backward`） | `0920issues/01-*.md`；`reports_validation/saved_tensor_mutation.py`、`saved_view_variants.py` | 无同报告（4 组关键词）；#113737、#158561 相关但不同 | 根因实测：前向图把输入的**视图**（`permute(primals_1)`）存给反向；aot_eager 下存的张量与 x 共享版本计数器（`x.add_` 后 `_version` 1/1），Inductor 下同存储但版本计数器独立（1/0）。修复建议三选一写在草稿里 |
| 32 | **torch._numpy 值错**：`np.fix(1.5)=2.0`（`round as fix`）、`np.cbrt(-8)=nan`（`pow(x,1/3)`）、`np.clip(int8,-1000,1000)` 全为 -24 / `np.clip(uint8,-2,5)` 全为 5（Python 边界被转成数组 dtype）、`np.sign(nan)=0`、`np.heaviside(nan,h)=0` | 2.14.0 + nightly（Win），backend=eager 即可 | **待提交（中–高）**（§9 跨目标 `xtarget_numpy_diff.py --target dynamo`） | `0920issues/12-*.md`；`reports_xtarget/dynamo_numpy_semantics.py` | 无同报告；同类已被接受：#195978、#194866 | 每条根因一行，修复写在草稿表里 |
| 33 | **torch._numpy dtype/形状**：`np.cumsum/cumprod(int8)` 不提升而溢出、`np.square/reciprocal(int)`→float64（被列在 `_fp_unary`）、`int8/int8`→float32、`np.sum(uint8)`→int64、`np.any(uint8)`→uint8、`np.median/percentile/quantile` 形状 (1,)、uint16/32/64 运算 NotImplementedError | 同上 | **待提交（中）** | `0920issues/19-*.md` | 无同报告 | `numpy_default_float` 默认 float64，不是它造成的 |
| 34 | **#197554 "[PT2] Validation lost" 的评论**：方法（error_inputs 过编译路径，916 例，20 条校验丢失）+ 10 个新成员：`index_add` source 形状不符被 Inductor 广播、`index_copy` 负索引、`bce` 输入越界→nan、`repeat_interleave(output_size=错)`、`softmax(int64)`→全 0、`layer_norm` float64 权重、`one_hot(float)`、`int64.mul_(float)`、`float.add_(1j)` 丢虚部、`take(out=重叠)` | 2.14.0+cpu | **待评论（高：维护者点名要这个扫描；成员表里已有用户的 #197095）** | `0920issues/04-*.md`；`reports_validation/validation_lost_members.py` | 已排除已有成员 #169779、#185885、#197095；#121135 只覆盖 randperm 模式 | 先提交 31，再把编号填进评论末句 |
| 35 | **JAX：`jnp.gcd` / `jnp.lcm` 遇整型最小值永不返回**（eager 与 jit 都是；`(-128, 6)`、`(6, INT64_MIN)` 等；另一参数为 0 时正常） | jax 0.11.2 CPU（Win）；Linux 复核 `tcc-xtargetlinux` | **待提交到 jax-ml/jax（中–高）** | `0920issues/11-*.md`；`reports_xtarget/jax_gcd_intmin_hang.py` | jax 仓库无同报告（#11378、#12728 不同） | 根因：`abs(INT_MIN)` 仍为负，循环末尾的大小交换使状态在 (-2,0)/(0,-2) 间振荡 |
| 36 | **Numba：`np.remainder/np.mod/np.divmod` 在 `MIN_INT % -1` 上杀死进程**（0xC0000095；四种有符号 dtype；`a % b` 正常）；`np.floor_divide(MIN_INT,-1)`=0（NumPy 为 MIN_INT） | numba 0.67.0 / numpy 2.5.3（Win）；Linux 复核同上 | **待提交到 numba/numba（高：硬崩溃）** | `0920issues/06-*.md`；`reports_xtarget/numba_intmin_remainder_crash.py` | numba 仓库无同报告；`np.fmod` 整数错 = #8574 重复、median NaN = #10095、clip NaN = #9995 | 根因：`np_int_srem_impl` 只防除数 0，缺 `(MIN_INT,-1)` 判断（`np_int_sdiv_impl` 有） |

本轮查重后不报的（重复）：`index_select` 负索引 #169779；`index_add` 负索引 #185885；eager `div(trunc)` INT_MIN 崩溃 #138425；eager `lcm` 崩溃 #121343 / `gcd` 负值 #187338。JAX 的 jit-vs-eager 一致性在 268 个程序上除第 35 条外干净（负结果，可写进论文外部效度）。


## 2026-09-21 新增（8.2 整数/IEEE 语义扫描分诊）

全部 Inductor 独有（aot_eager 与 eager 一致），2.14.0 与 nightly 2.15.0.dev20260911 都复现；草稿 `../0920issues/07–11`；**未提交**。

| # | 发现 | 状态 | 提交稿 | 查重 | 根因 |
|---|---|---|---|---|---|
| 37 | int8/uint8 的 `min/max/amin/amax/argmin/argmax` 在 ≥16 个元素时错：全正数据 `min`=0、`argmin`=**-1**；全负 int8 `max`=0、`argmax`=-1；int16+ 正常 | **待评论**（open #191499 只报了 Apple Silicon 的 amin=0；补 x86-64、amax、arg*=-1） | `0920issues/14-*.md`；`reports_intub/int8_minmax_vector_padding.py` | #191499 同根因 | 生成代码 `Vectorized<uint8_t>::loadu(ptr, 16)` 只装 16 个元素，其余 16 个通道补 0 却参与归约 |
| 38 | **浮点 `//`：`1.0 // 0.1` 得 10（eager 与 Python 得 9）**；`0.5//0.1`→5、`6.0//0.2`→30；float32 与 float64；`div(rounding_mode='floor')` 同 | **待提交（中–高）** | `0920issues/09-*.md`；`reports_intub/floor_divide_python_example.py` | 无同报告；相关 open #184408（CUDA trunc 差 1） | `_inductor/lowering.py::_floor_div_floating` = `floor(div_rn(a,b))`；eager 是 fmod 算法 |
| 39 | **CPU 向量化 `remainder`/`%`**：除数 ±inf 得 nan、`1e20 % 3` 得 0、`-0.0 % 1` 丢符号；长度 3 正确、长度 ≥8 错 | **待提交（中–高）** | `0920issues/13-*.md`；`reports_intub/remainder_vector_vs_tail.py` | open #194345 是 CUDA 且称"compiled CPU 正确"（他们用单元素输入）；#186875 是半精度 + 标量 | `codegen/cpp.py::CppVecOverrides.remainder`：`a - floordiv(a,b)*b`；标量路径用 `mod()` |
| 40 | 显式 `x.half().float()` / `x.bfloat16().float()` 往返被消掉（`70000→inf` 也没了） | **待评论**（#185337 已 closed completed，请求 reopen） | `0920issues/20-*.md` | #185337 | Inductor 消除成对 dtype 转换 |
| 41 | `torch.clamp(int8, -1000, 1000)` 在 Inductor 下全为 -24、`clamp(max=300)` 把 127 变 44；2.14 eager 报错、nightly eager 返回原张量 | **待提交（中）** | `0920issues/21-*.md` | 无同报告 | clamp 的 Python 边界被按张量 dtype 绕回 |

| 42 | **Dynamo 丢副作用**：类定义在外层函数里 + 对象在被编译函数内创建 + 方法里改闭包捕获的 list/dict → 修改静默丢失（`fullgraph=True` 无报错）；对象在外面创建 / 换成嵌套函数 / 类与状态在模块级 都正常 | **待提交（中–高）**（7.2–7.6 副作用差分 `side_effect_diff.py`，用例 `context_manager_exit_runs`） | `0920issues/10-*.md`；`reports_sideeffects/closure_mutation_min2.py` | 无同报告（7 组关键词）；#196864/#171590 是同区域的崩溃，已关 | 怀疑：追踪期创建的对象无 source，其方法闭包单元被当成新的本地对象，修改没登记到 SideEffects |

| 43 | **大窗口 max_pool2d/3d（核体积 >25）+ 空间维转置的输入：indices 错、梯度错**，前向值对；`return_indices=False` 梯度同样错；`nn.MaxPool3d(3,2)` 接 `permute` 即触发 | **待提交（高：静默错梯度）**（7.1 布局/别名扫描 `layout_alias_sweep.py`） | `0920issues/13-*.md`；`reports_layout/max_pool_indices_noncontiguous.py`、`max_pool_fallback_strides.py` | 无同报告；同类已修 #193751（argmax/argmin 对 channels_last 错位） | `should_fallback_max_pool_with_indices`（>25）走归约式路径：窗口内 argmax 用的是按输入步长重排后的循环序号（kw*6+kh），`_pool_offsets_to_indices` 按 kh*6+kw 解码。Win/Linux、CPU/CUDA、2.14/nightly 全复现 |
| 44 | **`adaptive_max_pool3d` + channels_last_3d 输入：Inductor 下梯度错**（前向值与 indices 都对） | **待提交（高：静默错梯度；根因在 eager C++ 内核）**（同上扫描） | `0920issues/02-*.md`；`reports_layout/pool_backward_noncontiguous_indices.py`、`compiled_pool_grad_channels_last.py` | 无同报告；属 ☂️ #195547 一类 | `adaptive_max_pool3d_backward`、`fractional_max_pool2d_backward`、`fractional_max_pool3d_backward` 对 grad_output 做了 `.contiguous()` 而对 indices 没做；eager 里 indices 总连续所以撞不到，Inductor 把保存的 indices 排成 channels_last 才触发。2.14 + nightly |

| 45 | **`x / 1.0`、`x * 1.0`、`x ± 0` 作为图输出时 Inductor 返回输入张量本身**（`out is x`），对结果原地更新会改坏输入；中性超参数才触发 | **待提交（中–高）**（10.1 改写前提差分 `rewrite_precondition_diff.py`，用例 `add_zero_is_copy` / `mul_one_is_copy`） | `0920issues/07-*.md`；`reports_rewrites/noop_returns_input_alias.py`、`noop_alias_realistic.py` | 无同报告；相关 #174187/#174938（同 pass，另一前提）、#197489（post_grad 同名 pass） | `joint_graph.remove_no_ops.replace_no_op` 缺"输入↔输出不得新增别名"检查（`post_grad.remove_noop_ops` 有）。2.14 + nightly；Linux/CUDA 复核 `tcc-rewritelinux/gpu` |
| 46 | **MSVC 19.44 误编译：连续两次 `_mm*_abs_epi8/16/32` 被整个消掉（`abs(abs(x)) → x`）**；在 Inductor 上表现为 Windows CPU `x.abs().abs()`（int8/16/32）返回负数 | **待提交到 Visual Studio Developer Community（高：编译器误编译）**（同上工具，用例 `abs_abs`） | `0920issues/05-*.md`；`reports_rewrites/msvc_abs/` | Web 搜索无同报告（站内需人工再搜） | `/Od` 对，`/O1`、`/O2`（含 AVX2/AVX512）错；一次、三次 abs 都对 → 优化器把 pabs 当对合而非幂等。**第一条 PyTorch 之下（C++ 工具链）的缺陷** |

| 47 | **MSVC：向量 `add_p{s,d}(x, 0.0)` 被消掉，-0.0 不再变 +0.0；`/fp:strict` 也一样**（标量正确） | **待提交到 Developer Community（中）**（10.13 `cxx_intrinsic_algebra_diff.py`） | `0920issues/17-*.md`；`reports_rewrites/msvc_addzero/` | Web 无同报告 | 30 个差异函数（3 宽度 × ps/pd × 5 种写法）；`/Od` 对 |
| 48 | **MSVC `/arch:AVX2`：`blendv(x,y,cmp LE)` → `vmaxps(y,x)`，±0 平局选错**；min 形式、pd、256 位同样；`/fp:strict` 也一样 | **待提交到 Developer Community（中）**（同上，词表 v2） | `0920issues/18-*.md`；`reports_rewrites/msvc_blend/` | Web 无同报告 | 2,256 个差异函数全部含 `blendv_p*` + `_CMP_LE_OS`；`<` 形式折叠精确；Inductor 上不表现（已查） |

10.13 C++ 工具链层差分总账（MSVC 19.44，109,754 个组合，5 档选项）：值差异只有三族（abs∘abs 10、add-zero 30、LE-select 2,256），另有 148 个仅 NaN 载荷/符号不同（操作数交换，允许）。整数域除 abs∘abs 外全部一致（饱和加减重结合、移位、pack、洗牌、select 均无差异）。g++ 11.4 / clang++ 14 的同款差分在 Kaggle `tcc-cxxalgebra`（结果见 `kaggle_out/cxxalgebra.log`）。

10.13 补充（2026-09-21 中午）：词表 v3（FMA 族 + AVX-512 掩码选择/掩码算术，共 124,334 个组合）→ 草稿 18 扩大为"带等号谓词的 select 折叠"：`LE_OS`、`GE_OS`、`NLE_US`，128/256 位 `blendv` 与 512 位 `mask_blend`；`NLE_US` 在 SSE 编码的普通 `/O2` 下就错。FMA 与整数掩码算子无差异。**g++ 11.4（Kaggle，98,488 个组合，-O0 对 -O1/2/3）：0 个真实差异**——48 个"值差异"全部是 `x * -1.0` 被化成取负后 NaN 符号位不同、再经按位运算显形，属 IEEE 允许（负结果）。clang++ 14 第一次因 `_mm_permute_pd` 立即数越界编译失败，已修并重推。

10.3 模型手术中期结果：Adam/AdamW 的编译 `step` 与 eager 每步差 ~1e-6（不改超参数也有；偏差修正项在编译路径里用 float32 张量算）→ 精度级，不报；lr/betas/eps/wd 的步间修改都被正确感知。

| 49 | **整数 `div(rounding_mode="trunc")` 除零 → 进程崩溃**（Inductor CPU；floor/remainder/fmod 已有保护） | **待提交（高；评论 #143649）**（10.4 `model_surgery_diff.py`，CRASH 记录） | `0920issues/03-*.md`；`reports_surgery/int_div_zero_crash.py` | #143649 open；PR #178591 open、#186021 未合入 | `CppOverrides.truncdiv` / `CppVecOverrides.truncdiv` 裸 `a / b`。Win `0xC0000094`、Linux SIGFPE；2.14 + nightly 0921 |
| 50 | Inductor 下丢失：inference tensor 原地修改检查、确定性模式 `put_` 报错、`fill_uninitialized_memory` | **待提交（中–低；#197554 第二条评论）**（同上） | `0920issues/22-*.md`；`reports_surgery/safety_mechanisms_lost.py`、`deterministic_mode_parity.py` | 无同报告 | aot_eager 与 eager 一致；CPU + CUDA |
| 51 | **torch.fx codegen：`operator.pow(-2, x)` → `-2 ** x`**，GraphModule 符号错；Dynamo dynamic 下 `(-1) ** x.shape[0]` 在 eager/aot_eager 后端恒为 -1 | **待提交（中–高）**（10.5 `symint_arith_diff.py`） | `0920issues/08-*.md`；`reports_symint/fx_codegen_negative_pow.py`、`negative_base_pow.py` | 无同报告 | `fx/graph.py` magic_methods 模板 + repr；只有 `**` 比一元负号优先级高 |
| 52 | **`SymInt.__round__` 忽略负 ndigits**（`return self`） | **待提交（中）**（同上） | `0920issues/15-*.md` | 无同报告 | 三后端一致地错；2.14 + nightly |
| 53 | **unbacked SymFloat 的 NaN 自比较被 SymPy 折叠**（`v != v` → False；`v - v` → 0） | **待提交（中）**（同上） | `0920issues/16-*.md`；`reports_symint/nan_self_compare.py` | 相关 #158087（不同症状） | 需 `capture_scalar_outputs=True`；`math.isnan` 正确 |

未报（2026-09-21）：2.14.0 的陈旧 alpha（EMA）= 已修 #194976/#195040；Adam 1e-6 漂移；CUDA 确定性模式下若干反向不报错（实现本身确定）；SymInt 次要差异（类型、-0.0、大整数）。gcc 11.4 / clang 14 的内建函数差分：0 个真实差异。

负结果三条（2026-09-21 夜，可直接写进论文的“干净的轴”）：
- **宿主编译器优化档差分 `host_opt_diff.py`**（同一批 Inductor 内核 `/Od` 对 `/O2`，178 个批次 ≈ 94 个一元 + 41 个二元程序 × 10 种 dtype，外加 22×22 个一元复合 × 4 种 dtype）：**只有 `abs(abs(x))`（int8 / int32）逐位不同**，即草稿 16；其余 176 个批次逐位相同。注意灵敏度陷阱：把 33 个程序并进一个编译函数时连 abs(abs) 都测不出来（MSVC 在大函数里不做这个折叠），所以每个编译函数只放 4 个程序。
- **前缀一致性 `prefix_consistency_sweep.py`**（135 个逐点程序 × 10 dtype × 动态/静态长度 × 对齐 × 线程数）：21 个程序有差异，全部是超越函数的向量主体与标量尾部用了不同的数学实现，最大 7.6 ε（gelu float32），典型 ≤ 1 ε；没有对齐或线程数造成的差异 → 不报。
- **符号尺寸 `%` 的除零**：`torch.full((2,), x.shape[0] % (y.shape[0] - 20))` 在 y 长度为 20 时 Inductor 进程崩溃（Python/`backend="eager"` 抛 ZeroDivisionError）→ 已并入草稿 19 作为第二条未保护路径（`reports_symint/symint_mod_zero_crash.py`）。

- **符号形状语义 `symshape_index_diff.py`**（10.5b，112 个程序 × 12 个长度 × 3 后端）：111 个一致——负下标、越界切片、负步长报错、roll/repeat/split/chunk/unfold/view/arange/linspace/tril/topk/池化参数在符号尺寸下都与 eager 相同（负结果）；唯一差异 `F.pad(x, (k, -k))` 裁过头（eager 报错、Inductor 返回全填充值）→ 并入草稿 20 第 4 项（`reports_symshape/negative_pad_beyond_input.py`，2.14 + nightly，查重无同报告）。

- **32 位索引边界（10.7，Kaggle T4，2.14.0+cu130 与 nightly 0921，`reports_symshape/int32_indexing_boundary.py`，日志 `kaggle_out/int32gpu.log`）**：2³¹+2¹⁶+64 个 int8 元素的缓冲区上，存储偏移 / 步长超过 2³¹ 的小视图（逐点、归约、cumsum、flip、sort/topk、as_strided、二维末列）、越过 2³¹ 的 index_select / gather / take / 高级索引、负下标与负切片、超过 2³¹ 个元素的逐点核、`dynamic=True` 先小后大：26 项可比较的全部与 eager 逐位相同（负结果）。其余 6 项是测试自身的问题（`.to(int64)` 要 16 GB 显存 OOM；一项形状写错），不是缺陷。

| 54 | **变化的 Python float 实参被烤进缓存图（#194976 残留）**：norm eps / huber delta / label_smoothing / SDPA scale / cdist p / `interpolate(scale_factor)`（错形状）等 18 个算子 | **待提交（高）**（10.9b `scalar_arg_sequence_diff.py`） | `0920issues/02b-*.md`；`reports_scalar_seq/`（普查、最小复现、缓存 2×2） | 无同报告；相关 #194976（closed）、修复 #195040（已在 nightly）、#194817（open PR，仅 SDPA） | Dynamo 图保留符号表达式，fake-tensor 传播特化并记 ShapeEnv 守卫，AOT 烤入常量；重编译得到同一张 Dynamo 图 → AOTAutogradCache 命中旧产物。2.14.0 + nightly 0921，Win/Linux，CPU/CUDA |

10.9b `scalar_arg_sequence_diff.py` 全量结果（100 个程序，默认缓存，每例全新缓存目录）：**2.14.0**：80 一致、16 个静默错值（ema / sub_alpha / addcmul / lerp / clamp / full_like / masked_fill / where / threshold / normalize eps / layer_norm eps / dist p / int(s*10) / 梯度版 EMA——除 layer_norm eps 外都属已修的 #194976）、4 个编译失败；**nightly 0911**：95 一致、1 个静默错值（layer_norm eps → 草稿 02b）、4 个响亮的编译失败（`x * (s // 0.25) + s % 0.25` BackendCompilerFailed；`x.long() ** (abs(k) % 4)`、`repeat_interleave(max(k, 0))` 在 k=0/-1 时 InductorError；`x * (s*s) + (1-s)**2` 在 s=2.0 时 TorchRuntimeError）——eager 正常而编译报错，非静默，优先级低，未写稿（记录在 `results/scalar_seq/nightly.jsonl`）。

**2026-09-22 上午**：11.2 普查 v2（`reports_scalar_seq/specialized_value_census_v2.py`，56 个程序，nightly 0911）：#197889 同根因再添 7 个静默成员（由 float 派生的 int 实参：cumsum dim、round decimals、linspace steps、one_hot num_classes、adaptive_avg_pool1d、unfold；另 `logit(eps=s/8)`）→ `0920issues/25-*.md`（对自己 #197889 的补充评论，2.14 + nightly 逐字验证，缓存 2×2 已验证）；5 个响亮失败（`view(-1, 2**int(s*2))`、`interpolate(size=int(...))`、`std(correction=int(...))`、`masked_fill(x>0, s>0.5)`、`x.long() ** k`：eager 正常、编译报错，低优先级未写稿）；接受 SymInt 的位置、按值分支的分解（pow / addmm beta=0 / lerp / dropout / clamp）、SymBool、int 序列全部正确（负结果）。11.3 成对实现审计第一项：`CppVecOverrides` 的移位是裸 `<<`/`>>` 而标量路径有越界保护 → 实测 5 种整数 dtype × 负数/超宽移位量，向量与标量、eager 全部一致（负结果，`reports_pairs/vector_shift_out_of_range.py`）。

**2026-09-22 早**：台账 55 已写成 `0920issues/23-*.md`（2.14 + nightly 逐字验证）。当天查重发现台账 50 的第 3 项（确定性模式下 `empty*` 不填充）= 已关闭的 #174386（2026-05-10 closed completed，PR #174718 仍 open），原复现程序在 2.14.0 与 nightly 的 CPU 上**原样失败** → 从 22 号稿拆出，单独写成 `0920issues/24-*.md`（评论 #174386，请求 reopen）；22 号稿改为 3 项并重新验证。14 号稿补记了 open 的修复 PR #191543。

| 55 | **torch.fx 代码生成：形参名 `nan` / `inf` 遮蔽同名浮点常量**（`symbolic_trace` 后 `GraphModule.forward` 静默错值，`Interpreter` 正确；形参名 `torch` 时响亮报错） | **已写稿 `0920issues/23-*.md`（2026-09-22，中–低；2.14 + nightly + Linux 已验证）**；原记：线索已确认（2026-09-21 夜，顺着 #197894 查 FX codegen 时发现） | 待写 `0920issues/23-*.md`；`reports_fx/codegen_name_shadowing.py`、`codegen_param_shadows_inf_nan.py` | 4 组关键词无同报告 | `fx/graph.py`：`inf`/`nan`/`torch`/`device`/… 注册为 custom builtin 当全局用；`_Namespace` 只改节点名，函数签名仍用占位符原名。2.14 + nightly 0911 |
| 56 | **确定性模式 `empty*` 在 Inductor CPU 上仍不填充**（= 已关闭的 #174386，原复现原样失败；PR #174718 未合） | **待评论（中–低）**（2026-09-22，从 22 号稿拆出） | `0920issues/24-*.md`；`reports_surgery/issue_174386_recheck.py` | 即 #174386 本身，故为评论 | 2.14 + nightly，Win + Linux；CUDA 浮点已修、整数仍不填 |
| 57 | **#197889 同根因第二入口：由 float 派生的 int 实参沿用旧值**（cumsum dim、round decimals、linspace steps、one_hot、adaptive_avg_pool1d、unfold） | **待评论（中）**（2026-09-22，11.2 普查 v2：56 程序，nightly 7 静默错 + 5 编译失败） | `0920issues/25-*.md`；`reports_scalar_seq/specialized_value_census_v2.py` | 根因同 #197889，故在自己 issue 下评论 | 关任一缓存即正确；接受 SymInt 的位置、按值分支的分解、SymBool 全部正确（负结果） |
| 58 | **torch.fx：GraphModule 经 pickle / torch.save 往返后原地运算变非原地**（输入不再被修改、dtype 报错消失；`__rpow__(-2)` 回来变 `-2 ** x`）；Dynamo 捕获的 `buf += g` 图受影响 | **待提交（中）**（2026-09-22，11.1 `fx_codegen_vs_interpreter.py`：10,518 图 × Interpreter / forward / deepcopy / pickle，pickle 档 103 张不同） | `0920issues/26-*.md`；`reports_fx/pickle_roundtrip_changes_semantics.py`、`dynamo_graph_pickle.py` | 无同报告（邻近 #185170、#186418 是 pickle 报错） | 根因 `_deserialize_graph_module` 重新符号追踪生成源码；deepcopy 正确；2.14 + nightly；Linux 见 `tcc-fxpicklelinux` |
| 59 | **Inductor Python wrapper 用 `repr()` 打印复数标量实参**：虚部为 inf / nan 的复数常量（`a * complex(1, inf)`、`full_like(a, complex(inf, -inf))`）生成 `(1+infj)` → 运行期 `NameError: name 'infj' is not defined`；C++ wrapper 正确 | **低（响亮报错，不写稿；可作 #188596 的补充评论）**（2026-09-22，11.3 `cpp_wrapper_diff.py` 副产物） | `reports_pairs/complex_scalar_repr_in_wrapper.py` | FX 侧同一问题已由 PR #188596 修（`fx/graph.py::_get_repr` 用 `complex(re, im)`），`_inductor/codegen/wrapper.py::val_to_arg_str` 末尾的 `repr(s)` 未修 | 2.14 + nightly；复数带符号零经 wrapper 往返未见错值（负结果） |
| 60 | **CPU float16 融合链中间量不按 float16 舍入**：`y = x*x; (y, y/x, isinf(y))` 在 x=300（f16）下 eager 得 `(inf, inf, True)`，编译得 `(inf, 300, False)`——同一次调用里返回的 y 是 inf 而 isinf(y) 是 False；`emulate_precision_casts=True` 全对 | **待评论（中–低；#183607 下，`0920issues/27-*.md`）**：补充无显式 cast 的形态、同一调用内 y 与 isinf(y) 自相矛盾、`emulate_precision_casts=True` 在 CPU 与 T4 上都有效（与机器人结论相反）（2026-09-22，11.4 预探） | `reports_pairs/half_intermediates_cpu.py`；Kaggle `tcc-halflinux` / `tcc-halfgpu`：Linux CPU 与 T4、2.14 与 nightly 六组逐字相同 | #183607 open（module: half, correctness (silent)），jansel 问 emulate 开关，机器人复现说开关无效——本机开关有效（CPU） | bfloat16 只有下溢差异（同一机制，幅度小） |
| 61 | **`int()` / `math.trunc` / `math.floor` 作用于 `.item()` 得到的 inf / nan 且结果只进张量运算时**（`torch.full((2,), int(x.item()))`）：eager 抛 OverflowError / ValueError，Inductor（Python 与 C++ wrapper 都）静默得 INT64_MIN；`round(inf)` 得 0；`round(-0.0, 1)` 丢符号；结果进形状则 C++ wrapper 抛 RuntimeError（响亮） | **不单独报（低；已有 open #158087 同根因，团队成员自报、认为 niche；可作补充评论：值进张量时是 INT64_MIN 静默）**（2026-09-22，`symint_arith_diff.py --backends inductor,inductor_cppwrap` 副产物） | `reports_pairs/cppwrap_int_of_nonfinite.py` | #158087 open（module: NaNs and Infs） | 2.14 + nightly；需 `capture_scalar_outputs=True` |
| 62 | **负结果：Python wrapper vs C++ wrapper（`cpp_wrapper=True`，AOTI 路径）**：174 个程序（fallback 内核的特殊浮点/大整数/复数/Optional/int 列表/字符串实参、返回 Python 数、输出别名、输入原地修改、非连续/扩展/0 维/空输入、f16/bf16/uint8）171 个三方一致；138 个符号表达式里 C++ 专属的静默差异只有 int64 回绕（`k**2`、`k*k*k`、`k+1` 在 INT64_MAX、`abs(INT64_MIN)`、`2**64`——Python wrapper 用任意精度 int），响亮的有 `round(a/2)`、返回 Python 数（`Unsupported scalar_to_tensor_handle`）、`int(inf)` 进形状 | **不报（int64 回绕是 SymInt 为 int64 的既定限制；其余响亮）**（2026-09-22，11.3/11.6 `cpp_wrapper_diff.py`、`symint_arith_diff.py --backends inductor,inductor_cppwrap`） | `results/cppwrap/cases_2.14.0.jsonl`、`symint_214.jsonl` | — | 2.14.0；两套 wrapper 生成器在实参打印上没有可报的分歧 |
| 63 | **11.4 OpInfo 全算子 × 边界值 × float16/bfloat16（本机，909 对 (op, dtype)，842 一致、65 不同、2 工具错）**：不同的全部归入已知族——① 融合链中间量不按低精度舍入（= #183607 / 27 号稿：addcdiv、std/var_mean、cosine_similarity 0 vs 1、group_norm、batch_norm、conv2d bf16、softmax_backward、mvlgamma bf16 在极点、grid_sampler、cross）；② 带符号零 14 个算子（= #188680）；③ 已提交项的再发现（自己的 #197108 var_mean 空张量；BCE 校验丢失 = #193757，04 号评论已列）；④ max_pool 多个 NaN 并列时返回哪个 NaN 的下标（未定义，不报） | **负结果（无新可报项）**（2026-09-22，`scripts/opinfo_edge_sweep.py`） | `results/opinfo_edge/lowfp_*.jsonl`；`reports_pairs/var_mean_empty_welford.py`（回归检查） | — | int8/uint8/int16/bool 与 float32/int32 组在 Kaggle（`tcc-edgeint`、`tcc-edgefp32`，进程会被整数除零杀死，已做成可续跑）运行中 |
| 64 | **11.4 float32/int32 组（Kaggle Linux gcc，895 对）与 11.6 Numba parallel / fastmath、ONNX Runtime 优化级别差分**：float32 组的不同项与 f16/bf16 组是同一批算子，说明不是精度转换而是 inf/NaN 边界上的算法/结合律差异（`addcdiv` eager `v*(t1/t2)`=inf 而 Inductor `(v*t1)/t2`=FLT_MAX；std/var 的 Welford vs 两步；batch/group_norm；Linux 专属的 `cross` 差异 = gcc FMA 收缩）；Numba fastmath 的差异全在其文档契约内（假定无 NaN/inf、忽略符号零）；Numba parallel 只在越界移位（本就未定义）上给出未初始化内存值、越界切片报 AssertionError 而非 ValueError（都响亮或 UB）；ORT `ORT_DISABLE_ALL` vs `ORT_ENABLE_ALL` 154 程序两级一致，只有 ORT 算子本身与 PyTorch 的 NaN 语义不同 | **负结果，不报**（2026-09-22） | `kaggle_out/edgefp32/cases.jsonl`；`results/xtarget/numba_parallel.jsonl`、`numba_fastmath.jsonl`；`results/ort/cases.jsonl`；`reports_xtarget/numba_parallel_vs_njit.py` | — | int8/uint8/int16/bool 组仍在 Kaggle 跑（`tcc-edgeint` v3） |
| 65 | **Inductor `torch.any(uint8)` 返回 bool**（eager、aot_eager、`torch.all` 保留 uint8；`any(x)*200+100` eager 44 vs 编译 300） | **待提交（中）**（2026-09-22，11.4 `opinfo_edge_sweep.py` int 组，Linux 1455 对里唯一的 dtype 差异） | `0920issues/28-*.md`；`reports_pairs/cuda_scan_and_any_u8.py` | 无同报告 | 根因 `reduce_any` lowering 绕过 `_refs.any` 的 uint8 恢复；`aten.all` 无 lowering 故正确 |
| 66 | **Numba `parallel=True` 丢失自身 lowering 抛出的 ZeroDivisionError**（`a[i] ** -1`、`7 // int(a[i])`、`1.0 / a[i]` 在 prange / 整数组运算里静默得 0 / inf / 未初始化内存；显式 `raise` 能传出） | **不报（= numba #6976，2021 起 open，标 discussion）**（2026-09-22，`xtarget_numpy_diff.py --target numba_parallel`） | `reports_xtarget/numba_parallel_lost_zero_division.py`；Linux 复核 `tcc-numbaparlinux`：`//`、`%`、`/` 同样丢失，`**` 负指数的垃圾值只在 Windows 上（Linux 正常抛出） | numba #6976；`a + a[:, :1]` 在 parallel 下 AssertionError = #3729 | 可作 #6976 的补充评论（0.67 仍在，`**` 负指数一样） |
| 67 | **T4（Triton）专属线索**：`cumprod` 在含 0 / inf / nan 的输入上 eager 与编译的 NaN 模式不同（`-0.0` vs nan、nan vs 0.0）；uint8 `div`/`floor_divide` 除零 eager 255 vs 编译 0（UB） | **不报**：`cumprod` 差异 = 并行扫描的结合律（`1e20·1e20·0`：顺序 eager 得 `inf·0 = nan`，树形扫描得 `1e20·(1e20·0) = 0`），与归约求和顺序同类；uint8 除零是 UB（2026-09-22，`opinfo_edge_sweep.py --device cuda`，T4 1199 对 ok / 43 不同，其余与 CPU 同族） | `kaggle_out/edgegpu/cases.jsonl`；`reports_pairs/cuda_scan_and_any_u8.py`、`kaggle_out/scangpu/log.txt` | — | CPU 上 cumprod 全部一致；28 号稿的 any(uint8) 在 T4 上同样复现 |
| 68 | **"dtype 未实现"校验丢失一族**：`round(int, decimals)`（Inductor 还返回 float32：`decomposition.py` 的 `aten.round(x*10**k)*(1.0/10**k)` 提升了 dtype）、`ceil/floor/trunc/round(bool)`、`glu(int8)`（float32）、`adaptive_max_pool2d(int8)` | **待评论（中–低；#197554 第三条，`0920issues/29-*.md`）**（2026-09-22，12.1 `lowering_vs_decomp_special_cases.py`：106 例） | `reports_pairs/dtype_not_implemented_lost.py` | `bool - 1` = #195674、upsample int = #193811（不列） | 2.14 + nightly；aot_eager 与 eager 一致 |
| 69 | **`F.pad(..., value=)` 越界填充值回绕**（uint8 -1 → 255、int8 300 → 44；eager 抛 overflow） | **待评论（低–中；自己的 #198071，`0920issues/30-*.md`）**（2026-09-22，同上） | 同上 | 无同报告 | 与 21 同机制、新入口 |
| 70 | **12.1 负结果**：106 个"专门 lowering + 含特判的分解"用例里，其余差异只是异常类型（RuntimeError vs TorchRuntimeError/InductorError）、`atan2(int8,uint8)` 1e-7 级、`native_dropout(p=1)` 的 -0.0 | 不报 | `results/pairs/lowering_vs_decomp_214.txt` | — | 静态枚举：1726 个有 lowering 的重载里 239 个的分解含 dtype/值特判，多为普通类型提升 |
| 71 | **复数 `linalg.pinv` / `polar` / `matrix_sqrth` 在任何 TorchDispatchMode 下（因此 torch.compile 下）前向值错**：内核内部 `.mH()` 惰性共轭在 Python 分发键处于 TLS include 集时未被物化（pinv = `vh.mT @ S⁻¹ @ u.mH`）；逐算子写同一公式正确；排除 Conjugate 键可复现；**#197084（自己的第一轮 issue）的梯度错由此而来**（转发 mode 下 eager 梯度与 compile 梯度逐位相同） | **待提交（高；`0920issues/31-*.md` 新 issue + `32-*.md` 更正评论）**（2026-09-22，`opinfo_edge_sweep.py` complex64 组的 `linalg.polar` 一行 → 逐层定位） | `reports_pairs/conj_lost_under_dispatch_mode.py`、`python_key_conj_composites.py`、`scripts/dispatch_mode_complex_sweep.py` | 无同报告 | 2.14 + nightly，Win + Linux；T4：polar 错、pinv 对；51 个复数 linalg 组合内核里只有 pinv/pinverse/polar（+ 非 OpInfo 的 matrix_sqrth） |
| 72 | **Inductor avg_pool 反向 `ceil_mode=True` 除数错**：`lowering.py::avg_pool2d_backward` 在 `count_include_pad` 时 `scale = kH*kW`，eager 用 `min(hstart+k, H+pad) - hstart`；只在越界窗口不同；前向正确 | **待提交（高；`0920issues/33-*.md`）**（2026-09-22，12.2 `opinfo_edge_sweep.py --grad` float32 454 对里的 `avg_pool1d` 样本 2） | `reports_pairs/avg_pool_ceil_mode_backward.py`；`kaggle_out/avgpoolgpu/log.txt` | 无同报告 | 2.14 + nightly，CPU + T4；其余反向差异：NaN 输入下 hardtanh/hardshrink/hardswish 梯度约定（eager 0/nan vs 编译 1）、`logit` 在 -0.0 的 ±inf——低，不报 |
| 73 | **12.2 反向扫描收尾**（float32，508 对，467 一致）：除 33 号稿外，其余差异 = NaN/inf 输入下的梯度约定（hardtanh/hardshrink/hardswish 在 NaN、`logit` 在 -0.0、`prod` 含 inf 时 eager nan vs 编译 6.0）与已知前向族 | 负结果，不报 | `results/opinfo_edge/grad_f32_*.jsonl` | — | `grid_sampler_2d` 反向在 Windows 上让进程静默退出一次（已作 CRASH 记录，未深究） |
| 74 | **负结果：eager vs 转发 TorchDispatchMode 全算子差分**（`scripts/dispatch_mode_sweep.py`，OpInfo × float32/float64/complex64/int64/bool/f16/bf16，2529 对，纯 eager 几分钟）：除 31 号稿的复数 pinv/polar 族外无差异 | 不报 | `results/pairs/dispatch_mode_all.jsonl` | — | 这个差分等价于"compile 运行时上下文 vs eager"，成本极低，值得放进检测器 |
| 75 | **Inductor CPU `erfinv` 近 ±1 精度差**（f32 7.5e-3 / f64 3.6e-3 MSVC；7.8e-6 / 6.2e-5 gcc；eager 精确；CUDA 正确） | **待提交（中；`0920issues/34-*.md`）**（2026-09-22，`large_arg_special_functions.py`：36 特殊函数 × 大参数/近奇点 × n=3/64） | `reports_pairs/vector_acosh_erfinv.py`、`kaggle_out/sleeflinux`、`erfinvgpu` | 无同报告 | 根因 `calc_erfinv` 两步牛顿相消；标量与向量路径同源 |
| 76 | **Inductor CPU 向量 float64 `acosh` 溢出**（x > √DBL_MAX；SLEEF）| **待提交（中–低；`0920issues/35-*.md`）**（同上） | 同上 | float32 版 = #183768/#152299，float64 无报告 | 标量尾部正确 → 结果依赖张量长度 |
| 77 | **"eager 内核未实现该 dtype、Inductor 照算"一族的穷举**：`opinfo_edge_sweep.py --unsupported`（OpInfo 全算子 × 各自不支持的 dtype，Linux 1,345 对）→ 134 算子 / 418 对静默；按机制分 5 组（int→float-only 53、bool 62、float 位运算 6、half 特殊函数 33、complex 17） | **待提交（中；`0920issues/36-*.md`）**（2026-09-22） | `kaggle_out/edgeunsup/cases.jsonl`、`results/pairs/unsupported_family_list.md`、`reports_pairs/unsupported_dtype_family.py` | 单算子已有 #195674/#195673/#193811/#144247/#193757，无汇总；伞形 #197554 | Windows 41 例复核 33 静默、8 响亮（如 `var(int64)` Welford<int64> C++ 编译失败） |
| 78 | **负结果：`dynamic=True` 全算子边界值扫描**（Linux，float32/int64/bool，1,203 对）：与静态扫描相比只多 2 行 inf/NaN 输入的 batch_norm 族 | 不报 | `kaggle_out/edgedyn/cases.jsonl` | — | 符号形状不改变 lowering 在边界值上的行为 |
| 79 | **负结果：`torch.export` 第三条部署路径**（`scripts/export_edge_diff.py`：OpInfo × 边界值 × float32/int64/bool，1,207 对，`ep.module()` 与 `run_decompositions().module()` vs eager）：`ep.module()` 除 `pca_lowrank` 的 SVD 符号歧义外与 eager 全同；`run_decompositions()` 的 8 处差异全是与 Inductor 共用的分解在 inf/NaN 输入上的已知族（SDPA、std/var、multilabel_margin_loss）与 BCE 校验丢失（#193757） | 不报 | `results/export_edge/cases_{0,1}.jsonl` | — | export 的追踪/functionalization 本身干净；差异只来自分解 |
| 80 | **边界值表补近奇点值**：`1±eps`、`√max·1.01`、`π/2`、`0.5±eps`，以及 **1 以下最大浮点数**（`nextafter(1, 0)`；`1 − eps` 对 erfinv 不够近，误差只有 6.8e-6 < 1e-4 容差）——补后 `erfinv` f32/f64 被扫描命中（34 号稿的教训：手写探针 n=3 只测标量尾部、值不够极端） | 工具改进 | `scripts/opinfo_edge_sweep.py::float_edges`；Windows f32/f64 重跑 `results/opinfo_edge/sing_f_*`：无新族；Kaggle `tcc-edgesing` v2（Linux f32/f64，925 对）跑完：只多 `grid_sampler_2d` 的 NaN 行（已知族）；`erfinv` 在 glibc 下误差 7.8e-6 低于 1e-4 容差故未标出（与 #198133 的 Linux 数字一致）——**负结果** | — | 每次扫描后把漏掉的缺陷的触发值回填进边界表 |
| 81 | **#198155 一族在 CUDA 上**：T4 1,303 对里 383 静默（310 与 CPU 同、73 CUDA 专属：整数 matmul 族、整数 max_pool、batch_norm 整数/复数）；96 对 CPU 专属 | **待评论（低–中；`0920issues/37-*.md`，自己的 #198155 下）**（2026-09-22，`opinfo_edge_sweep.py --unsupported --device cuda`） | `kaggle_out/edgeunsupgpu/cases.jsonl` | — | 说明通用修法需按设备查支持表（OpInfo `dtypesIfCUDA`） |
| 82 | **#198118 机制扩到反向与非 compile 场景**：`scripts/dispatch_mode_grad_sweep.py`（955 个可求导 (算子, dtype) 对，eager vs 转发 mode 下的梯度）→ 复数 `lstsq` 反向（C++ `linalg_lstsq_backward` 内部 pinv/.mH）梯度差 173、pinv/matrix_sqrth 梯度（已知）；`FlopCounterMode` 即可触发 | **待评论（中；`0920issues/38-*.md`）**（2026-09-22 16:20） | `reports_pairs/lstsq_complex_grad_under_mode.py` | 无他人报告 | torch.compile 对 lstsq 反而不受影响（AOTAutograd 逐算子追踪导数公式） |
| 83 | **7.4 自定义算子 / autograd.Function 经编译**（`scripts/custom_op_diff.py`，23 程序 × 3 后端）：唯一静默差异 = 在 `torch.library.custom_op` 内部调用复数 `torch.linalg.pinv`，compile 下值错（#198118 机制：自定义算子在运行时 mode 下执行）；无 fake 的 custom_op → Dynamo 报错（文档行为）；fake dtype 写错 → Inductor 检出报错（正确）；autograd.Function 各变体全部一致 | 负结果（pinv 一例可作 #198118 补充） | `results/custom_op/cases.jsonl` | — | 用户自己的 `.mH()` 公式在 custom_op 里是对的，只有 ATen 组合内核受影响 |
| 84 | **8.5 高阶算子（HOP）经编译**（`scripts/hop_consistency_diff.py`，48 程序 × eager / aot_eager / inductor：`cond`、`while_loop`、`scan`、`associative_scan`、`checkpoint`、嵌套与带梯度变体；1 个自写的 int64 计数器程序在 eager 下不终止，已删）：47 条记录，**没有静默差异**——所有不一致都是响的：`cond` 谓词为 NaN 时 Inductor 报 `ValueError: cannot convert float NaN to integer`（eager 按 Python 真值取 True 分支）、`associative_scan` 逐点 combine 在 CPU 报不支持、`checkpoint` 在 aot_eager 下报 rematerialization 错 | 负结果（HOP 路径成熟；NaN 谓词报错而非静默，不报） | `results/hop/cases.jsonl`、`hop_run2.log` | — | 上层 `torch.cond` 语义本就要求布尔谓词；不算缺陷 |
| 85 | **8.3 一元数学/特殊函数 vs mpmath 30 位参考**（`scripts/special_vs_mpmath.py`，44 函数 × 近奇点/大参数 × n=64 × f32/f64；判据：编译相对误差 > 1e-6 且 > 100× eager 误差，或 NaN/inf 模式不同）：命中 6 个 (函数, dtype)——`erfinv` f32/f64（#198133 已报）、`acosh` f64（#198134 已报）、**`erf` f32 44/64 点**（新）、**`atanh` f32/f64 各 2 点**（新，|x| < eps/2 → 0）；其余 40 个函数（exp/log/三角/双曲/贝塞尔/lgamma/digamma/ndtr/sigmoid 等）在大参数、近 0、近溢出处编译与 eager 同精度 | **→ 39 号稿，已提交 #198183（2026-09-22 18:56）**（erf + atanh 合一稿；Linux `tcc-erflinux` 同错且 n=3 也错——gcc 尾循环带掩码向量化） | `results/special_mpmath.jsonl`、`kaggle_out/erflinux/` | `0920issues/39-*.md` | 8.3 与 34/35 的 `large_arg_special_functions.py` 互补：那边只比 eager，这边比高精度参考，所以能把 eager 自己也不准的点（都没有）与编译独有的损失分开 |
| 86 | **8.8 编译组合律**（`scripts/compile_composition_diff.py`，14 程序 × 17 种函数组合 / 8 种模块组合 = 232 对：`compile(compile(f))`、编译函数调编译函数、`disable` / `allow_in_graph` / graph_break 包裹、`compile(mod)` / `mod.compile()` / `compile(mod.forward)`、同函数先 aot_eager / eager 后端再 inductor、`reset` 之间、先 no_grad / inference_mode / autocast 再正常调用、dynamic 真假、reduce-overhead / max-autotune-no-cudagraphs；有状态程序用相同的预跑序列生成 eager 参照）：211 OK、21 DIFF，**全部可解释**——`fullgraph=True` 对数据依赖分支 / 显式 graph_break 报 Unsupported（响）；`allow_in_graph` 包裹有副作用的闭包计数器（文档要求纯函数）与含参数模块（报 TorchRuntimeError，响）；`torch.where(c, a, b.half()).float()` 各模式一致差 1.4e-4 = Inductor 不模拟中间 half 舍入（`emulate_precision_casts` 默认关，已知设计） | 负结果（组合律成立） | `results/compile_composition.jsonl`、`.log` | — | 与 7.2/7.3/8.9 的 `side_effect_diff.py`（76 例）互补：那边换程序、这边换包裹方式 |
| 87 | **8.10 CI 跳过 / 容差清单挖掘**（main 分支 `test/inductor/test_torchinductor_opinfo.py` 的 `inductor_skips` / `inductor_expected_failures_single_sample` / `inductor_override_kwargs` / `inductor_one_sample`，`test/test_decomp.py` 的 `CROSS_REF_EXCLUDE_SET` / 容差表；只读 `gh api` 取文件）：逐条复现无链接 issue 的 CPU 条目——`polygamma_n_0..4` f32 atol 1e-3（本机编译与 eager 逐位相同、对 mpmath 相对误差 ≤ 1.6e-6）、`interpolate.bicubic` f32 atol 5e-3（与 eager 逐位相同，对 f64 参照 ≤ 1.7e-6）、`masked.logsumexp` "No idea what's going on"（`logsumexp(±inf/nan 标量, [])` 全一致）、`_upsample_bilinear2d_aa` xfail（f32/f64 四种尺寸逐位相同）、`histc` / `complex` / `polar` f16 xfail（dtype 与数值一致）：**Windows 2.14 上全部复现不出**（多数走 fallback 或容差是为 Linux/CUDA 老版本留的） | 负结果（清单里的 CPU 条目已是陈旧容差） | `$TEMP/ci_probe.py`（临时） | — | CUDA 的 `inductor_override_kwargs["cuda"]`（≈120 条）未查，若以后有 GPU 配额可作 Kaggle 任务 |
| 88 | **8.3b 二元函数与归约 vs mpmath**（`scripts/binary_vs_mpmath.py`，23 个二元函数 × 参数网格（每函数 100～250 对）+ 18 种归约 × 22 种极端向量，f32/f64，n=64 向量路径；判据同 8.3）：9 个 (函数, dtype) 命中，**全部是已报告的族**——`remainder` f32/f64（大商得 0、近整数商差一个 ulp：自己的 #198057 + 他人 CUDA 版 #194345）、`floor_divide` f32/f64（`1.0 // 0.1` 得 10：自己的 #197895）、`var` / `std` f32（|x−mean| ≳ 1.8e19 时编译得 inf/NaN、eager 有限：**#197089**，2026-09-15 已报）、`var`/`std` 大而相同的输入 eager 自己 inf（sum 溢出）而编译得对（0）——eager 侧缺陷、方向相反、不报；`prod` f32 `[1e-10, 1e10]×32` eager NaN（向量 lane 各自下/上溢后 0×inf）vs 编译 inf vs 真值 1——归约顺序未定义、两边都错、不报。其余 21 个二元函数（pow/atan2/hypot/xlogy/xlog1py/logaddexp(2)/fmod/copysign/ldexp/zeta/polygamma/igamma(c)/lerp/div/sub/mul/fmax/maximum/nextafter）与 16 种归约（sum/mean/logsumexp/norm 1,2,3,∞/cumsum/cumprod/logcumsumexp/softmax/log_softmax/dot/amax/dist）无差 | 负结果（新缺陷 0；方法把三条已报缺陷全部重新找出，作为检测器验证） | `results/binary_mpmath.jsonl`、`.log` | — | 记 NEXT_DIRECTIONS §12.5：一元/二元/归约三类对高精度参照的扫描到此完成 |
| 89 | **7.6 逐语句插 graph break 作为蜕变关系**（`scripts/graph_break_insertion_diff.py`：对 Dynamo 语义语料 185 程序做 AST 改写，变体 top = 函数顶层每条语句后插 `torch._dynamo.graph_break()`（477 处）、deep = 连 for/if/with/try 体内也插；backend=eager；与同进程未改写基线比）：基线 19 处分歧；top 变体 25 处——**新增 8**：`gen_send` / `gen_throw` 编译后 AttributeError（`'tuple_iterator' object has no attribute 'send'/'throw'`）、`gen_return_value_stopiteration` 返回值 42 → None（静默）、`collections_types` 键序 `['a','z']` → `['z','a']`（静默）；改变 4（`tensor_subclass_torch_function` / `threading_local_state` 的 state 日志，基线已分歧）；消失 2（`python_random`）。最小化后两条都**不需要 graph break**：(a) `OrderedDict.move_to_end` 在入参 / 对象属性字典上不回放（需两个 break 才在语料里露出来，是因为第一个 break 把在帧内创建的字典变成了下一帧的入参）；(b) 生成器只要从编译函数返回就被重建成 tuple_iterator。deep 变体只多插 6 处、无新分歧 | **→ 40 号稿（高，已提交 #198189，2026-09-22）、41 号稿（中，已提交 #198190，2026-09-22）** | `results/graph_break_insertion/eager/report.json`、`results/graph_break_insertion_eager.log`；`$TEMP/gb_min.py`、`od_input.py`、`od_variants.py`、`od_bisect.py` | `0920issues/40-*.md`、`41-*.md` | 教训：语料 + 蜕变关系比手写程序高产——同一语料 6.6 跑了 3 轮只出 19 处已知分歧，插 graph break 一轮出 2 条新缺陷；Kaggle `tcc-dynsemlinux` 在 Linux/3.12 上重跑同一脚本（含 2.14 / nightly / aot_eager）作平台复核 |
| 90 | **12.6 重建（reconstruct）路径差分**（`scripts/reconstruct_diff.py`：编译函数（backend=eager）返回 79 种 Python 对象——迭代器族、itertools 族、functools.partial、闭包、namedtuple、collections 各容器、异常实例（含 `__cause__`）、切片、类型对象、大整数、weakref、上下文管理器、帧内定义的类实例 / dataclass 等；比 `type()`、剩余元素、`StopIteration.value`、send/throw/close、属性、可调用性、身份）：36/79 有差异，其中 28 处只是 `repr` 里的地址（噪声）；**实质差异 8 处，全是同一族**——有张量运算的迭代器被重建成 `tuple_iterator`：生成器表达式、`enumerate`、`reversed`、`itertools.islice` / `tee`、部分消费的 `list_iterator`、`nn.Module.children()`、部分消费的生成器（后者丢 send/throw/close/返回值）；触发条件（`$TEMP/gen_kinds.py`）：体内有张量运算才会被提前耗尽，`yield x` / `yield 1` 的生成器重建成真生成器。其余 43 种对象（partial、闭包默认值、namedtuple、异常链、defaultdict/Counter/OrderedDict/deque/ChainMap、切片、2^70、weakref、no_grad 对象、帧内类实例、dataclass、super 对象、memoryview、Random 实例……）与 eager 一致 | 并入 **41 号稿**（作为族清单与触发条件；41 已提交 #198190；该段作为 41b 评论已由用户贴出：https://github.com/pytorch/pytorch/issues/198190#issuecomment-5776320367） | `results/reconstruct/cases.jsonl`、`results/reconstruct_diff.log` | `0920issues/41-*.md` | 重建路径整体比预期健壮；只有"惰性 + 需要追踪"的对象一类问题 |
| 91 | **12.6 同一语料的另三种蜕变关系**（`graph_break_insertion_diff.py --relation …`，各 471 处插入，backend=eager）：`state_marker`（每条语句后 `STATE['log'].append('m')`——纯 Python 副作用，无 graph break）：**0 新分歧**，副作用顺序与次数与 eager 完全一致；`disable_call`（每条语句后调用一个 `torch._dynamo.disable` 包裹的空函数）与 `print_marker`（每条语句后 `print`，比较 stdout 行数）：新分歧与 graph_break 关系**完全相同的 8 处**（即 40/41 两条缺陷），print 次数两侧相等——graph break 的恢复重放本身没有重复 / 漏掉副作用（8.9 的核心问题为负） | 负结果（无新缺陷；40/41 在三种触发方式下都复现） | `results/graph_break_insertion/{state_marker,disable_call,print_marker}_eager/report.json` 与对应 `.log` | — | 发现 deep 变体原实现没有真正递归插入（只插了 6 处）；已修并重跑（`results/graph_break_insertion_eager_deep2.log`：565 处插入，含 for/if/with/try 体内）——分歧与 top 变体完全相同，块内 graph break 没有额外缺陷 |
| 92 | **8.7 平台 / Python 版本 / torch 版本差分**（Kaggle `tcc-dynsemlinux`：Linux x86-64、Python 3.12、torch 2.14.0 与 nightly 2.15.0.dev20260922，`run.py dynsem` static / dynamic / aot_eager + `graph_break_insertion_diff.py`；本机 Windows、Python 3.14、2.14；比较脚本 `scripts/dynsem_platform_compare.py`）：static 三次运行的分歧集合**完全相同**（19 / 19 / 17：nightly 修掉了 `python_random`——Python `random` 在编译函数内的取值——两处）；dynamic：Windows/3.14 独有 `dataclass_namedtuple`（eager RuntimeError vs 编译 RecursionError）——最小化为 `NT(x, x+1) == NT(x, x+1)`（namedtuple 含张量，dynamic=True，Python 3.14），**就是自己 09-15 已报的 #197102**（8.7 的版本差分把它重新找了出来；Linux 3.12 上不复现与该 issue 的标题一致）；aot_eager Linux 20 / 18 同一模式。graph break 插入在 Linux 2.14 与 nightly 0922 上**新增的 8 处与 Windows 逐条相同**（→ 40/41 的 Linux 复核） | 负结果（无平台 / 版本独有的静默缺陷；作为 40/41 的跨平台证据） | `kaggle_out/dynsemlinux/plan/results/dynsem_linux_*`、`graph_break_insertion_{214,nightly}/eager/report.json` | — | 教训：Dynamo 的字节码翻译层在 3.12 与 3.14 之间没有可观察差异，8.7 的"版本差分"预期落空；平台差分对 Inductor（C++ 代码生成）才有产出（39 号的 gcc 尾循环即是） |
| 93 | **graph break 插入 × aot_eager 后端**（`graph_break_insertion_diff.py --backend aot_eager`）：基线 20（比 eager 后端多 `deepcopy_module_state` call1）；top 变体新增 9 = eager 后端的 8 + `deepcopy_module_state` call0（在编译函数内 `copy.deepcopy(module)` 后比较 state——基线已在 eager 后端分歧的老问题换了个位置露出来）；改变项多两处：`numpy_scalar_types` 编译结果 dtype 由 float32 变 float64（numpy 标量提升，基线已分歧的已知族）、`tensor_subclass_torch_function` 由 state 分歧变成 `TypeError`（`__torch_function__` 子类 + AOTAutograd + graph break，响） | 负结果（无新静默缺陷） | `results/graph_break_insertion/aot_eager/report.json`、`results/graph_break_insertion_aot_eager.log` | — | — |
| 94 | **语料第 4 批**（`tcc/dynamo_semantics_batch4.py`，57 个程序：with / try / finally / else 流、消费者驱动的生成器协议、contextlib、for-else / while-else / walrus / match / nonlocal、输入容器的有序操作、惰性内建的副作用、异常链与 notes、lru_cache、heapq / bisect、dataclass 排序、星号解包；`run.py dynsem --only …` static + dynamic）：基线 3 个程序分歧——(1) **`try_error_in_except_handler`：张量算子抛出的 `IndexError` 在编译函数内穿过用户的 `try/except`**（最小化：`try: x[10] except IndexError:` 编译后直接把 IndexError 抛出去；`select` / `sum(dim=5)` / `transpose` / `unsqueeze` / `flatten` / `squeeze` / `softmax(dim)` / `chunk` / `index_select` 的 IndexError 与 `&` 的 NotImplementedError 同样；`RuntimeError` 族（narrow / view / cat / matmul / item / reshape / expand）都能被捕获——PR #184990 只给 RuntimeError 接了用户 handler；`fullgraph=True` 不报错）→ **42 号稿**；(2) `gen_in_zip_strict_uneven`：`zip(strict=True)` 报错信息里的参数编号 0 基 vs CPython 1 基（`zip() argument 0 is` vs `argument 2 is`）——低，记入 §12.6 备用；(3) `global_statement_counter`：测试本身的全局残留，已改 | **→ 42 号稿（高，已提交 #198192，2026-09-22）** | `results/dynsem4/`、`results/dynsem4_dynamic/`、`$TEMP/nested_try.py`、`nested_try2.py`、`exc_types.py` | `0920issues/42-*.md` | 语料补的是"结构"（try 内的张量错误），一轮 57 程序出 1 条新缺陷；与 #184340（5 月已关：调用方 `except IndexError` 接不到，PR #184452 把逃出的异常类型改回 IndexError）不同：这次是**函数内部**的 handler 被跳过 |
| 95 | **graph break 插入 × 241 程序语料**（eager + aot_eager）：新增 18 处（eager）= 原 8 处 + 第 4 批里同一族的 10 处：`gen_send_running_mean` / `gen_throw_handled_inside`（41 的 send/throw）、`od_move_to_end_on_state` / `od_move_to_end_then_iterate_later`（40）、**`lru_cache_hits_and_info`**（新族：`functools.lru_cache` 包裹的函数在 graph break 后 / 作为模块级对象被编译函数调用时，Dynamo 直接内联被包裹函数——底层函数调用 4 次、`cache_info().hits` 0，eager 2 次 / 2 hits；模块级 lru_cache 被调用两轮后底层函数被调 4 次而缓存从未填充）；deep 变体（742 处）与 top 相同；aot_eager 多 `deepcopy_module_state`（已知）；**dynamic=True**（`--dynamic`，基线 29 处）新增仍是同样 18 处，无 dynamic 独有分歧；**inductor 后端**（基线 30）新增 24 = 同样 18 + 3 个程序：`tensor_hash_id_set` / `identity_after_ops`（graph break 后子图只剩 `x * 1` / `x + 0`，Inductor 把输入对象本身当输出返回，`y is x` 变 True——**自己的 #197893**，这次是从语料侧再次命中，且 `x + zeros_like(x)`、`x.mul(1).add(0)` 同族）、`while_with_tensor_condition_item`（2e-7 浮点噪声） | lru_cache：**已知设计**——Dynamo 有专门的 lru_cache 警告（PR #156639 / #177182 "Only warn about lru_cache side effects when existing objects are mutated"），内联被包裹函数是有意为之；不报 | `results/graph_break_insertion_eager_b4.log`、`_aot_eager_b4.log`、`$TEMP/lru.py` | — | — |
| 96 | **12.6 副作用语料 × 逐语句 graph break**（`scripts/side_effect_graph_break_diff.py`：`side_effect_diff.py` 的 75 例，内层被编译函数用 AST 改写、闭包单元重新绑定后再 compile；eager 后端）：**0 例新分歧**。但覆盖有限——75 例里 53 例编译的是 lambda 或 nn.Module（无可插语句，breaks=0），只有约 20 例有多语句内层函数真正插了 break；两处 DIFF(both)（`forward_hook_registered_after_compile`、`context_manager_exit_runs`）是基线就有的已知分歧。aot_eager 后端在跑 | 负结果（这套语料结构上不适合语句级关系；Dynamo 语料才适合） | `results/side_effects_gb/eager.jsonl`、`results/side_effects_gb_eager.log` | — | 若要提高覆盖，需把 lambda 改成 def、把 nn.Module 的 forward 也改写（内建 nn.Linear 等做不到） |
| 97 | **测试基础设施教训（7.7 扫描首轮的假阳性）**：`torch.export.save → load` 在 mul / ceil / cosh / eq 等 12 个算子上间歇性抛 `SpecViolationError: Node.meta wrap_with_set_grad_enabled is missing val field`，单独重跑正常。根因在我们的 harness：`itertools.islice(op.sample_inputs(...), n)` 留下未耗尽的 OpInfo 样本生成器，它带 `@torch.no_grad` 装饰，GC 终结时在**下一个算子的 export 追踪中途**执行 `set_grad_enabled`，往图里塞了一个 `wrap_with_set_grad_enabled` 节点。修法：`take_samples()` 取完立即 `gen.close()`（已加到 `export_roundtrip_edge.py`、`autocast_sweep.py`、`opinfo_edge_sweep.py`），删掉 12 条假阳性记录续跑 | 不报（harness 问题；export 对终结器里的 grad-mode 切换不鲁棒属过于冷门） | `$TEMP/specviol_seq.log` | — | 之前所有用 islice 取样的扫描理论上都可能被同一终结器干扰（对不带梯度的比较影响很小）；Kaggle `tcc-gpuextra` 用的是修前的脚本，分诊时若见奇怪的 grad 相关差异先怀疑这一点 |
| 98 | **8.11 autocast 全算子扫描**（`scripts/autocast_sweep.py --acdtype bfloat16`：530 个 OpInfo 算子 × 2 个 float32 样本，`torch.autocast("cpu", bfloat16)` 下 eager vs compile，比输出 dtype、数值（3e-2）、NaN/inf 模式、异常一致性）：**507 ok、0 差异**，22 无 float32 样本，10 个算子编译报 AssertionError（基础设施错误，两侧都不算差异），1 个 ERROR。autocast 的 cast 策略在 Dynamo/Inductor 侧与 eager 完全一致；float16 变体同样 507 ok / 0 差异（`results/autocast/float16.jsonl`） | 负结果 | `results/autocast/bfloat16.jsonl`、`results/autocast_bfloat16.log` | — | 6.2b（一元）+ 本轮（全算子）后 autocast 角度可以关掉 |
| 99 | **7.7 取回后再算**（`scripts/export_roundtrip_edge.py`：520 个 OpInfo 算子 × 1 个边界值样本（inf/nan/max/近奇点），两条制品路径 `export → save → load → module()` 与 `aoti_compile_and_package → load → runner()` vs eager，比数值/dtype/stride/异常）：477 ok、21 差异、22 无样本。差异分诊：**(a) 进程级崩溃 2（`slice_scatter`、`kron`）→ 最小化后与算子无关：Windows 上销毁 AOTI runner 时模型 DLL 被卸载而 OpenMP 工作线程仍在自旋 → 访问违例（≥ 4096 元素即并行；sleep 2 s / 单线程都不崩；2.14 + nightly）→ 43 号稿（高）**；(b) `hash_tensor` AOTI 运行期 `Mismatch between ints consumed and num_ints`（proxy executor 对 fallback 算子默认 kwarg-only int 实参的计数错；eager / compile 正常）——**nightly 0911 已修**（PR #194261，2026-08-20 合入，2.14 分支未含），不报；(c) 已知：`return_types` 命名元组 6 个算子的 NotImplementedError = 自己的 #197104（已修，nightly 0911 早于修复）；`linalg.ldl_factor_ex` / `inv_ex` 的序列化名缺失——nightly 0921 上 14 种 `return_types`（含这两个）save/load 全部正常，PR #197128 已覆盖，不报；instance_norm AOTI INTERNAL ASSERT = 自己的 #197096；erfinv = #198133；max_pool 多 NaN 并列下标、group_norm / grid_sampler NaN 模式、三处零符号 = 台账 63 已分诊的族；这些与 plain `torch.compile` 结果相同，不是取回路径独有。save/load 往返本身：**0 处独有差异**（除 return_types 序列化）。首轮 12 条 `SpecViolationError` 假阳性见台账 97 | **→ 43 号稿（高，crash；Windows 独有），已提交 #198205（2026-09-22）**；hash_tensor 已在 nightly 修复 | `results/export_roundtrip/float32.jsonl`、`results/export_roundtrip_float32.log`、`scripts/export_roundtrip_report.py` | `0920issues/43-*.md` | 7.7 的价值在于"制品加载 + 销毁"这一段生命周期——数值本身与 compile 一致 |
| 100 | **语料第 5 批：张量算子抛出的异常 × Python 异常构造**（`tcc/dynamo_semantics_batch5.py`，33 程序：`except Exception` / `LookupError` / 元组、嵌套调用、循环 continue、推导式、`map`、生成器消费者、finally 顺序、re-raise、`raise from`、`contextlib.suppress`、`__exit__` 吞、else 分支、`sys.exc_info`、消息内容、`no_grad` 内；以及 TypeError / NotImplementedError / ZeroDivisionError / ValueError / assert / `torch._check` / warnings / 自定义异常 / `except*`）：**22 个程序 44 处分歧，全部是 #198192 一族**——`IndexError`（及 `NotImplementedError`）无论 handler 写成什么样都穿出编译调用，连 `except Exception:` 和 `contextlib.suppress` 都拦不住；TypeError / ZeroDivisionError / assert / `_check` / `except*` / warnings 全部与 eager 一致 | **→ 42b 评论，已由用户贴出**（#198192 comment 5778031868，2026-09-22） | `results/dynsem5/`、`results/dynsem5.log` | `0920issues/42b-*.md` | 语料按"缺陷机制"定向补程序，一批 33 个程序 22 个命中——用来给已报 issue 划范围很有效 |
| 101 | **8.12 `reduce-overhead`（CUDA graph trees）陈旧输出检测**（`scripts/cudagraph_stale_sweep.py`，Kaggle T4，torch 2.14.0+cu：19 种"留住旧输出 → 再跑 → 再读旧输出"的模式：普通输出、视图、列表/字典中的输出、detach、reshape、扩展视图、标量、requires_grad、lazy `.cpu()`、形状变化后、两个编译函数交错、`cudagraph_mark_step_begin` 之间、模块 buffer 更新、输出是输入别名 / 原地修改后的输入）：**0 例 STALE**——13 例读旧输出时抛出文档承诺的 `accessing tensor output of CUDAGraphs that has been overwritten by a subsequent run`，5 例输出与输入共享存储或 requires_grad 走非 cudagraph 路径而值不变（SAME），1 例把旧输出当下一次输入时同样报该错（保护生效）；三次调用的数值都与 eager 一致 | 负结果（cudagraph 树的覆盖保护对全部模式生效） | `kaggle_out/gpustale/plan/results/cudagraph_stale/cases.jsonl` | — | 原 `tcc-gpuextra` 被 Kaggle 取消后拆出的第一段；`gpudyn` / `gpuautotune` 在跑 |
| 102 | **P0-1 主实验复算（FSE2027 补充清单，2026-09-25）**（`scripts/gb_p01_audit.py` + `gb_p01_summary.py`：冻结 185 程序 = 前三批 77+74+34，第 4 批重名程序 `chained_comparison_tensor_scalar` 覆盖了第 3 批同名程序，脚本 reload 第 3 批恢复原版；这也是 185+57=241 的原因；同进程三臂 eager / 编译原程序 / 编译顶层逐语句断点，每程序 2 次调用 = 每臂 370 次）：**与 09-22 日志完全一致**——原程序臂 19 处分歧、断点臂 25、交集 17；成对分类 同 343、同一分歧 13、新增 8、消失 2、改变 4；静态插入 477 处（11 个程序 0 处），Dynamo 计数器实际触发用户断点 544 次（155 个程序全部触发，19 个程序少于插入数：语句未执行到或提前抛出），图段 614 → 1175。新增 8 处 = 4 程序 = **2 个独立根因**（C41 `collections_types`；C42 `gen_send`/`gen_throw`/`gen_return_value_stopiteration`）；消失 2 = `python_random`（基线分歧是已知 B17，断点后与 eager 相等）；改变 4 = `tensor_subclass_torch_function`/`threading_local_state`（基线已分歧的 state 日志内容变化）。最小复现：40 号稿代码块无 graph_break 即复现 C41；41 号稿第一段（生成器返回）无断点即复现，第二段（挂起时 send）以显式断点为挂起点。整轮墙钟 48.7 s | 复算通过；论文 RQ1 数字不变，但“477 处”须写作静态插入位置，实际触发 544 次另列 | `results/p01_audit/{audit_eager.json,pairs_eager.csv,P0-1_eager.md,audit_eager.log,minimal/}`；aot_eager 臂 `audit_aot_eager.*` | — | P1-3 成对计时已跑（`results/p13_timing/P1-3.md`：每臂 3 次全新运行，原程序臂中位 13.23 s、断点臂 32.65 s，离散 <4%；断点臂编译帧 1.9 倍、墙钟 2.5 倍；首个新根因出现于累计 0.6 s、第二个 5.2 s）。运行间唯一不稳定项 `deepcopy_module_state` call0（基线项，两次运行一次分歧一次一致），不影响新增/根因计数 |
| 103 | **P0-2 证据口径核验（FSE2027 补充清单，2026-09-25）**：73 项逐项台账（`results/p02_audit/items.csv`：来源行、版本/平台、契约有效性、重复关系、根因 id、状态截止日、修复是否合入）——73 = 55 issue + 15 评论 + 3 MSVC；候选 = 台账 102 行 + 编号前排除 22 行；独立根因 64（55 条新 issue 对应 54 个）；契约有效 66、跨编译器不适用 4、无效 2（B16、C12 两侧都错）、预期行为 1（B13）。35 对历史重放（`historical_pairs.csv`）：35/35 同设备（Kaggle Linux CPU 2.10 vs 2.14）；修复提交已在 v2.14.0 中 19（清单所述 12 + 复查 7）、1 可能、15 仅版本差异；未关闭 issue 检出率 45.7%→41.4%（论文正文写 41% 不变，需改）。GitHub 只读复查后 `ISSUE_OUTCOMES.md` 刷新：73 = 16/13/43/1（09-23 为 14/10/48/1）；7 项变动见报告；**B16（修复 PR 关闭未合入）与 C37（草稿 PR 覆盖 6/134）待用户定档** | 复算通过；论文四档数字待定档后统一改 | `results/p02_audit/AUDIT.md`、`items.csv`、`historical_pairs.csv`；`../ISSUE_OUTCOMES.md`；`scripts/issue_outcomes.py` VERDICT 更新 7 条 | — | 14 篇第一轮稿件头部未写平台（README 声称 Windows+Linux） |
| 104 | **P1-1 最近方法对照（FSE2027 补充清单，2026-09-25）**：(a) **TorchProbe** = 作者 soodoshll 的 `temisu` 仓库（提交 `3aa039c4`，1087 行，NNSmith 之上四种累积变异，值 oracle，`no_grad`，硬编码 cuda）；两处可移植改动（device 参数化驱动；`random.sample(dict.items())`→`list`，Python ≥3.11 必需）后在 3.14/2.14/nnsmith 0.1.0 可跑。适用性：29 程序语料 3 个可表达、185 程序 Python 语料 0 个（`scripts/p11_torchprobe_applicability.py`）。自有种子：`max_nodes=10` 35 种子 175 测试 + `max_nodes=20` 10 种子 47 测试，全部通过、0 不一致、0 崩溃、0 无效，与已报缺陷无对应、无新发现。Setting B（其程序装入本 harness）：未变异 5/19、变异后 6/19；harness 原始 6/19、15/19 含两类人为项——注入包装器 `_make_scalar_boundary` 对张量参数 `if tensor` 抛错（NNSmith 腿同样含 1 个，去掉后 5/19）；同进程数百次 compile/reset 后 Dynamo `dictionary changed size during iteration`（Python 3.14 `get_items_from_dict` 与 GC 回调竞争，上游 PR #191281 2026-08-20 合入 main，2.14 仍旧；单独不复现）。(b) **Random B1 逐种子**（预算 12/程序，值 oracle）：10 种子检出 11,12,11,11,11,11,6,10,11,10 → 均值 10.4、中位 11、sd 1.65；种子 0–2 均值 11.33 = 论文数；无效率均值 17.9%；逐故障首次检出见 `random_seeds_per_fault.csv`。(c) **NNSmith B3 自有输入** 3 模型种子 6/6/6 | TorchProbe 不适用于 Python 语料，不能排名；论文 RQ5 “could not be executed” 改写；Random 补分布 | `results/p11_baselines/{TORCHPROBE.md,random_seeds.md,random_seeds.csv,random_seeds_per_fault.csv,nnsmith_seeds.md,torchprobe_*.json,README.md}`；`scripts/p11_torchprobe_setting_b.py` | — | **harness 教训**：`_external_bench` 把限定程序的注入故障套到外部程序上会产生包装器异常计入检出；跨故障批量 compile/reset 在 3.14 上触发 Dynamo 已修的 GC 竞争 |
| 105 | **P1-2 真实缺陷的 oracle 三档分析（FSE2027 补充清单，2026-09-25）**（`scripts/oracle_tiers_cases.py` + `oracle_tiers_real.py`：73 项 − 5 跨编译器 − 10 流水线外 = 58 项，合并评论与同根因后 **52 个独立根因**、127 子用例；每子用例 eager/编译各执行一次存同一轨迹，三档离线判定，复用 `tcc.oracle`/`tcc.observe`）：T1 值+崩溃/异常 36，T2 +元数据 38，T3 +别名/状态/梯度/异常路由/多精度/进程 51，本机不可复现 1（B5 需 CUDA）；最弱档 T1 36 / T2 2（B15、B25）/ T3 13。误报：29 个一致语料程序三档均 0；autocast 阴性集 924 样本三档各 2（bf16 几个 ULP，论文容差不报）。人工复核时间只有 09-22 有钟点（C41/C42 ≤15 min 成稿，其余见 `review_times.csv`），其他未记录 | 复算通过；RQ3 可改为按 52 根因报 36/38/51 | `results/p12_oracle_tiers/{REPORT.md,root_causes.csv,tiers.csv,subcases.csv,traces/,control_traces/}` | — | **harness 待修**：`tcc/oracle.py` 浮点比较在 eager 输出含 NaN 时容差为 NaN、屏蔽全部差异（C13 靠位置级 NaN 检查才报）；`_exact_equal` 把 int/bool dtype 差算值差；B7 依赖进程内先跑过 jvp |

**2026-09-21 重新编号**：`0920issues/` 按优先级从高到低重排，编号即提交顺序；本文件里的 `0920issues/NN-*.md` 指针已换成新编号，正文叙述里的“草稿 NN”仍是旧编号（旧→新：01→01、02→12、03→19、04→04、05→11、06→06、07→14、08→09、09→13、10→20、11→21、12→10、14→02、15→07、16→05、17→17、18→18、19→03、20→22、21→08、22→15、23→16）。

**台账 43 终态（2026-09-21）**：用户确认已被他人提交（#197434），草稿 13 已删除，不提交。

**台账 43 更正（2026-09-21 深夜）**：已被他人报告为 #197434（09-17，标题是 adaptive_max_pool2d）并有修复 PR #197596；草稿 13 降级为可选评论（增量：`return_indices=False` 时梯度同样错）。

10.1 改写前提差分其余结果（183 例，174 一致）：`x+0` 不规整 -0.0（#188680 一类）、`x ** -0.5`（偏离的是 eager）、cast 链（=草稿 10）。10.2 前缀一致性（进行中，70/135）：仅向量主体与标量尾部 1 ULP 级差异，无对齐/线程差异 → 负结果。

7.1 布局/别名扫描其余结果（534 个算子 × 7 种布局 × 前向+反向，4 路分片约 70 分钟）：527 个完全一致；`view_copy` 对不可 view 的布局 Inductor 编译失败而 eager 正常（低，未写稿）；4 个算子输出步长与 eager 不同（信息性，未报）。→ 布局这根轴除池化外是干净的（负结果）。

副作用差分其余结果（75 例，71 一致）：编译后才注册的 forward hook 不被调用 = 文档化限制（`skip_nnmodule_hook_guards`），不报；`x.add_(1)` 之后图里出形状错误——eager 已改了 x、compile 在追踪期就报错所以 x 未改 = 追踪式编译的固有差异，不报；**对"既被返回又参与 loss 的中间量"注册 tensor hook：eager 的 hook 收到梯度并可改写（x.grad 变 0），compile 下 hook 收到 `None`、改写不生效（x.grad=2）**——AOTAutograd 把整段当一个 autograd 节点的固有限制，但"hook 收到 None"值得查重后再定（`reports_sideeffects/ctx_manager_and_output_hook.py`）。

不报：`atanh(次正规数)`→0 与 `erf` 3e-5 相对误差（精度类，量级小）；有符号零 15 条（`maximum/minimum/fmax/fmin/clamp(0.0,-0.0)`、`remainder(-0.0,·)`、`relu6(-0.0)`、`atanh(-0.0)`）——open #188680 伞形 issue 讨论的正是测试对有符号零不敏感，可在其下补清单；float→int 越界转换（C++ 与文档均为未定义）；eager `div(trunc)`/`lcm` 崩溃（重复 #138425 / #121343）。

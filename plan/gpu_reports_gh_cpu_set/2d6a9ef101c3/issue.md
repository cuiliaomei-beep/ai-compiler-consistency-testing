# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    x = torch.ones([7, 7])
    t = bn1(x)
    t = gn(t)
    # normalised residual: t + t / clamp(t, min=1e-6)
    t = torch.addcdiv(t, t, torch.clamp(t, min=1e-6))   # ← wrong in compiled
    t = bn2(t)
    return t

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.9999985695, 0.9999985695, 0.9999985695, 0.9999985695, 0.9999985695, 0.9999985695, 0.9999985695, 0.9999985695, ... (41 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-30.51745605, -30.51745605, -30.51745605, -30.51745605, -30.51745605, -30.51745605, -30.51745605, -30.51745605, ... (41 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.152e+01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 31.517454624176025
      first mismatching element : [0, 0]
      mismatching elements      : 49
      total elements            : 49
      eager error vs float64    : 0.0
      compiled error vs float64 : 31.517454624176025
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0.9999985695
      actual at that index      : -30.51745605
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 8 -> 8 lines, inputs 0 -> 0 elements; 0 reductions kept of 4 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/181694 — origin issue of this reproducer

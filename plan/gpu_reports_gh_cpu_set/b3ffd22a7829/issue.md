# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x):
    # Base tensor `x` has size 10.
    # We create a valid overlapping view of size 100 pointing entirely inside `x`
    view = torch.as_strided(x, (100,), (0,))

    # In Inductor, resizing this view unwraps it to the 10-element base,
    # but wrongly accesses it with stride 1 for the first 50 elements,
    # resulting in reading 40 elements of out-of-bounds memory.
    return torch.ops.aten.resize(view, (50,))

args = (torch.randn(1),)

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.9246624112, -0.9246624112, -0.9246624112, -0.9246624112, -0.9246624112, -0.9246624112, -0.9246624112, -0.9246624112, ... (42 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.9246624112, -0.4253442287, -2.643845797, 0.1451838613, -0.120866403, -0.5797257423, -0.6228510141, -0.3283869028, ... (42 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.190e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 3.190167248249054
      first mismatching element : [1]
      mismatching elements      : 49
      total elements            : 50
      eager error vs float64    : 0.0
      compiled error vs float64 : 3.190167248249054
      accepted budget           : 4.76837158203125e-07
      expected at that index    : -0.9246624112
      actual at that index      : -0.4253442287
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 9 -> 9 lines, inputs 64 -> 1 elements; 7 reductions kept of 7 tried.
- kept: drop rank
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 0

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/175919 — origin issue of this reproducer

# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `(base)` to `{'arg': 0, 'value': False}`.
'requires_grad' changed without a recompile and the result matched; guards may be under-specified (informational)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def overlapping_views(x):
    a = x[:6]
    b = x[4:]
    a.mul_(2.0)
    b.add_(1.0)
    return x, a, b

args = (torch.randn(10),)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'requires_grad': {'arg': 0, 'value': False}}
args_b = (torch.randn(10),)
torch._dynamo.reset()
compiled = torch.compile(overlapping_views, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(overlapping_views, backend='inductor')(*args_b)
eager = overlapping_views(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: P[T,T,T]
[0] [1.322704315, 0.5338482261, 0.1233545169, 1.242634654, 0.09618806839, 0.6677395105, -0.5227684975, 1.381683946, ... (2 more)]
[1] [1.322704315, 0.5338482261, 0.1233545169, 1.242634654, 0.09618806839, 0.6677395105]
[2] [0.09618806839, 0.6677395105, -0.5227684975, 1.381683946, -0.02760863304, 0.4369472265]
```

## Actual Behavior

Compiled result:

```text
structure: P[T,T,T]
[0] [1.322704315, 0.5338482261, 0.1233545169, 1.242634654, 0.09618806839, 0.6677395105, -0.5227684975, 1.381683946, ... (2 more)]
[1] [1.322704315, 0.5338482261, 0.1233545169, 1.242634654, 0.09618806839, 0.6677395105]
[2] [0.09618806839, 0.6677395105, -0.5227684975, 1.381683946, -0.02760863304, 0.4369472265]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: (base) -> {'arg': 0, 'value': False}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | same |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
(no findings recorded)
```

## Suspected Stage

```text
no divergence
```

Attribution basis: first divergent layer = n/a; 'requires_grad' changed without a recompile and the result matched; guards may be under-specified (informational)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 1.
```

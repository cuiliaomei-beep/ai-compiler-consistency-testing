# [pytorch compile] Incorrect result when `substitute` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `substitute` changes from `(base)` to `{'arg': 1, 'value': 0}`.
'substitute' changed without a recompile and the result matched; guards may be under-specified (informational)
This test was generated because the static analysis found: parameter compared against a constant / used as index or bound (factor `n`, oracles ['value', 'exception']).

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def loop_bound(x, n):
    for _ in range(n):
        x = x * 1.5 + 1.0
    return x

args = (torch.randn(8, 4), 3)

# context A then context B on the same compiled callable (no reset)
# A = {} (base)
# B = {'substitute': {'arg': 1, 'value': 0}}
args_b = (torch.randn(8, 4), 0)
torch._dynamo.reset()
compiled = torch.compile(loop_bound, backend='inductor')
compiled(*args)              # warm the cache under A
warm = compiled(*args_b)     # execute under B without a reset
torch._dynamo.reset()
cold = torch.compile(loop_bound, backend='inductor')(*args_b)
eager = loop_bound(*args_b)
print('eager :', eager)
print('cold  :', cold)
print('warm  :', warm)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.525595903, -0.7502318025, -0.6539809108, -1.609484792, -0.1001671776, -0.6091889143, -0.9797722697, -1.609096289, ... (24 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-1.525595903, -0.7502318025, -0.6539809108, -1.609484792, -0.1001671776, -0.6091889143, -0.9797722697, -1.609096289, ... (24 more)]
```

## Trigger Condition

Changed semantic factor:

```text
substitute: (base) -> {'arg': 1, 'value': 0}
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E1 = eager(A)
C1 = compiled_cold(A)
E2 = eager(B)
C2cold = compiled_cold(B)
reset; compiled(A)
C2warm = compiled_warm(B)
C1' = compiled_warm(A)
```

## Cache / Specialization Evidence

| Execution | Result |
|---|---|
| eager(B) | reference |
| compiled cold(B) | correct |
| compiled A -> warm B | correct |
| cold(B) vs warm(B) | same |
| recompile on A -> B | **no** |
| compiled A -> B -> A vs A | same |
| recompile on B -> A | **no** |

## Difference

```text
(no findings recorded)
```

## Suspected Stage

```text
no divergence
```

Attribution basis: first divergent layer = n/a; 'substitute' changed without a recompile and the result matched; guards may be under-specified (informational)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 1.
```

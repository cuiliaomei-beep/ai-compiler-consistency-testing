# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, idx):
    res = torch.zeros(1, x.shape[1], device=x.device, dtype=x.dtype)
    res.scatter_add_(0, idx.unsqueeze(-1).expand(-1, x.shape[1]), x)
    return torch.index_select(res, 0, idx)

args = (torch.randn(2, 1).requires_grad_(True), torch.randint(0, 4, (2,)))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.321661234, -1.311121464, -0.4889325202, -0.8959231377, -1.321661234, -1.311121464, -0.4889325202, -0.8959231377, ... (24 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 0, 0, 0, 0, 0, 0, 0, ... (24 more)]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.322e+00 exceeds 4.0x eager err 7.451e-08
      max absolute error        : 1.3216612339019775
      first mismatching element : [0, 0]
      mismatching elements      : 32
      total elements            : 32
      eager error vs float64    : 7.450580596923828e-08
      compiled error vs float64 : 1.3216611945535988
      accepted budget           : 6.302171868810547e-07
      expected at that index    : -1.321661234
      actual at that index      : 0
  [gradient] grad[0]: compiled err vs fp64 = 8.000e+00 exceeds 8.0x eager err 0.000e+00
      max absolute error        : 8.0
      first mismatching element : [0, 0]
      mismatching elements      : 32
      total elements            : 32
      eager error vs float64    : 0.0
      compiled error vs float64 : 8.0
      accepted budget           : 3.814697265625e-06
      expected at that index    : 8
      actual at that index      : 0
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 40 -> 4 elements; 7 reductions kept of 12 tried.
- kept: halve dim 0
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 1

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/191571 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/191571 — `torch.compile` silently returns stale zeros when a size-1 `scatter_add_` is gathered from in the same graph (read-before-write in a fused Triton kernel)

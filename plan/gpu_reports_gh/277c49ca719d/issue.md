# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': False}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    out = F.scaled_dot_product_attention(x, x, x)   # [4, 12, 4]
    out, _ = m_mha(out, out, out)                    # [4, 12, 4]
    out = torch.fft.fft(out).abs()                   # [4, 12, 4] real
    out = m_hardswish(out)                           # [4, 12, 4]
    return out

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.0485582687, 0.05907614529, 0.07680762559, 0.05907614529, 0.04911318421, 0.06295895576, 0.09388402849, 0.06295895576, ... (184 more)]
```

## Actual Behavior

Compiled result:

```text
exception AssertionError: expected size 4==4, stride 4==48 at dim=0; expected size 12==12, stride 16==4 at dim=1
Error in op: torch.ops.aten._fft_r2c.default
This error most often comes from a incorrect fake (aka meta) kernel for a custom op.
Use torch.library.opcheck to test your custom op.
See https://pytorch.org/docs/stable/library.html#torch.library.opcheck
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': False} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: AssertionError
```

## Difference

```text
E3_inductor:
  [exception] test raised AssertionError, ref returned normally
      exception                 : AssertionError
      message                   : expected size 4==4, stride 4==48 at dim=0; expected size 12==12, stride 16==4 at dim=1
Error in op: torch.ops.aten._fft_r2c.default
This error most often comes from a incorrect fake (aka meta) kernel 
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 6 -> 6 lines, inputs 0 -> 0 elements; 0 reductions kept of 3 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/181625 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/195325 — [switch] Support torch.vmap with an unbatched index
- https://github.com/pytorch/pytorch/issues/160018 — Triton kernel generated for Flex Attention throws InductorError: RuntimeError: No valid triton configs when seq length < 128
- https://github.com/pytorch/pytorch/pull/185163 — [aot_autograd] Handle None in saved tensors for LSTM/RNN backward; enable allow_rnn by default
- https://github.com/pytorch/pytorch/pull/185891 — [dynamo] Guard AOTAutograd input mutation overlap changes
- https://github.com/pytorch/pytorch/pull/194091 — support min, max in maybe mark dynamic

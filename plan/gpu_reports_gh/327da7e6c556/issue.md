# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def cu_seqlens(mask):
    lengths = mask.sum(dim=-1, dtype=torch.int32)
    return F.pad(torch.cumsum(lengths, dim=0, dtype=torch.int32), (1, 0))

args = (torch.randint(0, 4, (1, 16384,)).to(torch.bool),)

eager = cu_seqlens(*args)
torch._dynamo.reset()
compiled = torch.compile(cu_seqlens, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 16384]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [16384, 16384]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: non-float values differ exactly
      first mismatching element : [0]
      mismatching elements      : 1
      total elements            : 2
      expected at that index    : 0
      actual at that index      : 16384
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 3 -> 3 lines, inputs 16384 -> 16384 elements; 3 reductions kept of 7 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/189895 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/195582 — [inductor] Silent wrong and non-reproducible results: BatchNorm2d followed by a shrinking F.pad
- https://github.com/pytorch/pytorch/pull/195465 — [inductor] Fix ValueRangeError crash on negative padding in inplace_constant_pad_nd
- https://github.com/pytorch/pytorch/issues/189895 — [inductor] Silent wrong results: constant_pad_nd fused into a split reduction drops the pad's boundary select (corrupts flash-attn cu_seqlens)
- https://github.com/pytorch/pytorch/issues/77764 — General MPS op coverage tracking issue

# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def my_example():
    torch.manual_seed(0)
    d_float32 = torch.rand((8, 8), device="cpu")
    return d_float32 + d_float32

args = (...)  # see execution_trace.json

eager = my_example(*args)
torch._dynamo.reset()
compiled = torch.compile(my_example, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0.9925131798, 1.536443591, 0.1769548655, 0.2640609741, 0.6148456335, 1.268157363, 0.98018682, 1.792889476, ... (56 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.1761855334, 0.9087455869, 1.577113271, 1.008804917, 0.4254157841, 0.7839221358, 0.7593485713, 1.200536132, ... (56 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.547e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.5470104217529297
      first mismatching element : [0, 0]
      mismatching elements      : 64
      total elements            : 64
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.5470104217529297
      accepted budget           : 9.508767675470153e-07
      expected at that index    : 0.9925131798
      actual at that index      : 0.1761855334
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 0 -> 0 elements; 0 reductions kept of 1 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/95161 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/194658 — add aot autograd cache tests and fix over-caching
- https://github.com/pytorch/pytorch/pull/186476 — Fix Dynamo resume tensor lifetimes across graph breaks
- https://github.com/pytorch/pytorch/issues/95161 — AOTAutograd based torch.compile doesn't capture manual seed setting in the graph
- https://github.com/pytorch/pytorch/pull/118155 — [Dyanmo Single Step Graph]Inline NNModule's fwd in dynamo instead of leave it allow_in_graph

# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x, w):
    x = torch.matmul(x, w)
    x = x.to(torch.bfloat16).float()  # intentional precision truncation
    x = x * torch.sigmoid(x)
    return x.sum(dim=1)

args = (torch.randn(4, 4, 32), torch.randn(32, 4))

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [50.02272034, 61.40285492, 104.7847366, 56.56276703, 55.37533569, 47.36238861, 72.97634888, 89.15085602, ... (120 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [50.07450867, 61.4493866, 104.848587, 56.50841522, 55.41065979, 47.34102631, 72.86689758, 89.16520691, ... (120 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.095e-01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.1094512939453125
      first mismatching element : [0, 0]
      mismatching elements      : 127
      total elements            : 128
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.1094512939453125
      accepted budget           : 6.591012788703665e-05
      expected at that index    : 50.02272034
      actual at that index      : 50.07450867
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 5 -> 5 lines, inputs 5120 -> 640 elements; 6 reductions kept of 18 tried.
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 1
- kept: halve dim 1

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/179561 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/179561 — [inductor] `x.to(torch.bfloat16).float()` precision cast silently elided during fusion
- https://github.com/pytorch/pytorch/pull/179572 — [inductor] Preserve explicit low-precision FP casts during fusion

# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `float16` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `q.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def foo(q, k, v, a):
    qk = q @ k.transpose(-2, -1) / 1.0
    qk = qk + a
    attn = torch.softmax(qk, dim=-1)
    output = attn @ v
    return output

args = (torch.randn(1, 1, 64, 64).to(torch.float16), torch.randn(1, 1, 64, 64).to(torch.float16), torch.randn(1, 1, 64, 64).to(torch.float16), torch.randn(64, 64).to(torch.float16))

eager = foo(*args)
torch._dynamo.reset()
compiled = torch.compile(foo, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.137695312, 0.3820800781, -0.1608886719, -0.112487793, -0.419921875, -0.916015625, -0.3747558594, 0.4291992188, ... (4088 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.4291992188, 0.3481445312, -0.07775878906, 0.2395019531, -0.1696777344, -0.3068847656, -0.1315917969, 0.427734375, ... (4088 more)]
```

## Trigger Condition

Changed semantic factor:

```text
dtype: float16 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.205e+00 exceeds 4.0x eager err 1.095e-02
      max absolute error        : 3.206298828125
      first mismatching element : [0, 0, 0, 0]
      mismatching elements      : 3891
      total elements            : 4096
      eager error vs float64    : 0.010947700407759609
      compiled error vs float64 : 3.2050198465822684
      accepted budget           : 0.043790801631038434
      expected at that index    : -1.137695312
      actual at that index      : -0.4291992188
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 6 -> 6 lines, inputs 16384 -> 16384 elements; 3 reductions kept of 14 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/175632 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/194309 — [Inductor] Add gfx950 FlyDSL FlexAttention forward
- https://github.com/pytorch/pytorch/pull/185816 — Enable CPU FlexAttention on AArch64
- https://github.com/pytorch/pytorch/pull/183328 — [special] Add arbitrary-order modified Bessel functions
- https://github.com/pytorch/pytorch/pull/192085 — [Inductor] Fix invalid attn_mask + is_causal combo in qwen2 SDPA repro test
- https://github.com/pytorch/pytorch/pull/190903 — [Inductor] Add gfx950 FlyDSL GEMM and torch.mm autotuning

# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, c):
    z = torch.where(c, torch.full_like(x, 0.5), torch.full_like(x, -0.5))
    y = z.to(torch.float16) + x.to(torch.float16)
    return y, y.float().sum()

args = (torch.randn(2).requires_grad_(True), torch.randint(0, 4, (2,)).to(torch.bool))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: P[T,T]
[0] [0.5, -0.3999023438, -0.3000488281, -0.1999511719, -0.1000976562, 1, 0.1000976562, 0.2001953125, ... (16 more)]
[1] [20.59960938]
```

## Actual Behavior

Compiled result:

```text
structure: P[T,T]
[0] [0.5, -0.3999023438, -0.3000488281, -0.1999511719, -0.09997558594, 1, 0.09997558594, 0.1999511719, ... (16 more)]
[1] [20.59999847]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[1]: compiled err vs fp64 = 3.891e-04 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 0.00038909912109375
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.0
      compiled error vs float64 : 0.00038909912109375
      accepted budget           : 9.822659194469452e-06
      expected at that index    : 20.59960938
      actual at that index      : 20.59999847
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 48 -> 4 elements; 6 reductions kept of 10 tried.
- kept: drop rank
- kept: halve dim 1
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: drop rank

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/182131 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/194310 — Add tiled CUDA kernel for dense 2D transpose copies
- https://github.com/pytorch/pytorch/pull/195223 — [inductor] Preserve low-precision rounding before comparisons
- https://github.com/pytorch/pytorch/issues/182131 — torch.compile/Inductor produces different result for float16 cast before elementwise add
- https://github.com/pytorch/pytorch/pull/183639 — Add low precision cast barrier regression coverage
- https://github.com/pytorch/pytorch/pull/176994 — [Inductor] Improve materialization heuristic for a chain of computations

# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: None
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def test_factory_apis():
    return {
        "ones": torch.ones(2),
        "zeros": torch.zeros(2),
        "tensor": torch.tensor([1.0, 2.0]),
        "arange": torch.arange(2.0),
        "empty": torch.empty(2)
    }

args = (...)  # see execution_trace.json

eager = test_factory_apis(*args)
torch._dynamo.reset()
compiled = torch.compile(test_factory_apis, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: D{'arange':T,'empty':T,'ones':T,'tensor':T,'zeros':T}
[0] [0, 1]
[1] [0, 1.366405341e-315]
[2] [1, 1]
[3] [1, 2]
[4] [0, 0]
```

## Actual Behavior

Compiled result:

```text
exception InternalTorchDynamoError: IndexError: list index out of range

from user code:
   File "reproducers_gh_cuda/issue_184405_torch_compile_backend_inductor_returns_s.py", line 17, in test_factory_apis
    "ones": torch.ones(2),

Set TORCHDYNAMO_VERBOSE=1 for the internal stack trace (please do this especially if you're reporting a bug to PyTorch). For even more developer context, set TORCH_LOGS="+dynamo"
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: InternalTorchDynamoError
E2_aot_eager: InternalTorchDynamoError
E3_inductor: InternalTorchDynamoError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : IndexError: list index out of range

from user code:
   File "reproducers_gh_cuda/issue_184405_torch_compile_backend_inductor_returns_s.py", line 17, in test_factory_apis
    "ones": torch.ones(2),

S
E2_aot_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : IndexError: list index out of range

from user code:
   File "reproducers_gh_cuda/issue_184405_torch_compile_backend_inductor_returns_s.py", line 17, in test_factory_apis
    "ones": torch.ones(2),

S
E3_inductor:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : IndexError: list index out of range

from user code:
   File "reproducers_gh_cuda/issue_184405_torch_compile_backend_inductor_returns_s.py", line 17, in test_factory_apis
    "ones": torch.ones(2),

S
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/184405 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/190903 — [Inductor] Add gfx950 FlyDSL GEMM and torch.mm autotuning
- https://github.com/pytorch/pytorch/pull/195346 — [guards] Make serialized guards rebuild correctly in a fresh process
- https://github.com/pytorch/pytorch/issues/184405 — `torch.compile(..., backend="inductor")` returns stale float32 for dtype=None factory op after torch.set_default_dtype(torch.float64)
- https://github.com/pytorch/pytorch/pull/184279 — Avoid MultiOutput for single-output fallback kernels
- https://github.com/pytorch/pytorch/issues/147551 — FlexAttention compiled has illegal memory access or device-side assert even though all tensors are contiguous

# [pytorch compile] Compiled execution inconsistent with eager (value, mutation)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        p = torch.randperm(5)
        # Create a non-injective (overlapping) view of randperm 
        idx = torch.as_strided(p, (5,), (0,))
        y = x[idx] + 1.0
        x.index_put_((idx,), y, accumulate=False)
        return x

args = (...)  # see execution_trace.json

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 1, 0, 1]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 1, 1, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.000e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0
      first mismatching element : [1]
      mismatching elements      : 2
      total elements            : 5
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 1
  [mutation] input[0] holds a different value after the call (max diff 1.000e+00); in-place effects diverge
      max absolute error        : 1.0
      first mismatching element : [1]
      mismatching elements      : 2
      total elements            : 5
      expected at that index    : 0
      actual at that index      : 1
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 7 -> 7 lines, inputs 5 -> 0 elements; 0 reductions kept of 1 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/175903 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/175903 — [Inductor] Incorrect results in `index_put_` when `randperm` views are not injective

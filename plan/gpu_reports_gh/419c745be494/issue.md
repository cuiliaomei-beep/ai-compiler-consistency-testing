# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def compiled_bmm(x, y):
    return torch.bmm(x, y)

args = (torch.randn(2, 8, 8).to(torch.float16), torch.randn(2, 8, 64))

eager = compiled_bmm(*args)
torch._dynamo.reset()
compiled = torch.compile(compiled_bmm, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: expected scalar type Half but found Float
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [3.713933945, -2.971517801, 0.453440249, -1.927093506, 5.945209503, -1.589057565, 4.293181896, 1.420083165, ... (1016 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : expected scalar type Half but found Float
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 2 -> 2 lines, inputs 1152 -> 1152 elements; 3 reductions kept of 11 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/177629 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/pull/173459 — Add dtype mismatch validation for mm/bmm in meta registrations (#171190)
- https://github.com/pytorch/pytorch/issues/177629 — `torch.compile(backend="inductor")` silently succeeds on `torch.bmm` with mismatched dtypes (float16 @ float32) where eager raises RuntimeError
- https://github.com/pytorch/pytorch/issues/177630 — `torch.compile(backend="inductor")` silently succeeds on `torch.matmul` (≥3D) with mismatched dtypes (float16 @ float32) where eager raises RuntimeError
- https://github.com/pytorch/pytorch/issues/177480 — `torch.compile` silently succeeds on `bmm`/`matmul` with mixed dtypes (float16 @ float32) where eager raises RuntimeError

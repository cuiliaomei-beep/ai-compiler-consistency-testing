# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(mask, box):
        mask = torch.randn(1, 1, 30, 30, device="cuda")
        h, w = box.tolist()
        return mask.sum() + h + w

args = (torch.randint(0, 4, (2,)), torch.randint(0, 4, (2,)))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [71.17281342]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [105.4078064]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 3.423e+01 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 34.23499298095703
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 0.0
      compiled error vs float64 : 34.23499298095703
      accepted budget           : 3.393784209038131e-05
      expected at that index    : 71.17281342
      actual at that index      : 105.4078064
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 4 -> 4 elements; 3 reductions kept of 7 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/190093 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/190093 — test_interpolate_propagate_real_tensors_cuda fails CUDA mem-leak check: real CUDA tensor from data-dependent .tolist() retained past dynamo.reset()
- https://github.com/pytorch/pytorch/pull/187858 — [inductor] Add torch._inductor.compile_to_python

# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    # Invalid Bernoulli probabilities — eager checks p in [0, 1]
    p = torch.full((4,), 2.0, dtype=torch.float32)
    return torch.bernoulli(p)

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: Expected p_in >= 0 && p_in <= 1 to be true, but got false.  (Could this error message be improved?  If so, please report an enhancement request to PyTorch.)
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1, 1, 1, 1]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : Expected p_in >= 0 && p_in <= 1 to be true, but got false.  (Could this error message be improved?  If so, please report an enhancement request to PyTorch.)
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 0 -> 0 elements; 0 reductions kept of 0 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/185246 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/185246 — torch.compile silently accepts invalid Bernoulli probabilities that eager mode rejects
- https://github.com/pytorch/pytorch/pull/185544 —  [inductor] Validate Bernoulli probabilities in decomposition to match eager mode (#185544)

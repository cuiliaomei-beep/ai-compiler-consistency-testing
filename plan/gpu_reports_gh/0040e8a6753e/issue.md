# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cu128
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: 12.8
- GPU: Tesla T4
- OS: Linux 6.12.90+
- Triton: 3.6.0

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x):
    values = [x * 2, x * 3, x * 4]
    selected = values.pop(x.shape[0] - 2)
    return selected +values[0]

args = (torch.randn(3),)

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.9018669724, 4.044142723, -3.077429533]
```

## Actual Behavior

Compiled result:

```text
exception InternalTorchDynamoError: AsPythonConstantNotImplementedError: SymNodeVariable() is not a constant

from user code:
   File "reproducers_gh_cuda/issue_196285_torch_compile_dynamic_true_fails_to_hand.py", line 10, in fn
    selected = values.pop(x.shape[0] - 2)

Set TORCHDYNAMO_VERBOSE=1 for the internal stack trace (please do this especially if you're reporting a bug to PyTorch). For even more developer context, set TORCH_LOGS="+dynamo"
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: InternalTorchDynamoError
E2_aot_eager: InternalTorchDynamoError
E3_inductor: InternalTorchDynamoError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : AsPythonConstantNotImplementedError: SymNodeVariable() is not a constant

from user code:
   File "reproducers_gh_cuda/issue_196285_torch_compile_dynamic_true_fails_to_hand.py", line 10, in fn
    sel
E2_aot_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : AsPythonConstantNotImplementedError: SymNodeVariable() is not a constant

from user code:
   File "reproducers_gh_cuda/issue_196285_torch_compile_dynamic_true_fails_to_hand.py", line 10, in fn
    sel
E3_inductor:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : AsPythonConstantNotImplementedError: SymNodeVariable() is not a constant

from user code:
   File "reproducers_gh_cuda/issue_196285_torch_compile_dynamic_true_fails_to_hand.py", line 10, in fn
    sel
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/196285 — origin issue of this reproducer
- https://github.com/pytorch/pytorch/issues/196285 — torch.compile(dynamic=True) fails to handle shape-derived symbolic indices in list.pop()
- https://github.com/pytorch/pytorch/pull/194366 — [inductor] Fix graph ordering and broadcast values in partitioned scatter
- https://github.com/pytorch/pytorch/pull/186479 — [dynamo] Respect mark_dynamic on Parameter inputs
- https://github.com/pytorch/pytorch/pull/192631 — [dynamo] Parameterize invoke_subgraph reuse over a subscript index
- https://github.com/pytorch/pytorch/issues/63076 — Tracing models all the way down to basic functions

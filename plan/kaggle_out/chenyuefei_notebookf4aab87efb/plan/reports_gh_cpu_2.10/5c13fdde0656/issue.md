# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cpu
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Linux 6.12.90+
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x):
    return torch.quantile(x, 0.5)

args = (torch.randn(5),)

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [3]
```

## Actual Behavior

Compiled result:

```text
exception TorchRuntimeError: Dynamo failed to run FX node with fake tensors: call_function <built-in method quantile of type object at 0x79e0a3182820>(*(FakeTensor(..., size=(s77,)), 0.5), **{}): got RuntimeError('Cannot call numel() on tensor with symbolic sizes/strides\nException raised from throw_cannot_call_with_symbolic at /pytorch/c10/core/TensorImpl.cpp:289 (most recent call first):\nframe #0: c10::Error::Error(c10::SourceLocation, std::__cxx11::basic_string<char, std::char_traits<char>, std::allocator<char> >) + 0x9d (0x79e0a3500f6d in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #1: c10::TensorImpl::throw_cannot_call_with_symbolic(char const*) const + 0x8b (0x79e0a3494cab in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #2: c10::TensorImpl::numel_custom() const + 0x9c (0x79e0a34da3ec in /usr/local/lib/python3.12/dist-packages/torch/lib/libc10.so)\nframe #3: <unknown function> + 0x219a346 (0x79e08ee9f346 in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #4: at::native::quantile(at::Tensor const&, at::Tensor const&, std::optional<long>, bool, std::basic_string_view<char, std::char_traits<char> >) + 0x5d (0x79e08eeb694d in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #5: at::native::quantile(at::Tensor const&, double, std::optional<long>, bool, std::basic_string_view<char, std::char_traits<char> >) + 0x184 (0x79e08eeb6be4 in /usr/local/lib/python3.12/dist-packages/torch/lib/libtorch_cpu.so)\nframe #
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: TorchRuntimeError
E2_aot_eager: TorchRuntimeError
E3_inductor: TorchRuntimeError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method quantile of type object at 0x79e0a3182820>(*(FakeTensor(..., size=(s77,)), 0.5), **{}): got RuntimeError('Cannot call num
E2_aot_eager:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method quantile of type object at 0x79e0a3182820>(*(FakeTensor(..., size=(s77,)), 0.5), **{}): got RuntimeError('Cannot call num
E3_inductor:
  [exception] test raised TorchRuntimeError, ref returned normally
      exception                 : TorchRuntimeError
      message                   : Dynamo failed to run FX node with fake tensors: call_function <built-in method quantile of type object at 0x79e0a3182820>(*(FakeTensor(..., size=(s77,)), 0.5), **{}): got RuntimeError('Cannot call num
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/179383 — origin issue of this reproducer

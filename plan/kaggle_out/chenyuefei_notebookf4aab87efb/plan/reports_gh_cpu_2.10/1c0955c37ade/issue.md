# [pytorch compile] Incorrect result when `dtype` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `dtype` changes from `bfloat16` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: numerics depend on precision regardless of source (factor `self.dtype`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cpu
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Linux 6.12.90+
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def forward(self, x):
        x = F.pad(x, (0, 318))
        return x.stft(n_fft=512, hop_length=160, win_length=320,
                       return_complex=True, window=self._window, pad_mode="constant")

args = (torch.randn(2, 2).to(torch.bfloat16),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [(0.7469833493232727+0j), (8.696853183209896e-05+0j), 0j, (-0.7469154000282288+0.011072039604187012j), (3.2292911782860756e-05-8.075081132119521e-05j), 0j, (0.7467116117477417-0.022142529487609863j), (-6.298670632531866e-05-5.9968318964820355e-05j), ... (1534 more)]
```

## Actual Behavior

Compiled result:

```text
exception AssertionError: expected size 2==2, stride 257==771 at dim=0; expected size 3==3, stride 514==257 at dim=1
Error in op: torch.ops.aten._fft_r2c.default
This error most often comes from a incorrect fake (aka meta) kernel for a custom op.
Use torch.library.opcheck to test your custom op.
See https://pytorch.org/docs/stable/library.html#torch.library.opcheck
```

## Trigger Condition

Changed semantic factor:

```text
dtype: bfloat16 -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: AssertionError
```

## Difference

```text
E3_inductor:
  [exception] test raised AssertionError, ref returned normally
      exception                 : AssertionError
      message                   : expected size 2==2, stride 257==771 at dim=0; expected size 3==3, stride 514==257 at dim=1
Error in op: torch.ops.aten._fft_r2c.default
This error most often comes from a incorrect fake (aka meta) ker
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 4 -> 4 elements; 3 reductions kept of 9 tried.
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/179807 — origin issue of this reproducer

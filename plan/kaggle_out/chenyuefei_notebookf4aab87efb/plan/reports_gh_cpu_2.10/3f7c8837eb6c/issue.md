# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cpu
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Linux 6.12.90+
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def fn(x):
    x = _m_t3759174(x)
    x = _m_t3759182(x)
    x = _m_t3759183(x)
    return torch.softmax(x, dim=0).mean()

args = (torch.randn(3, 5, 14, 15).requires_grad_(True),)

eager = fn(*args)
torch._dynamo.reset()
compiled = torch.compile(fn, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-1.159511636e-11, 2.493610179e-11, -8.045793198e-12, 9.926263804e-12, -6.235690783e-12, 7.537334472e-12, 3.868561721e-11, 2.132626541e-11, ... (3142 more)]
```

## Actual Behavior

Compiled result:

```text
exception InternalTorchDynamoError: ModuleNotFoundError: No module named '_repro_issue_181175_aot_autograd_runtimeerror_cannot_call_nu'
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: InternalTorchDynamoError
E2_aot_eager: InternalTorchDynamoError
E3_inductor: InternalTorchDynamoError
```

## Difference

```text
E1_dynamo_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_181175_aot_autograd_runtimeerror_cannot_call_nu'
E2_aot_eager:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_181175_aot_autograd_runtimeerror_cannot_call_nu'
E3_inductor:
  [exception] test raised InternalTorchDynamoError, ref returned normally
      exception                 : InternalTorchDynamoError
      message                   : ModuleNotFoundError: No module named '_repro_issue_181175_aot_autograd_runtimeerror_cannot_call_nu'
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/181175 — origin issue of this reproducer

# [pytorch compile] Incorrect result when `compile` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `compile` changes from `{'dynamic': True}` to `(base)`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation
This test was generated because the static analysis found: compile flag changes specialization (factor `compile.dynamic`, oracles ['value', 'metadata']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cpu
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Linux 6.12.90+
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def model():
    t = inst_norm(x)
    t = pool(t)
    t = F.normalize(t, dim=0)
    t = torch.mul(t, torch.where(x > 0, x, x))
    t = hardswish(t)               # ← Inductor generates wrong kernel here
    t = F.normalize(t, dim=0)
    return t

args = (...)  # see execution_trace.json

eager = model(*args)
torch._dynamo.reset()
compiled = torch.compile(model, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.08439046144, -0.1005035639, -0.0293657966, -0.07997085154, 0.1486446112, 0.07495182008, -0.07102759182, -0.215301618, ... (3576 more)]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0.09857463092, 0.1193157434, 0.0160760507, 0.06371482462, -0.02941777743, -0.07560661435, 0.06272066385, 0.1600725651, ... (3576 more)]
```

## Trigger Condition

Changed semantic factor:

```text
compile: {'dynamic': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.858e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.8583117127418518
      first mismatching element : [0, 0, 0, 0]
      mismatching elements      : 3584
      total elements            : 3584
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.8583117127418518
      accepted budget           : 4.76837158203125e-07
      expected at that index    : -0.08439046144
      actual at that index      : 0.09857463092
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 8 -> 8 lines, inputs 0 -> 0 elements; 0 reductions kept of 5 tried.

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/181693 — origin issue of this reproducer

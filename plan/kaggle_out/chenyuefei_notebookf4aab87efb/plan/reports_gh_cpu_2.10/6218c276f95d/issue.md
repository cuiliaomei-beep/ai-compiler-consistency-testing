# [pytorch compile] Incorrect result when `requires_grad` changes

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution when `requires_grad` changes from `{'arg': 0, 'value': True}` to `(base)`.
divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)
This test was generated because the static analysis found: requires_grad changes the traced graph regardless of source (factor `x.requires_grad`, oracles ['value', 'metadata', 'gradient']).

## Environment
- Python: 3.12.13
- Framework: PyTorch 2.10.0+cpu
- Compiler backend: inductor
- Commit/version: 449b1768410104d3ed79d3bcfe4ba1d65c7f22c0
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Linux 6.12.90+
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x, dx):
    return torch.func.jvp(SinWithZeroJvp.apply, (x,), (dx,))[1]

args = (torch.randn(4).requires_grad_(True), torch.randn(4))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='eager')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [0, 0, 0, 0]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1.03963232, 0.6165617108, -0.2958724499, -0.1608417034]
```

## Trigger Condition

Changed semantic factor:

```text
requires_grad: {'arg': 0, 'value': True} -> (base)
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E1_dynamo_eager:
  [value] output[0]: compiled err vs fp64 = 1.040e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0396323204040527
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0396323204040527
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 1.03963232
  [gradient] grad[0]: present on one side only
E2_aot_eager:
  [value] output[0]: compiled err vs fp64 = 1.040e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0396323204040527
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0396323204040527
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 1.03963232
  [gradient] grad[0]: present on one side only
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.040e+00 exceeds 4.0x eager err 0.000e+00
      max absolute error        : 1.0396323204040527
      first mismatching element : [0]
      mismatching elements      : 4
      total elements            : 4
      eager error vs float64    : 0.0
      compiled error vs float64 : 1.0396323204040527
      accepted budget           : 4.76837158203125e-07
      expected at that index    : 0
      actual at that index      : 1.03963232
  [gradient] grad[0]: present on one side only
```

## Suspected Stage

```text
TorchDynamo (graph capture)
```

Attribution basis: first divergent layer = E1_dynamo_eager; divergence already present at backend='eager': Dynamo graph-capture semantics (often documented-unsupported behaviour)

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/192985 — origin issue of this reproducer

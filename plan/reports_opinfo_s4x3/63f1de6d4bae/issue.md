# [pytorch compile] Compiled execution inconsistent with eager (value)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def opinfo_nn_functional_hinge_embedding_loss_1105(*args):
    return torch.nn.functional.hinge_embedding_loss(*args, reduction='sum', margin=8.396163642458497)

args = (torch.randn(5), torch.randn(5))

eager = opinfo_nn_functional_hinge_embedding_loss_1105(*args)
torch._dynamo.reset()
compiled = torch.compile(opinfo_nn_functional_hinge_embedding_loss_1105, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
structure: T
[0] [-0.6207094193]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.6207103729]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: ok
E1_dynamo_eager: ok
E2_aot_eager: ok
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [value] output[0]: compiled err vs fp64 = 1.133e-06 exceeds 4.0x eager err 1.789e-07
      max absolute error        : 9.5367431640625e-07
      first mismatching element : []
      mismatching elements      : 1
      total elements            : 1
      eager error vs float64    : 1.788659673707116e-07
      compiled error vs float64 : 1.1325402837769616e-06
      accepted budget           : 7.154638694828463e-07
      expected at that index    : -0.6207094193
      actual at that index      : -0.6207103729
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

A float64 reference was computed; numeric findings are reported only when the compiled error against that reference exceeds the eager error by more than the configured multiplier.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 4.
```

## Minimization

Program 2 -> 2 lines, inputs 10 -> 10 elements; 2 reductions kept of 5 tried.
- kept: shrink containers
- kept: default flags

# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x):
    v1 = torch.full([1, 2], 1, dtype=torch.float32)                    # storage: 2 elements
    v2 = torch.as_strided(v1, size=[64, 64, 3], stride=[1, 64, 192])   # addresses 4480
    return torch.cumsum(v2, 1)

args = (torch.randn(1),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: setStorage: sizes [64, 64, 3], strides [1, 64, 192], storage offset 0, and itemsize 4 requiring a storage size of 17920 are out of bounds for storage of size 8
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [1, 0.8392887712, -2, 1, 0.8392887712, -2, 1, 0.8392887712, ... (12280 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : setStorage: sizes [64, 64, 3], strides [1, 64, 192], storage offset 0, and itemsize 4 requiring a storage size of 17920 are out of bounds for storage of size 8
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 4 -> 4 lines, inputs 64 -> 1 elements; 4 reductions kept of 5 tried.
- kept: drop rank
- kept: simplify values
- kept: shrink containers
- kept: default flags

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195665 — origin issue of this reproducer

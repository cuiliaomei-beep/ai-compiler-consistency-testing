# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(inp, m1, m2):
    return torch.addmm(inp, m1, m2)

args = (torch.randn(7, 1), torch.randn(1, 1), torch.randn(1, 1))

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception RuntimeError: The expanded size of the tensor (1) must match the existing size (500) at non-singleton dimension 0.  Target sizes: [1, 1].  Tensor sizes: [500, 1]
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [-0.9177620411, -0.9442824125, -0.04250079393, -0.2258010209, 1.056788206, 0.9000869393, -0.1079349816, -1.907141566, ... (492 more)]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: RuntimeError
E1_dynamo_eager: RuntimeError
E2_aot_eager: RuntimeError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised RuntimeError, test returned normally
      exception                 : RuntimeError
      message                   : The expanded size of the tensor (1) must match the existing size (500) at non-singleton dimension 0.  Target sizes: [1, 1].  Tensor sizes: [500, 1]
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 2 -> 2 lines, inputs 502 -> 9 elements; 9 reductions kept of 16 tried.
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0
- kept: halve dim 0
- kept: halve dim 0
- kept: halve dim 0
- kept: halve dim 0

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195579 — origin issue of this reproducer

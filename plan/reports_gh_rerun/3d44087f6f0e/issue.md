# [pytorch compile] Compiled execution inconsistent with eager (exception)

<!-- DRAFT generated by tcc. Review every section before filing (plan 18.6). -->

## Summary
The compiled execution is inconsistent with Python eager execution for context `base`.
all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Environment
- Python: 3.14.7
- Framework: PyTorch 2.14.0+cpu
- Compiler backend: inductor
- Commit/version: 08187d9e0fba026dc8217405802ab5381dc88d90
- CUDA/ROCm: none (CPU)
- GPU: cpu
- OS: Windows 11
- Triton: None

## Minimal Reproducer

```python
import torch
torch.manual_seed(0)

def f(x):
    return torch.rand_like(x)

args = (torch.randint(0, 4, (1,)),)

eager = f(*args)
torch._dynamo.reset()
compiled = torch.compile(f, backend='inductor')(*args)
print('eager   :', eager)
print('compiled:', compiled)
```

## Expected Behavior

Eager/reference result:

```text
exception NotImplementedError: "check_uniform_bounds" not implemented for 'Long'
```

## Actual Behavior

Compiled result:

```text
structure: T
[0] [0, 0, 0, 0]
```

## Trigger Condition

Changed semantic factor:

```text
(single context): - -> -
```

Controlled factors:

```text
shape / dtype / stride / device / compile flags: unchanged
```

Execution sequence:

```text
E0_eager: NotImplementedError
E1_dynamo_eager: NotImplementedError
E2_aot_eager: NotImplementedError
E3_inductor: ok
```

## Difference

```text
E3_inductor:
  [exception] ref raised NotImplementedError, test returned normally
      exception                 : NotImplementedError
      message                   : "check_uniform_bounds" not implemented for 'Long'
```

## Suspected Stage

```text
TorchInductor (lowering / codegen)
```

Attribution basis: first divergent layer = E3_inductor; all pre-Inductor layers agree and only Inductor differs: lowering / code generation

## Accuracy methodology

No float64 reference could be built; a dtype-aware tolerance was used instead.

## Reproducibility

```text
Reproduced 2/2 runs with fixed seed 0.
```

## Minimization

Program 2 -> 2 lines, inputs 4 -> 1 elements; 5 reductions kept of 5 tried.
- kept: halve dim 0
- kept: simplify values
- kept: shrink containers
- kept: default flags
- kept: halve dim 0

## Possible existing issues

- https://github.com/pytorch/pytorch/issues/195673 — origin issue of this reproducer

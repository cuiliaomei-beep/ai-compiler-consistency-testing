# torch.compile 一致性缺陷 — issue 稿（2026-09-13）

27 条新 issue，编号即建议提交顺序（小号先提）。全部针对 pytorch/pytorch，都在 torch 2.14.0 与 nightly、Windows 与 Linux 上复核过；每条正文开头的 **Summary** 段按 `SUMMARY_WRITING_GUIDE.md` 的三句结构写（事实 / 困惑 / 暴露），其余字段按仓库的 issue 模板（`pt2-bug-report.yml`；第 16 条是 eager 缺陷，用 `bug-report.yml`）。文件头部的表格是提交信息，不要复制进 issue。

| # | 类型 | 模板 | 标题 | 文件 |
|---|---|---|---|---|
| 01 | **已提交 → https://github.com/pytorch/pytorch/issues/197084 （2026-09-15）** | pt2-bug-report.yml | [aot_autograd] wrong gradients for `torch.linalg.pinv` and `torch.linalg.matrix_sqrth` with complex input under torch.compile (aot_eager and inductor); eager, functorch and Dynamo-only are correct | [01-aot-autograd-complex-pinv-matrix-sqrth-gradient.md](01-aot-autograd-complex-pinv-matrix-sqrth-gradient.md) |
| 02 | issue | pt2-bug-report.yml | [dynamo] `random.shuffle` / `random.sample` inside a compiled function return the same permutation on every call (eager advances the RNG) | [02-dynamo-random-shuffle-sample-frozen.md](02-dynamo-random-shuffle-sample-frozen.md) |
| 03 | issue | pt2-bug-report.yml | [aot_autograd] process crash (access violation) when `torch.func.jvp` over `torch.ldexp` is compiled with `backend="aot_eager"` | [03-aot-autograd-jvp-ldexp-process-crash.md](03-aot-autograd-jvp-ldexp-process-crash.md) |
| 04 | issue | pt2-bug-report.yml | [inductor][cpu] argmin/argmax under `dynamic=True` emits the index expression with Python `**` into C++ → CppCompileError | [04-inductor-cpu-argmin-dynamic-pow-in-cpp.md](04-inductor-cpu-argmin-dynamic-pow-in-cpp.md) |
| 05 | issue | pt2-bug-report.yml | [inductor][cudagraphs] `x.new_zeros(shape, device="cpu")` inside a compiled function returns a **CUDA** tensor when `triton.cudagraphs` is on (eager and default config return a CPU tensor) | [05-inductor-cudagraphs-new-zeros-cpu-device.md](05-inductor-cudagraphs-new-zeros-cpu-device.md) |
| 06 | issue | pt2-bug-report.yml | [inductor][cpu] `torch.std`/`var` accumulate in float32: inf/NaN for 1e30-scale inputs, and **zero output with an all-zero gradient** for 1e-30-scale inputs (eager and float64 are correct) | [06-inductor-cpu-std-var-fp32-accumulation.md](06-inductor-cpu-std-var-fp32-accumulation.md) |
| 07 | issue | pt2-bug-report.yml | [dynamo] `torch.func.jvp` inside a compiled function hits `INTERNAL ASSERT FAILED: InferenceMode::is_enabled() && primal.is_inference() && tangent.is_inference()` (`_make_dual`) for `torch.quantile` / `nanquantile` / `addr` | [07-dynamo-jvp-make-dual-internal-assert.md](07-dynamo-jvp-make-dual-internal-assert.md) |
| 08 | issue | pt2-bug-report.yml | [inductor] `torch.ldexp` output dtype differs from eager in both directions (int/bool × half → float32; half × float32 → half) | [08-inductor-ldexp-output-dtype.md](08-inductor-ldexp-output-dtype.md) |
| 09 | issue | pt2-bug-report.yml | [compiled autograd] backward of `F.interpolate(mode="bilinear"/"bicubic"/"linear"/"trilinear")` fails with `isIntList() INTERNAL ASSERT FAILED` while running meta for `aten.upsample_bilinear2d_backward` | [09-compiled-autograd-interpolate-isintlist-assert.md](09-compiled-autograd-interpolate-isintlist-assert.md) |
| 10 | issue | pt2-bug-report.yml | [dynamo] `dynamic=True`: f-string with a format spec on a SymInt/SymFloat (`f"{x.shape[0]:d}"`) raises InternalTorchDynamoError "Unknown format code 'd' for object of type 'str'" | [10-dynamo-dynamic-fstring-format-spec.md](10-dynamo-dynamic-fstring-format-spec.md) |
| 11 | issue | pt2-bug-report.yml | [inductor] `torch.compile(optimizer.step)` fails for SGD with `foreach=True` on complex parameters: "AssertionError: <built-in function getitem> is not an OpOverload" (eager and aot_eager work) | [11-inductor-compiled-sgd-foreach-complex.md](11-inductor-compiled-sgd-foreach-complex.md) |
| 12 | issue | pt2-bug-report.yml | [inductor] `F.interpolate` on a zero-size spatial dim returns uninitialised data (NaN) from an out-of-bounds read instead of raising; output size 0 is accepted silently | [12-inductor-interpolate-zero-size-oob-nan.md](12-inductor-interpolate-zero-size-oob-nan.md) |
| 13 | issue | pt2-bug-report.yml | [export][aoti] In-place update of a captured (lifted-constant) tensor: `torch.export` succeeds and silently drops the update, `run_decompositions` raises the intended error, `aoti_compile_and_package` raises an INTERNAL ASSERT ("please report a bug") | [13-export-aoti-lifted-constant-mutation.md](13-export-aoti-lifted-constant-mutation.md) |
| 14 | issue | pt2-bug-report.yml | [pt2] `linalg.vector_norm(ord=inf / -inf / negative, dim=-1)` on an empty *batch* fails at trace time — the empty-dimension check in `_refs/linalg` does not canonicalize a negative `dim` (eager returns an empty result) | [14-refs-vector-norm-negative-dim-empty-batch.md](14-refs-vector-norm-negative-dim-empty-batch.md) |
| 15 | issue | pt2-bug-report.yml | [inductor] `binary_cross_entropy` with a weight of another dtype and `huber_loss` with mixed low-precision inputs return float32 under Inductor; eager and aot_eager return the input dtype | [15-inductor-bce-huber-loss-mixed-dtype.md](15-inductor-bce-huber-loss-mixed-dtype.md) |
| 16 | issue（eager） | bug-report.yml | Backward of `F.pdist` on an input with zero rows crashes the process (SIGSEGV on Linux, integer division by zero on Windows), no Python exception | [16-eager-pdist-backward-empty-rows-crash.md](16-eager-pdist-backward-empty-rows-crash.md) |
| 17 | issue | pt2-bug-report.yml | [dynamo] `random.seed()` inside a compiled function is ignored on the first (compiling) call — first call returns values from a different RNG state | [17-dynamo-random-seed-first-call.md](17-dynamo-random-seed-first-call.md) |
| 18 | issue | pt2-bug-report.yml | [dynamo] Python 3.14 + `dynamic=True`: `==` between namedtuples that hold tensors overflows the stack (RecursionError) instead of comparing / raising like eager | [18-dynamo-dynamic-namedtuple-eq-recursion-py314.md](18-dynamo-dynamic-namedtuple-eq-recursion-py314.md) |
| 19 | issue | pt2-bug-report.yml | [inductor][cpu][windows] `max_autotune`: bmm C++ template kernel is not exported (`extern "C"` without `__declspec(dllexport)`) → `AttributeError: function 'cpp_CppMicroGemmFP32Vec' not found` | [19-inductor-windows-bmm-max-autotune-export.md](19-inductor-windows-bmm-max-autotune-export.md) |
| 20 | issue | pt2-bug-report.yml | [aoti] `aoti_compile_and_package` fails for any model returning a `torch.return_types.*` named tuple (`torch.max(x, dim)`, `topk`, `cummax`, …): "No registered serialization name for <class 'torch.return_types.max'>" | [20-aoti-return-types-namedtuple-packaging.md](20-aoti-return-types-namedtuple-packaging.md) |
| 21 | issue | pt2-bug-report.yml | [inductor] `torch.lerp(bool_tensor, bool_tensor, scalar_tensor_weight)` works in eager but fails to compile: "Subtraction, the `-` operator, with two bool tensors is not supported" | [21-inductor-lerp-bool-scalar-weight.md](21-inductor-lerp-bool-scalar-weight.md) |
| 22 | issue | pt2-bug-report.yml | [inductor] `torch.sum(x_half, dtype=torch.bool)` fails with a C++ compile error and `torch.prod(x, dtype=torch.bool)` with "expected reduction_type in (min, max, any, sum)"; both work in eager | [22-inductor-cpu-sum-prod-dtype-bool.md](22-inductor-cpu-sum-prod-dtype-bool.md) |
| 23 | issue | pt2-bug-report.yml | [inductor] `max_autotune`: `torch.addmm` with a 0-d (scalar) bias fails in `tuned_addmm` with `IndexError: list index out of range` (`inp.get_stride()[0]` evaluated before the rank check) | [23-inductor-addmm-scalar-bias-max-autotune.md](23-inductor-addmm-scalar-bias-max-autotune.md) |
| 24 | issue | pt2-bug-report.yml | [inductor] `torch.var_mean` / `torch.std_mean` on an empty tensor return mean `0.` instead of `nan` under `torch.compile` | [24-inductor-var-mean-empty-mean-zero.md](24-inductor-var-mean-empty-mean-zero.md) |
| 25 | issue | pt2-bug-report.yml | [inductor] `F.channel_shuffle` / `native_channel_shuffle` drop the channels_last memory format of the input (eager and aot_eager preserve it) | [25-inductor-channel-shuffle-channels-last.md](25-inductor-channel-shuffle-channels-last.md) |
| 26 | issue | pt2-bug-report.yml | [dynamo] `str(KeyError(...))` inside a traced region omits the quotes CPython adds (`"'k'"` in eager, `'k'` compiled) | [26-dynamo-keyerror-str-quotes.md](26-dynamo-keyerror-str-quotes.md) |
| 27 | issue | pt2-bug-report.yml | [fake tensor] `torch.abs(complex, out=real_tensor_of_other_dtype)` is accepted by eager but rejected under torch.compile (meta requires out dtype == real dtype) | [27-meta-abs-complex-out-dtype.md](27-meta-abs-complex-out-dtype.md) |

**2026-09-15 独立复核**（`plan/scripts/verify_0913_issues.py`，27 条各在新进程重写复现，2.14 与 nightly 各跑一遍；`results/verify_0913_{214,nightly}.log`）：25 条重现，05 需 GPU（引用 Kaggle T4 结果）；两条据此修正：07 的矩阵改为逐用例独立进程的结果（三个后端全部断言，并注明同进程内的状态依赖），13 删去"export 静默丢更新"的错误说法（导出模块与 eager 一样原地更新，其余两点不变）。

提交链接：torch.compile 问题 → https://github.com/pytorch/pytorch/issues/new?template=pt2-bug-report.yml ；普通 bug（16）→ https://github.com/pytorch/pytorch/issues/new?template=bug-report.yml 。

对应关系：本目录编号 → 原 `plan/TO_SUBMIT.md` 候选编号：01→24，02→14，03→27，04→1，05→13，06→5，07→28，08→18，09→25，10→16，11→23，12→3，13→12，14→4，15→8，16→7，17→15，18→17，19→2，20→11，21→20，22→21，23→10，24→6，25→9，26→22，27→30。

证据与复现脚本都在 `plan/`（各文件头部"证据包"一行给出路径）；模板要求的 fuzzer 事项：比较使用默认容差、以 float64/complex128 为基准、比较前重置随机种子，各条正文已按此写；按模板说明，这类由自动差分发现且满足上述条件的 issue 提交时加标签 `topic: fuzzer`（数值类：01、06、08、15、24、25）。
